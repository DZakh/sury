// @generated by protoc-gen-sury with parameter "target=res"
// @generated from file example/v1/kitchen_sink.proto (package example.v1, syntax proto3)
@@warning("-30")

let number = S.union([S.float, S.literal(Float.Constants.nan)])

/**
 The kinds of user.
 */
module User_Kind = {
  type t = | @as(0) Unspecified | @as(1) Person | @as(2) Robot
  let schema: S.t<t> = S.enum([Unspecified, Person, Robot])
}

module Reserved_Enum = {
  type t = | @as(0) Unspecified
  let schema: S.t<t> = S.enum([Unspecified])
}

/**
 An enum whose values keep their prefix: not every value shares it.
 */
module Mixed = {
  type t = | @as(0) MixedUnspecified | @as(1) Other
  let schema: S.t<t> = S.enum([MixedUnspecified, Other])
}

module Aliased = {
  type t = | @as(0) Unspecified | @as(1) One
  let schema: S.t<t> = S.enum([Unspecified, One])
}

/**
 Only one value.
 */
module Lonely = {
  type t = | @as(0) Unspecified
  let schema: S.t<t> = S.enum([Unspecified])
}

/**
 Every scalar protobuf has, once each.
 */
module Scalars = {
  type t = {
    double: float,
    float: float,
    int32: int,
    int64: bigint,
    uint32: S.integer,
    uint64: bigint,
    sint32: int,
    sint64: bigint,
    fixed32: S.integer,
    fixed64: bigint,
    sfixed32: int,
    sfixed64: bigint,
    bool: bool,
    string: string,
    bytes: Uint8Array.t,
    asString: string,
  }
  let schema: S.t<t> = S.schema(s => {
    double: s.matches(number->S.protobufField(1, ~type_=#double)),
    float: s.matches(number->S.protobufField(2, ~type_=#float)),
    int32: s.matches(S.int->S.protobufField(3, ~type_=#int32)),
    int64: s.matches(S.bigint->S.protobufField(4, ~type_=#int64)),
    uint32: s.matches(S.integer->S.protobufField(5, ~type_=#uint32)),
    uint64: s.matches(S.bigint->S.protobufField(6, ~type_=#uint64)),
    sint32: s.matches(S.int->S.protobufField(7, ~type_=#sint32)),
    sint64: s.matches(S.bigint->S.protobufField(8, ~type_=#sint64)),
    fixed32: s.matches(S.integer->S.protobufField(9, ~type_=#fixed32)),
    fixed64: s.matches(S.bigint->S.protobufField(10, ~type_=#fixed64)),
    sfixed32: s.matches(S.int->S.protobufField(11, ~type_=#sfixed32)),
    sfixed64: s.matches(S.bigint->S.protobufField(12, ~type_=#sfixed64)),
    bool: s.matches(S.bool->S.protobufField(13, ~type_=#bool)),
    string: s.matches(S.string->S.protobufField(14, ~type_=#string)),
    bytes: s.matches(S.uint8Array->S.protobufField(15, ~type_=#bytes)),
    asString: s.matches(S.string->S.protobufField(16, ~type_=#int64)),
  })
}

/**
 Where a user receives mail.
 */
module User_Mailbox = {
  type t = {
    name: string,
    address: option<Example_common_address_pb.Address.t>,
  }
  let schema: S.t<t> = S.schema(s => {
    name: s.matches(S.string->S.protobufField(1, ~type_=#string)),
    address: s.matches(S.option(Example_common_address_pb.Address.schema)->S.protobufField(2, ~type_=#message)),
  })
}

/**
 A user of the system.
 */
module User = {
  @tag("case")
  type contact =
    | @as("email") Email({value: string})
    | @as("phone") Phone({value: bigint})
    | @as("mailbox") Mailbox({value: User_Mailbox.t})
    | @as("boxedInOneof") BoxedInOneof({value: SuryProtobuf.Int32Value.t})
  type t = {
    id: int,
    firstName: string,
    tags: array<string>,
    scores: dict<int>,
    home: option<Example_common_address_pb.Address.t>,
    nickname: option<string>,
    kind: User_Kind.t,
    contact: option<contact>,
    unpacked: array<int>,
    mailboxes: dict<User_Mailbox.t>,
    currency: Example_common_address_pb.Currency.t,
    maybeKind: option<User_Kind.t>,
    deprecatedField: string,
    kinds: array<User_Kind.t>,
    byId: dict<string>,
  }
  let schema: S.t<t> = S.schema(s => {
    id: s.matches(S.int->S.protobufField(1, ~type_=#int32)),
    firstName: s.matches(S.string->S.protobufField(2, ~type_=#string)),
    tags: s.matches(S.array(S.string)->S.protobufField(3, ~type_=#string)),
    scores: s.matches(S.dict(S.int)->S.protobufField(4, ~type_=#int32, ~key=#string)),
    home: s.matches(S.option(Example_common_address_pb.Address.schema)->S.protobufField(5, ~type_=#message)),
    nickname: s.matches(S.option(S.string)->S.protobufField(6, ~type_=#string)),
    kind: s.matches(User_Kind.schema->S.protobufField(7, ~type_=#enum)),
    contact: s.matches(S.option(
      S.union([
        S.schema(s => Email({value: s.matches(S.string->S.protobufField(8, ~type_=#string))})),
        S.schema(s => Phone({value: s.matches(S.bigint->S.protobufField(9, ~type_=#int64))})),
        S.schema(s => Mailbox({value: s.matches(User_Mailbox.schema->S.protobufField(10, ~type_=#message))})),
        S.schema(s => BoxedInOneof({value: s.matches(SuryProtobuf.Int32Value.schema->S.protobufField(11, ~type_=#message))})),
      ]),
    )),
    unpacked: s.matches(S.array(S.int)->S.protobufField(12, ~type_=#int32, ~packed=false)),
    mailboxes: s.matches(S.dict(User_Mailbox.schema)->S.protobufField(13, ~type_=#message, ~key=#int32)),
    currency: s.matches(Example_common_address_pb.Currency.schema->S.protobufField(14, ~type_=#enum)),
    maybeKind: s.matches(S.option(User_Kind.schema)->S.protobufField(15, ~type_=#enum)),
    deprecatedField: s.matches(S.string->S.protobufField(16, ~type_=#string)),
    kinds: s.matches(S.array(User_Kind.schema)->S.protobufField(17, ~type_=#enum)),
    byId: s.matches(S.dict(S.string)->S.protobufField(18, ~type_=#string, ~key=#uint64)),
  })
}

module WellKnown = {
  type t = {
    at: option<SuryProtobuf.Timestamp.t>,
    took: option<SuryProtobuf.Duration.t>,
    label: option<string>,
    count: option<bigint>,
    meta: option<dict<JSON.t>>,
    metas: array<dict<JSON.t>>,
    byName: dict<dict<JSON.t>>,
    value: option<SuryProtobuf.Value.t>,
    boxedList: array<SuryProtobuf.Int32Value.t>,
  }
  let schema: S.t<t> = S.schema(s => {
    at: s.matches(S.option(SuryProtobuf.Timestamp.schema)->S.protobufField(1, ~type_=#message)),
    took: s.matches(S.option(SuryProtobuf.Duration.schema)->S.protobufField(2, ~type_=#message)),
    label: s.matches(S.option(S.string)->S.protobufField(3, ~type_=#"google.protobuf.StringValue")),
    count: s.matches(S.option(S.bigint)->S.protobufField(4, ~type_=#"google.protobuf.Int64Value")),
    meta: s.matches(S.option(S.dict(S.json))->S.protobufField(5, ~type_=#"google.protobuf.Struct")),
    metas: s.matches(S.array(S.dict(S.json))->S.protobufField(6, ~type_=#"google.protobuf.Struct")),
    byName: s.matches(S.dict(S.dict(S.json))->S.protobufField(7, ~type_=#"google.protobuf.Struct", ~key=#string)),
    value: s.matches(S.option(SuryProtobuf.Value.schema)->S.protobufField(8, ~type_=#message)),
    boxedList: s.matches(S.array(SuryProtobuf.Int32Value.schema)->S.protobufField(9, ~type_=#message)),
  })
}

@tag("case")
type rec node_payload =
  | @as("leaf") Leaf({value: int})
  | @as("link") Link({value: node})
and node = {
  name: string,
  children: array<node>,
  parent: option<node>,
  payload: option<node_payload>,
}

/**
 A tree: a message that holds itself.
 */
module Node = {
  type t = node
  let schema: S.t<t> = S.recursive("Node", self =>
    S.schema(s => {
      name: s.matches(S.string->S.protobufField(1, ~type_=#string)),
      children: s.matches(S.array(self)->S.protobufField(2, ~type_=#message)),
      parent: s.matches(S.option(self)->S.protobufField(3, ~type_=#message)),
      payload: s.matches(S.option(
        S.union([
          S.schema(s => Leaf({value: s.matches(S.int->S.protobufField(4, ~type_=#int32))})),
          S.schema(s => Link({value: s.matches(self->S.protobufField(5, ~type_=#message))})),
        ]),
      )),
    })
  )
}

type rec branch = {
  n: int,
  leaf: option<leaf>,
}
and leaf = {
  branches: array<branch>,
}

/**
 Two messages that hold each other.
 */
module Branch = {
  type t = branch
  let schema: S.t<t> = S.recursive("Branch", self =>
    S.schema(s => {
      n: s.matches(S.int->S.protobufField(1, ~type_=#int32)),
      leaf: s.matches(S.option(S.recursive("Leaf", (_self2: S.t<leaf>) =>
      S.schema(s => {
        branches: s.matches(S.array(self)->S.protobufField(1, ~type_=#message)),
      })
    ))->S.protobufField(2, ~type_=#message)),
    })
  )
}

module Leaf = {
  type t = leaf
  let schema: S.t<t> = S.recursive("Leaf", self =>
    S.schema(s => {
      branches: s.matches(S.array(S.recursive("Branch", (_self2: S.t<branch>) =>
      S.schema(s => {
        n: s.matches(S.int->S.protobufField(1, ~type_=#int32)),
        leaf: s.matches(S.option(self)->S.protobufField(2, ~type_=#message)),
      })
    ))->S.protobufField(1, ~type_=#message)),
    })
  )
}

/**
 Names protobuf-es escapes.
 */
module Reserved = {
  type t = {
    constructor_: string,
    toString_: string,
    valueOf_: int,
    enum: Reserved_Enum.t,
  }
  let schema: S.t<t> = S.schema(s => {
    constructor_: s.matches(S.string->S.protobufField(1, ~type_=#string)),
    toString_: s.matches(S.string->S.protobufField(2, ~type_=#string)),
    valueOf_: s.matches(S.int->S.protobufField(3, ~type_=#int32)),
    enum: s.matches(Reserved_Enum.schema->S.protobufField(4, ~type_=#enum)),
  })
}

module Empty = {
  type t
  let schema: S.t<t> = S.schema(_ => %raw(`{}`))
}

module Uses = {
  type t = {
    mixed: Mixed.t,
    aliased: Aliased.t,
    lonely: Lonely.t,
    mixes: array<Mixed.t>,
    aliases: dict<Aliased.t>,
  }
  let schema: S.t<t> = S.schema(s => {
    mixed: s.matches(Mixed.schema->S.protobufField(1, ~type_=#enum)),
    aliased: s.matches(Aliased.schema->S.protobufField(2, ~type_=#enum)),
    lonely: s.matches(Lonely.schema->S.protobufField(3, ~type_=#enum)),
    mixes: s.matches(S.array(Mixed.schema)->S.protobufField(4, ~type_=#enum)),
    aliases: s.matches(S.dict(Aliased.schema)->S.protobufField(5, ~type_=#enum, ~key=#string)),
  })
}
