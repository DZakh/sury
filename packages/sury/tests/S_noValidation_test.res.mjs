// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "../src/S.res.mjs";
import * as U from "./U.res.mjs";
import Ava from "ava";

Ava("Successfully parses", (function (t) {
        var schemaWithoutTypeValidation = S.noValidation(S.string, true);
        U.assertThrows(t, (function () {
                return S.parseOrThrow(1, S.string);
              }), {
              operation: "Parse",
              code: {
                TAG: "InvalidType",
                expected: S.string,
                received: 1
              },
              path: S.Path.empty
            });
        t.deepEqual(S.parseOrThrow(1, schemaWithoutTypeValidation), 1, undefined);
      }));

Ava("Works for literals", (function (t) {
        var schema = S.literal("foo");
        var schemaWithoutTypeValidation = S.noValidation(schema, true);
        U.assertThrows(t, (function () {
                return S.parseOrThrow(1, schema);
              }), {
              operation: "Parse",
              code: {
                TAG: "InvalidType",
                expected: schema,
                received: 1
              },
              path: S.Path.empty
            });
        t.deepEqual(S.parseOrThrow(1, schemaWithoutTypeValidation), 1, undefined);
        U.assertCompiledCodeIsNoop(t, schemaWithoutTypeValidation, "Parse", undefined);
      }));

export {
  
}
/*  Not a pure module */
