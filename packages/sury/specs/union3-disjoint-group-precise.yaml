# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.string.with(S.minLength, 3), S.string.with(S.maxLength, 1), S.number.with(S.gt, 5)])
  input: string | number
  output: string | number
  instantiations: 2365
jsonSchema:
  input: '{ anyOf: [{ type: "string", minLength: 3 }, { type: "string", maxLength: 1 }, { type: "number", exclusiveMinimum: 5 }] }'
  output: '{ anyOf: [{ type: "string", minLength: 3 }, { type: "string", maxLength: 1 }, { type: "number", exclusiveMinimum: 5 }] }'
  openapi-3.0:
    input: '{ anyOf: [{ type: "string", minLength: 3 }, { type: "string", maxLength: 1 }, { type: "number", minimum: 5, exclusiveMinimum: true }] }'
    output: '{ anyOf: [{ type: "string", minLength: 3 }, { type: "string", maxLength: 1 }, { type: "number", minimum: 5, exclusiveMinimum: true }] }'
isEqual: strictEqual
compare: "throws: [Sury] Can't compare string.length >= 3 | string.length <= 1 | number > 5. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
vs:
  zod: z.union([z.string().min(3), z.string().max(1), z.number().gt(5)])
operations:
  parse:
    expression: i=>{try{let v0;for(;;){l3:{if(typeof i==="string"){for(;;){l1:{if(!(i.length>2)){(v0||(v0=[])).push(e[0],i,void 0);break l1};break}l2:{if(!(i.length<2)){(v0||(v0=[])).push(e[1],i,void 0);break l2};break}break l3};break}if(typeof i==="number"&&i==i){i>5||e[2](i);break}}throw e[3](i,v0)}return i}catch(v4){e[4](v4)}}
    examples:
      long-string:
        input: '"abc"'
        output: '"abc"'
      short-string:
        input: '"a"'
        output: '"a"'
      number:
        input: "7"
        output: "7"
      string-rejected-by-both:
        input: '"ab"'
        error: |-
          Expected string.length >= 3 | string.length <= 1 | number > 5, received "ab"
          - Expected string.length >= 3, received "ab"
          - Expected string.length <= 1, received "ab"
      number-rejected-never-reached-string-members:
        input: "1"
        error: Expected number > 5, received 1
  decode: identity
  encode: identity
