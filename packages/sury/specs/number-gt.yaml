# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.number.with(S.gt, 5)
  input: number
  output: number
  instantiations: 5179
vs:
  zod: z.number().gt(5)
jsonSchema:
  input: '{ type: "number", exclusiveMinimum: 5 }'
  output: '{ type: "number", exclusiveMinimum: 5 }'
operations:
  parse:
    expression: i=>{typeof i==="number"&&!Number.isNaN(i)||e[1](i);i>5||e[0](i);return i}
    examples:
      just-above:
        input: "5.5"
        output: "5.5"
      above:
        input: "6"
        output: "6"
      at-the-bound:
        input: "5"
        error: Number must be greater than 5
      below:
        input: "4"
        error: Number must be greater than 5
      infinity:
        input: Infinity
        output: Infinity
      nan:
        input: NaN
        error: Expected number, received NaN
      wrong-type:
        input: '"6"'
        error: Expected number, received "6"
  decode:
    expression: i=>{i>5||e[0](i);return i}
    examples:
      just-above:
        input: "5.5"
        output: "5.5"
      at-the-bound:
        input: "5"
        error: Number must be greater than 5
      below:
        input: "4"
        error: Number must be greater than 5
  encode:
    expression: i=>{i>5||e[0](i);return i}
    examples:
      just-above:
        input: "5.5"
        output: "5.5"
      at-the-bound:
        input: "5"
        error: Number must be greater than 5
      below:
        input: "4"
        error: Number must be greater than 5
