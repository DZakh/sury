# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.string.with(S.to, S.number, (string) => { const number = Number(string); if (Number.isNaN(number)) { throw new Error("Invalid number") } return number }, (number) => { if (number < 100) { throw new Error("Number is too small") } return number.toString() })
  input: string
  output: number
  instantiations: 442
jsonSchema:
  input: '{ type: "string" }'
  output: '{ type: "number" }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{typeof i==="string"||e[3](i);let v0;try{v0=e[0](i)}catch(x){e[1](x)}typeof v0==="number"&&!Number.isNaN(v0)||e[2](v0);return v0}
    examples:
      valid:
        input: '"123"'
        output: "123"
      small:
        input: '"80"'
        output: "80"
      user-error:
        input: '"asdf"'
        error: Invalid number
  decode:
    expression: i=>{let v0;try{v0=e[0](i)}catch(x){e[1](x)}typeof v0==="number"&&!Number.isNaN(v0)||e[2](v0);return v0}
    examples:
      valid:
        input: '"123"'
        output: "123"
  encode:
    creationError: "SuryError: Encoding is ambiguous when only a decode function is provided. Use S.to(target, {decode, encode})"
