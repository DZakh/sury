# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: 'S.fromJSONSchema({ type: "string", minLength: 5, maxLength: 1 })'
  input: never
  output: never
  instantiations: 754
vs:
  zod:
    _skip: not-applicable
jsonSchema:
  input: "{ not: {} }"
  output: "{ not: {} }"
operations:
  parse:
    expression: i=>{e[0](i);return i}
    examples:
      rejects:
        input: '"anything"'
        error: Expected never, received "anything"
  decode: eq-to-parse
  encode: eq-to-parse
