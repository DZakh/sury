# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.string.with(S.minLength, 3), S.string])
  input: string
  output: string
  instantiations: 1362
jsonSchema:
  input: '{ anyOf: [{ type: "string", minLength: 3 }, { type: "string" }] }'
  output: '{ anyOf: [{ type: "string", minLength: 3 }, { type: "string" }] }'
isEqual: strictEqual
compare: strictCompare
vs:
  zod: z.union([z.string().min(3), z.string()])
operations:
  parse:
    expression: i=>{try{let v0;if(typeof i==="string"){for(;;){l1:{if(!(i.length>2)){(v0||(v0=[])).push(e[0],i,void 0);break l1};break}break;}}else{throw e[1](i,v0)}return i}catch(v2){e[2](v2)}}
    examples:
      refined-member:
        input: '"long"'
        output: '"long"'
      broad-member:
        input: '"no"'
        output: '"no"'
      wrong-tag:
        input: "42"
        error: Expected string.length >= 3 | string, received 42
  decode: identity
  encode: identity
