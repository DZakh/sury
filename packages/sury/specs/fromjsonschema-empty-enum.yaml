# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: "S.fromJSONSchema({ enum: [] })"
  input: never
  output: never
  instantiations: 630
vs:
  zod: z.never()
jsonSchema:
  input: "{ not: {} }"
  output: "{ not: {} }"
operations:
  parse:
    expression: i=>{e[0](i);return i}
    examples:
      rejects-string:
        input: '"anything"'
        error: Expected never, received "anything"
      rejects-null:
        input: "null"
        error: Expected never, received null
  decode: eq-to-parse
  encode: eq-to-parse
