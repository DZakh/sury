# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.env.with(S.to, S.port)
  input: string | undefined
  output: number
  instantiations: 4419
jsonSchema:
  input: '{ type: "string" }'
  fromInputType: string
  output: '{ type: "integer", minimum: 0, maximum: 65535 }'
isEqual: strictEqual
compare:
  input: (a,b)=>a===b?0:a===void 0?b===void 0?0:-1:b===void 0?1:a<b?-1:a>b?1:0
  output: strictCompare
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{(typeof i==="string"||i===void 0)||e[2](i);let v0=+i;v0==v0&&(v0||i.trim())||e[1](i);v0>=0&&v0<65536&&v0%1==0||e[0](v0);return v0}catch(v1){e[3](v1)}}
    examples:
      valid:
        input: '"8080"'
        output: "8080"
      blank:
        input: '""'
        error: Expected port, received ""
      not-a-number:
        input: '"x"'
        error: Expected port, received "x"
      too-big:
        input: '"70000"'
        error: Expected port, received 70000
  decode:
    expression: i=>{try{let v0=+i;v0==v0&&(v0||i.trim())||e[1](i);v0>=0&&v0<65536&&v0%1==0||e[0](v0);return v0}catch(v1){e[2](v1)}}
    examples:
      valid:
        input: '"8080"'
        output: "8080"
      unset:
        input: undefined
        error: Expected port, received undefined
  encode:
    expression: i=>{try{i>=0&&i<65536&&i%1==0||e[0](i);return ""+i}catch(v0){e[1](v0)}}
    examples:
      valid:
        input: "8080"
        output: '"8080"'
      too-big:
        input: "70000"
        error: Expected port, received 70000
