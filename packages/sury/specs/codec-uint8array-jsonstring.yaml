# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.uint8Array.with(S.to, S.jsonString)
  input: Uint8Array<ArrayBufferLike>
  output: string
  instantiations: 5091
vs:
  zod:
    _skip: not-applicable
jsonSchema:
  input: Expected JSON, received Uint8Array
  output: '{ type: "string" }'
operations:
  parse:
    expression: i=>{i instanceof e[2]||e[3](i);return e[1](e[0](i))}
    examples:
      ascii:
        input: new Uint8Array([72, 105])
        output: '"\"SGk=\""'
      not-valid-utf8:
        input: new Uint8Array([255, 254, 253])
        output: '"\"//79\""'
      empty:
        input: new Uint8Array([])
        output: '"\"\""'
  decode:
    expression: i=>{return e[1](e[0](i))}
    examples:
      ascii:
        input: new Uint8Array([72, 105])
        output: '"\"SGk=\""'
      not-valid-utf8:
        input: new Uint8Array([255, 254, 253])
        output: '"\"//79\""'
      empty:
        input: new Uint8Array([])
        output: '"\"\""'
  encode:
    expression: i=>{let v0;try{v0=JSON.parse(i)}catch(t){e[0](i)}typeof v0==="string"||e[4](v0);e[2].test(v0)||e[3](v0);return e[1](v0)}
    examples:
      ascii:
        input: '"\"SGk=\""'
        output: new Uint8Array([72, 105])
      not-valid-utf8:
        input: '"\"//79\""'
        output: new Uint8Array([255, 254, 253])
      not-base64:
        input: '"\"not base64!\""'
        error: Expected base64, received "not base64!"
