# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.nullable("foo")
  input: '"foo" | null'
  output: '"foo" | null'
  instantiations: 435
jsonSchema:
  input: '{ enum: ["foo", null] }'
  output: '{ enum: ["foo", null] }'
isEqual: strictEqual
compare: (a,b)=>a===b?0:(a===null?b===null?0:-1:b===null?1:0)
vs:
  zod: z.literal("foo").nullable()
operations:
  parse:
    expression: i=>{try{(typeof i==="string"&&i==="foo"||i===null)||e[0](i);return i}catch(v0){e[1](v0)}}
    examples:
      literal:
        input: '"foo"'
        output: '"foo"'
      null-passes-through:
        input: "null"
        output: "null"
      wrong-literal:
        input: '"bar"'
        error: Expected "foo" | null, received "bar"
      invalid-undefined:
        input: undefined
        error: Expected "foo" | null, received undefined
  decode: identity
  encode: identity
