# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.optional(S.string).with(S.to, S.string)
  input: string | undefined
  output: string
  instantiations: 4529
jsonSchema:
  input: '{ type: "string" }'
  fromInputType: string
  output: '{ type: "string" }'
isEqual: strictEqual
compare:
  input: (a,b)=>a===b?0:(a===void 0?b===void 0?0:-1:b===void 0?1:a<b?-1:a>b?1:0)
  output: strictCompare
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{typeof i==="string"||e[0](i);return i}
    examples:
      string:
        input: '"abc"'
        output: '"abc"'
      undefined-is-rejected:
        input: undefined
        error: Expected string, received undefined
      not-a-string:
        input: "12"
        error: Expected string, received 12
  decode: eq-to-parse
  encode: identity
