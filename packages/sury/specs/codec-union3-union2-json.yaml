# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.string, S.number, S.bigint]).with(S.to, S.union([S.json, S.bigint]))
  input: string | number | bigint
  output: bigint | JSON
  instantiations: 6901
vs:
  zod:
    _skip: not-applicable
jsonSchema:
  input: Expected JSON, received string | number | bigint
  output: Expected JSON, received JSON | bigint
operations:
  parse:
    creationError: "SuryError: Can't decode string | number | bigint to JSON | bigint: string has no variant of the same type on the other side. Use S.to on it, or S.never to make it unreachable"
  decode: eq-to-parse
  encode: eq-to-parse
