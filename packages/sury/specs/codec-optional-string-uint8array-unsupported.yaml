# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.optional(S.string).with(S.to, S.uint8Array)
  input: string | undefined
  output: Uint8Array<ArrayBufferLike>
  instantiations: 5401
jsonSchema:
  input: Expected JSON, received undefined | string
  output: Expected JSON, received Uint8Array
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Can't decode undefined to Uint8Array. Use S.to to define a custom decoder"
  decode: eq-to-parse
  encode:
    expression: i=>{for(;;){if(i===void 0)break;let v0=e[0].decode(i);i=v0;break;}return i}
    examples:
      bytes-back:
        input: new Uint8Array([104, 105])
        output: '"hi"'
