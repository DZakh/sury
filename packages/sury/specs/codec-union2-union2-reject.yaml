# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.number, S.string]).with(S.to, S.union([S.number.with(S.to, S.never), S.string]))
  input: string | number
  output: string
  instantiations: 6742
vs:
  zod:
    _skip: not-applicable
jsonSchema:
  input: '{ anyOf: [{ type: "number" }, { type: "string" }] }'
  output: '{ anyOf: [{ not: {} }, { type: "string" }] }'
operations:
  parse:
    creationError: "SuryError: Invalid operation: can't convert number | string to number | string — number has no same-type variant on the other side. Use S.to to say what you mean, or S.never to mark a variant unreachable"
  decode: eq-to-parse
  encode: eq-to-parse
