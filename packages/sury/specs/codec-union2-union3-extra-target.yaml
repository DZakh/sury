# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.string, S.number]).with(S.to, S.union([S.number, S.string, S.boolean]))
  input: string | number
  output: string | number | boolean
  instantiations: 5850
jsonSchema:
  input: '{ anyOf: [{ type: "string" }, { type: "number" }] }'
  output: '{ anyOf: [{ type: "number" }, { type: "string" }, { type: "boolean" }] }'
isEqual: strictEqual
compare:
  input: "throws: [Sury] Can't compare string | number. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
  output: "throws: [Sury] Can't compare number | string | boolean. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Ambiguous string | number -> number | string | boolean. Should boolean be decoded or ignored? Choose with S.to for string -> boolean, or S.never -> boolean"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Ambiguous number | string | boolean -> string | number. Should boolean be decoded or ignored? Choose with S.to for boolean -> string, or S.never -> boolean"
