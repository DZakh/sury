# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.bigint
  input: bigint
  output: bigint
  instantiations: 254
vs:
  zod: z.bigint()
jsonSchema:
  input: Expected JSON, received bigint
  output: Expected JSON, received bigint
operations:
  parse:
    expression: i=>{try{typeof i==="bigint"||e[0](i);return i}catch(x){throw e[1](x,i)}}
    examples:
      valid:
        input: 42n
        output: 42n
      invalid-number:
        input: "42"
        error: Expected bigint, received 42
  decode: identity
  encode: identity
