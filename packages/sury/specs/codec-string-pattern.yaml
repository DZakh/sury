# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.string.with(S.to, S.string.with(S.pattern, /^a$/))
  input: string
  output: string
  instantiations: 4817
jsonSchema:
  input: '{ type: "string", pattern: "^a$" }'
  output: '{ type: "string", pattern: "^a$" }'
isEqual: strictEqual
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{typeof i==="string"||e[2](i);e[0].test(i)||e[1](i);return i}catch(v0){e[3](v0)}}
    examples:
      valid:
        input: '"a"'
        output: '"a"'
      padded:
        input: '" a "'
        error: Invalid pattern
      other-text:
        input: '"b"'
        error: Invalid pattern
      not-a-string:
        input: "0"
        error: Expected string, received 0
  decode:
    expression: i=>{try{e[0].test(i)||e[1](i);return i}catch(v0){e[2](v0)}}
    examples:
      valid:
        input: '"a"'
        output: '"a"'
  encode:
    expression: i=>{try{e[0].test(i)||e[1](i);return i}catch(v0){e[2](v0)}}
    examples:
      valid:
        input: '"a"'
        output: '"a"'
