# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.optional(S.env).with(S.to, S.string)
  input: string | undefined
  output: string
  instantiations: 4587
jsonSchema:
  input: '{ type: "string" }'
  fromInputType: string
  output: '{ type: "string" }'
isEqual: strictEqual
compare:
  input: (a,b)=>a===b?0:(a===void 0?b===void 0?0:-1:b===void 0?1:a===void 0?b===void 0?0:-1:b===void 0?1:a<b?-1:a>b?1:0)
  output: strictCompare
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: 'SuryError: Ambiguous "" for string. Should a blank input be rejected, kept, or read as absent? Choose with S.nonEmpty, S.minLength(0), or S.optional'
  decode: eq-to-parse
  encode: identity
