# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.never.with(S.to, S.schema(null)), S.string]).with(S.to, S.union([S.schema(undefined), S.string]))
  input: string
  output: string | undefined
  instantiations: 6456
jsonSchema:
  input: '{ anyOf: [{ not: {} }, { type: "string" }] }'
  output: Expected JSON, received undefined | string
isEqual:
  input: (a,b)=>a===b||e[0](a,b)
  output: strictEqual
compare:
  input: (a,b)=>a===b?0:e[0](a,b)
  output: (a,b)=>a===b?0:(a===void 0?b===void 0?0:-1:b===void 0?1:a<b?-1:a>b?1:0)
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Ambiguous never | string -> undefined | string. Should undefined be decoded or ignored? Choose with S.to for string -> undefined, or S.never -> undefined"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Ambiguous undefined | string -> null | string. Should undefined be decoded or ignored? Choose with S.to for undefined -> string, or S.never -> undefined"
