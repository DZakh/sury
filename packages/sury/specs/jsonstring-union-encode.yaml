# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: 'S.union([S.schema({type: "user.created", id: S.bigint}), S.schema({type: "user.renamed", id: S.bigint, name: S.string})]).with(S.to, S.jsonString)'
  input: '{ type: "user.created"; id: bigint; } | { type: "user.renamed"; id: bigint; name: string; }'
  output: string
  instantiations: 8537
vs:
  zod:
    _skip: not-applicable
jsonSchema:
  input: 'Failed at ["id"]: Expected JSON, received bigint'
  output: '{ type: "string" }'
operations:
  parse:
    expression: i=>{for(;;){if(typeof i==="object"&&i&&!Array.isArray(i)&&i["type"]==="user.created"){let v0=i["id"];typeof v0==="bigint"||e[0](v0);i="{\"type\":\"user.created\",\"id\":\""+v0+"\"}";break}if(typeof i==="object"&&i&&!Array.isArray(i)&&i["type"]==="user.renamed"){let v1=i["id"],v2=i["name"];typeof v1==="bigint"||e[1](v1);typeof v2==="string"||e[2](v2);i="{\"type\":\"user.renamed\",\"id\":\""+v1+"\",\"name\":"+e[3](v2)+"}";break}e[4](i)}return i}
    examples:
      # FIXME: the per-field typeof checks are redundant once the discriminant
      # selected the variant, and `decode` (which skips validation everywhere
      # else) compiles to the same code as `parse` because of them.
      renamed:
        input: '{ type: "user.renamed", id: 42n, name: "Dmitry" }'
        output: '"{\"type\":\"user.renamed\",\"id\":\"42\",\"name\":\"Dmitry\"}"'
      created:
        input: '{ type: "user.created", id: 42n }'
        output: '"{\"type\":\"user.created\",\"id\":\"42\"}"'
      unknown-variant:
        input: '{ type: "user.removed", id: 42n }'
        error: 'Expected { type: "user.created"; id: bigint; } | { type: "user.renamed"; id: bigint; name: string; }, received { type: "user.removed"; id: 42n; }'
  decode: eq-to-parse
  encode:
    expression: i=>{let v0;try{v0=JSON.parse(i)}catch(t){e[0](i)}if(typeof v0==="object"&&v0&&!Array.isArray(v0)){for(;;){if(v0["type"]==="user.created"){let v1=v0["id"];typeof v1==="bigint"||e[1](v1);v0={"type":v0["type"],"id":v1,};break}if(v0["type"]==="user.renamed"){let v2=v0["id"],v3=v0["name"];typeof v2==="bigint"||e[2](v2);typeof v3==="string"||e[3](v3);v0={"type":v0["type"],"id":v2,"name":v3,};break}e[4](v0)}}else{e[5](v0)}return v0}
    examples:
      # FIXME: the encode direction expects a bigint where JSON can only carry
      # the string the parse direction produced, so no input round-trips.
      does-not-round-trip:
        input: '"{\"type\":\"user.created\",\"id\":\"42\"}"'
        error: 'Failed at ["id"]: Expected bigint, received "42"'
