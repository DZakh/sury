# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.uint8Array.with(S.to, S.base64)
  input: Uint8Array<ArrayBufferLike>
  output: string
  instantiations: 5233
jsonSchema:
  input: Expected JSON, received Uint8Array
  output: '{ type: "string", contentEncoding: "base64" }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{i instanceof e[3]||e[4](i);let v0=e[0](i);e[1](v0)||e[2](v0);return v0}
    examples:
      non-utf8-bytes:
        input: new Uint8Array([137, 80, 78, 71])
        output: '"iVBORw=="'
      empty:
        input: new Uint8Array([])
        output: '""'
      one-byte:
        input: new Uint8Array([255])
        output: '"/w=="'
  decode:
    expression: i=>{let v0=e[0](i);e[1](v0)||e[2](v0);return v0}
    examples:
      non-utf8-bytes:
        input: new Uint8Array([137, 80, 78, 71])
        output: '"iVBORw=="'
  encode:
    expression: i=>{e[1](i)||e[2](i);return e[0](i)}
    examples:
      non-utf8-bytes:
        input: '"iVBORw=="'
        error: cannot represent a Uint8Array instance as spec source code
      invalid-base64:
        input: '"!!"'
        error: Expected base64, received "!!"
