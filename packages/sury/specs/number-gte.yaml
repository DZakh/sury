# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.number.with(S.gte, 5)
  input: number
  output: number
  instantiations: 5179
vs:
  zod: z.number().gte(5)
jsonSchema:
  input: '{ type: "number", minimum: 5 }'
  output: '{ type: "number", minimum: 5 }'
operations:
  parse:
    expression: i=>{typeof i==="number"&&!Number.isNaN(i)||e[2](i);i>=e[0]||e[1](i);return i}
    examples:
      just-above:
        input: "5.5"
        output: "5.5"
      above:
        input: "6"
        output: "6"
      at-the-bound:
        input: "5"
        output: "5"
      below:
        input: "4"
        error: Number must be greater than or equal to 5
      infinity:
        input: Infinity
        output: Infinity
      nan:
        input: NaN
        error: Expected number, received NaN
      wrong-type:
        input: '"6"'
        error: Expected number, received "6"
  decode:
    expression: i=>{i>=e[0]||e[1](i);return i}
    examples:
      just-above:
        input: "5.5"
        output: "5.5"
      at-the-bound:
        input: "5"
        output: "5"
      below:
        input: "4"
        error: Number must be greater than or equal to 5
  encode:
    expression: i=>{i>=e[0]||e[1](i);return i}
    examples:
      just-above:
        input: "5.5"
        output: "5.5"
      at-the-bound:
        input: "5"
        output: "5"
      below:
        input: "4"
        error: Number must be greater than or equal to 5
