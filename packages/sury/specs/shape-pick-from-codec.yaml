# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: 'S.schema({ f: S.schema({ b: S.string }).with(S.to, S.schema({ b: S.string }), (v) => ({ b: v.b + "!" }), (v) => ({ b: v.b.slice(0, -1) })) }).with(S.shape, (v) => ({ z: v.f.b }))'
  input: "{ f: { b: string; }; }"
  output: "{ z: string; }"
  instantiations: 9468
jsonSchema:
  input: '{ type: "object", properties: { f: { type: "object", properties: { b: { type: "string" } }, required: ["b"] } }, required: ["f"] }'
  output: '{ type: "object", properties: { z: { type: "string" } }, required: ["z"] }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{typeof i==="object"&&i&&!Array.isArray(i)||e[4](i);let v0=i["f"];typeof v0==="object"&&v0&&!Array.isArray(v0)||e[3](v0);let v1=v0["b"];typeof v1==="string"||e[0](v1);let v2;try{v2=e[1]({"b":v1,})}catch(x){e[2](x)}return {"z":v2["b"],}}
    examples:
      reads-through-field-transform:
        input: '{ f: { b: "x" } }'
        output: '{ z: "x!" }'
  decode:
    expression: i=>{let v0=i["f"];let v1;try{v1=e[0](v0)}catch(x){e[1](x)}return {"z":v1["b"],}}
    examples:
      reads-through-field-transform:
        input: '{ f: { b: "x" } }'
        output: '{ z: "x!" }'
  encode:
    expression: i=>{let v0;try{v0=e[0]({"b":i["z"],})}catch(x){e[1](x)}return {"f":v0,}}
    examples:
      round-trip:
        input: '{ z: "x!" }'
        output: '{ f: { b: "x" } }'
