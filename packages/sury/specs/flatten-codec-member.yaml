# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: 'S.object((s) => s.flatten(S.schema({ b: S.string }).with(S.to, S.schema({ b: S.string }), (v) => ({ b: v.b + "!" }), (v) => ({ b: v.b.slice(0, -1) }))))'
  input: "{ [x: string]: unknown; }"
  output: "{ b: string; }"
  instantiations: 6820
vs:
  zod:
    _skip: not-applicable
jsonSchema:
  input: '{ type: "object", properties: { b: { type: "string" } }, required: ["b"] }'
  output: '{ type: "object", properties: { b: { type: "string" } }, required: ["b"] }'
operations:
  parse:
    expression: i=>{typeof i==="object"&&i&&!Array.isArray(i)||e[3](i);let v0=i["b"];typeof v0==="string"||e[0](v0);let v1;try{v1=e[1]({"b":v0,})}catch(x){e[2](x)}return v1}
    examples:
      applies-member-transform:
        input: '{ b: "x" }'
        output: '{ b: "x!" }'
  decode:
    expression: i=>{let v0;try{v0=e[0](i)}catch(x){e[1](x)}return v0}
    examples:
      applies-member-transform:
        input: '{ b: "x" }'
        output: '{ b: "x!" }'
  encode:
    expression: i=>{let v0;try{v0=e[0](i)}catch(x){e[1](x)}return {"b":v0["b"],}}
    examples:
      round-trip:
        input: '{ b: "x!" }'
        output: '{ b: "x" }'
