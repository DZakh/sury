# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.optional(S.uint8Array.with(S.to, S.jsonString))
  input: Uint8Array<ArrayBufferLike> | undefined
  output: string | undefined
  instantiations: 4746
jsonSchema:
  input: Expected JSON, received Uint8Array | undefined
  output: Expected JSON, received JSON string | undefined
isEqual:
  input: (a,b)=>a===b||(a===void 0?b===void 0:b!==void 0&&e[1](a,b))
  output: strictEqual
compare:
  input: "throws: [Sury] Can't compare Uint8Array. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
  output: (a,b)=>a===b?0:(a===void 0?b===void 0?0:-1:b===void 0?1:a<b?-1:a>b?1:0)
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: 'SuryError: Ambiguous Uint8Array -> JSON string. Should the bytes be packed or unpacked? Choose with S.to and "pack" or "unpack"'
  decode: eq-to-parse
  encode:
    creationError: 'SuryError: Ambiguous JSON string -> Uint8Array. Should the bytes be packed or unpacked? Choose with S.to and "pack" or "unpack"'
