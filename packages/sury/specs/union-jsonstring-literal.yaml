# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.jsonString, S.literal("x")])
  input: string
  output: string
  instantiations: 1044
jsonSchema:
  input: '{ anyOf: [{ type: "string", contentMediaType: "application/json" }, { type: "string", const: "x" }] }'
  output: '{ anyOf: [{ type: "string", contentMediaType: "application/json" }, { type: "string", const: "x" }] }'
  openapi-3.0:
    input: '{ anyOf: [{ type: "string" }, { type: "string", enum: ["x"] }] }'
    output: '{ anyOf: [{ type: "string" }, { type: "string", enum: ["x"] }] }'
isEqual: strictEqual
compare: strictCompare
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{let v0;for(;;){l2:{if(typeof i==="string"){for(;;){l1:{try{JSON.parse(i)}catch(t){{(v0||(v0=[])).push(e[0],i,void 0);break l1}};break}if(i==="x")break;break l2};break}}throw e[1](i,v0)}return i}catch(v3){e[2](v3)}}
    examples:
      json-number:
        input: '"1"'
        output: '"1"'
      json-string:
        input: '"\"x\""'
        output: '"\"x\""'
      json-object:
        input: '"{\"a\":1}"'
        output: '"{\"a\":1}"'
      not-json-at-all:
        input: '"a"'
        error: |-
          Expected JSON string | "x", received "a"
          - Expected JSON string, received "a"
      unterminated-object:
        input: '"{"'
        error: |-
          Expected JSON string | "x", received "{"
          - Expected JSON string, received "{"
      empty:
        input: '""'
        error: |-
          Expected JSON string | "x", received ""
          - Expected JSON string, received ""
      the-literal:
        input: '"x"'
        output: '"x"'
  decode: identity
  encode: identity
