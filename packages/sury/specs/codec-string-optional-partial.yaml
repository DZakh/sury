# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.string.with(S.to, S.optional(S.string))
  input: string
  output: string | undefined
  instantiations: 5413
jsonSchema:
  input: '{ type: "string" }'
  output: Expected JSON, received undefined | string
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Invalid operation: can't convert string to undefined | string — string has the same type as the source and the others don't. Use S.to to say what you mean, or S.never to mark a variant unreachable"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Invalid operation: can't convert undefined | string to string — string has the same type as the target and the others don't. Use S.to to say what you mean, or S.never to mark a variant unreachable"
