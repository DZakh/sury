# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.string.with(S.to, S.optional(S.string))
  input: string
  output: string | undefined
  instantiations: 5243
vs:
  zod:
    _skip: not-applicable
jsonSchema:
  input: '{ type: "string" }'
  output: Expected JSON, received string | undefined
operations:
  parse:
    creationError: "SuryError: Can't decode string to string | undefined: string matches it, undefined doesn't. Use S.to on it, or S.never to make it unreachable"
  decode: eq-to-parse
  encode: eq-to-parse
