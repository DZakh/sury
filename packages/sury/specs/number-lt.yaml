# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.number.with(S.lt, 5)
  input: number
  output: number
  instantiations: 5146
jsonSchema:
  input: '{ type: "number", exclusiveMaximum: 5 }'
  output: '{ type: "number", exclusiveMaximum: 5 }'
vs:
  zod: z.number().lt(5)
operations:
  parse:
    expression: i=>{typeof i==="number"&&!Number.isNaN(i)||e[1](i);i<5||e[0](i);return i}
    examples:
      just-above:
        input: "5.5"
        error: Expected number < 5, received 5.5
      above:
        input: "6"
        error: Expected number < 5, received 6
      at-the-bound:
        input: "5"
        error: Expected number < 5, received 5
      below:
        input: "4"
        output: "4"
      infinity:
        input: Infinity
        error: Expected number < 5, received Infinity
      nan:
        input: NaN
        error: Expected number < 5, received NaN
      wrong-type:
        input: '"6"'
        error: Expected number < 5, received "6"
  decode:
    expression: i=>{i<5||e[0](i);return i}
    examples:
      just-above:
        input: "5.5"
        error: Expected number < 5, received 5.5
      at-the-bound:
        input: "5"
        error: Expected number < 5, received 5
      below:
        input: "4"
        output: "4"
  encode:
    expression: i=>{i<5||e[0](i);return i}
    examples:
      just-above:
        input: "5.5"
        error: Expected number < 5, received 5.5
      at-the-bound:
        input: "5"
        error: Expected number < 5, received 5
      below:
        input: "4"
        output: "4"
