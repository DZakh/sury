# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: "S.record(S.env).with(S.to, S.schema({ user: S.schema({ name: S.string }) }))"
  input: "{ [x: string]: string | undefined; }"
  output: "{ user: { name: string; }; }"
  instantiations: 6531
jsonSchema:
  input: '{ type: "object", additionalProperties: { type: "string" } }'
  fromInputType: "{ [key: string]: string; }"
  output: '{ type: "object", properties: { user: { type: "object", properties: { name: { type: "string" } }, required: ["name"] } }, required: ["user"] }'
isEqual:
  input: (a,b)=>{if(a===b)return true;let n=0;for(const k in a){if(!(k in b)||!(a[k]===b[k]))return false;n++}for(const k in b)n--;return !n}
  output: (a,b)=>a===b||a.user.name===b.user.name
compare:
  input: (a,b)=>{if(a===b)return 0;let ka=Object.keys(a).sort(),kb=Object.keys(b).sort(),n=ka.length,m=kb.length,l=n<m?n:m,c;for(let i=0;i<l;i++){if(ka[i]!==kb[i])return ka[i]<kb[i]?-1:1;if(c=a[ka[i]]===void 0?b[kb[i]]===void 0?0:-1:b[kb[i]]===void 0?1:a[ka[i]]<b[kb[i]]?-1:a[ka[i]]>b[kb[i]]?1:0)return c}return n<m?-1:n>m?1:0}
  output: (a,b)=>a===b?0:a.user.name<b.user.name?-1:a.user.name>b.user.name?1:0
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Failed at user: Can't decode env -> { name: string; }. Define custom codec with S.to"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Failed at user: Can't decode { name: string; } -> env. Define custom codec with S.to"
