# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: 'S.schema({ foo: "bar" })'
  aliases:
    - 'S.schema({ foo: "bar" as const })'
    - 'S.schema({ foo: S.schema("bar") })'
    - 'S.literal({ foo: "bar" })'
  input: '{ foo: "bar"; }'
  output: '{ foo: "bar"; }'
  instantiations: 1043
jsonSchema:
  input: '{ type: "object", properties: { foo: { type: "string", const: "bar" } }, required: ["foo"] }'
  output: '{ type: "object", properties: { foo: { type: "string", const: "bar" } }, required: ["foo"] }'
  openapi-3.0:
    input: '{ type: "object", properties: { foo: { type: "string", enum: ["bar"] } }, required: ["foo"] }'
    output: '{ type: "object", properties: { foo: { type: "string", enum: ["bar"] } }, required: ["foo"] }'
isEqual: alwaysEqual
compare: "throws: [Sury] Can't compare { foo: \"bar\"; }. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
vs:
  zod: 'z.object({ foo: z.literal("bar") })'
operations:
  parse:
    expression: i=>{try{typeof i==="object"&&i&&!Array.isArray(i)||e[1](i);let v0=i.foo;v0==="bar"||e[0](v0);return {foo:v0}}catch(v1){e[2](v1)}}
    examples:
      valid:
        input: '{ foo: "bar" }'
        output: '{ foo: "bar" }'
      invalid-value:
        input: '{ foo: "baz" }'
        error: 'Failed at foo: Expected "bar", received "baz"'
      invalid-type:
        input: "null"
        error: 'Expected { foo: "bar"; }, received null'
  decode: identity
  encode: identity
