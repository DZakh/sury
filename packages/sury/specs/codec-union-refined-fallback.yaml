# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.string.with(S.minLength, 3), S.number, S.string])
  input: string | number
  output: string | number
  instantiations: 1758
jsonSchema:
  input: '{ anyOf: [{ type: "string", minLength: 3 }, { type: "number" }, { type: "string" }] }'
  output: '{ anyOf: [{ type: "string", minLength: 3 }, { type: "number" }, { type: "string" }] }'
isEqual: strictEqual
compare: "throws: [Sury] Can't compare string.length >= 3 | number | string. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
vs:
  zod: z.union([z.string().min(3), z.number(), z.string()])
operations:
  parse:
    expression: i=>{try{let v0;for(;;){if(typeof i==="string"){for(;;){l1:{if(!(i.length>2)){(v0||(v0=[])).push(e[0],i,void 0);break l1};break}break;};break}if(typeof i==="number"&&i==i)break;throw e[1](i,v0)}return i}catch(v2){e[2](v2)}}
    examples:
      long-string-matches-the-refined-member:
        input: '"abcd"'
        output: '"abcd"'
      short-string-falls-through-to-the-catch-all:
        input: '"ab"'
        output: '"ab"'
      empty-string-falls-through:
        input: '""'
        output: '""'
      number:
        input: "42"
        output: "42"
      grouped-past-the-number-member:
        input: "-0"
        output: "-0"
      invalid:
        input: "true"
        error: Expected string.length >= 3 | number | string, received true
  decode: identity
  encode: identity
