# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: "S.fromJSONSchemaOrThrow({ contains: { const: 1 } })"
  input: JSON
  output: JSON
  instantiations: 593
jsonSchema:
  input: "{ contains: { const: 1 } }"
  output: "{ contains: { const: 1 } }"
isEqual: (a,b)=>a===b||e[0](a,b)
compare: "throws: [Sury] Can't compare JSON. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{e[0](i);e[1](i)||e[2](i);return i}catch(v0){e[3](v0)}}
    examples:
      contains-one:
        input: "[0, 1, 2]"
        output: "[0, 1, 2]"
      missing:
        input: "[0, 2]"
        error: Should satisfy the contains keyword.
      empty-array:
        input: "[]"
        error: Should satisfy the contains keyword.
      ignores-object:
        input: "{}"
        output: "{}"
  decode:
    expression: i=>{try{e[0](i)||e[1](i);return i}catch(v0){e[2](v0)}}
    examples:
      contains-one:
        input: "[0, 1, 2]"
        output: "[0, 1, 2]"
  encode:
    expression: i=>{try{e[0](i)||e[1](i);return i}catch(v0){e[2](v0)}}
    examples:
      contains-one:
        input: "[0, 1, 2]"
        output: "[0, 1, 2]"
