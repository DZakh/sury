# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: 'S.fromJSONSchema({ type: "object", properties: { id: { type: "string" }, role: { enum: ["admin", "user"] }, tags: { type: "array", items: { type: "string" } } }, required: ["id", "role"] })'
  input: '{ id: string; role: "admin" | "user"; tags?: string[] | undefined; }'
  output: '{ id: string; role: "admin" | "user"; tags?: string[] | undefined; }'
  instantiations: 2373
vs:
  zod:
    _skip: not-applicable
jsonSchema:
  input: '{ type: "object", properties: { id: { type: "string" }, role: { enum: ["admin", "user"] }, tags: { type: "array", items: { type: "string" } } }, required: ["id", "role"] }'
  output: '{ type: "object", properties: { id: { type: "string" }, role: { enum: ["admin", "user"] }, tags: { type: "array", items: { type: "string" } } }, required: ["id", "role"] }'
operations:
  parse:
    expression: i=>{typeof i==="object"&&i&&!Array.isArray(i)||e[3](i);e[1](i)||e[4](i);e[2](i)||e[5](i);for(let v0 in i){try{e[0](i[v0]);}catch(v1){v1.path='["'+v0+'"]'+v1.path;throw v1}}return i}
    examples:
      valid:
        input: '{ id: "1", role: "admin", tags: ["a"] }'
        output: '{ id: "1", role: "admin", tags: ["a"] }'
      optional-key-absent:
        input: '{ id: "1", role: "user" }'
        output: '{ id: "1", role: "user" }'
      undeclared-key-preserved:
        input: '{ id: "1", role: "admin", extra: true }'
        output: '{ id: "1", role: "admin", extra: true }'
      enum-mismatch:
        input: '{ id: "1", role: "root" }'
        error: Should pass every declared property schema.
      missing-required:
        input: '{ role: "admin" }'
        error: Should contain every required property.
  decode:
    expression: i=>{e[0](i)||e[2](i);e[1](i)||e[3](i);return i}
    examples:
      valid:
        input: '{ id: "1", role: "admin", tags: ["a"] }'
        output: '{ id: "1", role: "admin", tags: ["a"] }'
      optional-key-absent:
        input: '{ id: "1", role: "user" }'
        output: '{ id: "1", role: "user" }'
      undeclared-key-preserved:
        input: '{ id: "1", role: "admin", extra: true }'
        output: '{ id: "1", role: "admin", extra: true }'
      enum-mismatch:
        input: '{ id: "1", role: "root" }'
        error: Should pass every declared property schema.
      missing-required:
        input: '{ role: "admin" }'
        error: Should contain every required property.
  encode:
    expression: i=>{e[0](i)||e[2](i);e[1](i)||e[3](i);return i}
    examples:
      valid:
        input: '{ id: "1", role: "admin", tags: ["a"] }'
        output: '{ id: "1", role: "admin", tags: ["a"] }'
      optional-key-absent:
        input: '{ id: "1", role: "user" }'
        output: '{ id: "1", role: "user" }'
      undeclared-key-preserved:
        input: '{ id: "1", role: "admin", extra: true }'
        output: '{ id: "1", role: "admin", extra: true }'
      enum-mismatch:
        input: '{ id: "1", role: "root" }'
        error: Should pass every declared property schema.
      missing-required:
        input: '{ role: "admin" }'
        error: Should contain every required property.
