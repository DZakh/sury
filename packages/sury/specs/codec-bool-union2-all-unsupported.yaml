# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.boolean.with(S.to, S.union([S.number, S.symbol]))
  input: boolean
  output: number | symbol
  instantiations: 5394
jsonSchema:
  input: '{ type: "boolean" }'
  output: Expected JSON, received number | symbol
isEqual: strictEqual
compare:
  input: strictCompare
  output: (a,b)=>a===b?0:(typeof a==="number"&&a==a?typeof b==="number"&&b==b?a<b?-1:a>b?1:0:-1:typeof b==="number"&&b==b?1:a===b?0:1)
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Can't decode boolean -> number. Define custom codec with S.to"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Can't decode number -> boolean. Define custom codec with S.to"
