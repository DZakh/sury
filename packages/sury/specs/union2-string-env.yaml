# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.string.with(S.minLength, 3), S.env])
  input: string | undefined
  output: string | undefined
  instantiations: 1612
jsonSchema:
  input: '{ anyOf: [{ type: "string", minLength: 3 }, { type: "string" }] }'
  fromInputType: string
  output: '{ anyOf: [{ type: "string", minLength: 3 }, { type: "string" }] }'
  fromOutputType: string
isEqual: strictEqual
compare: "throws: [Sury] Can't compare string.length >= 3 | env. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{let v0;for(;;){if(typeof i==="string"){l1:{if(!(i.length>2)){(v0||(v0=[])).push(e[0],i,void 0);break l1};break}}if((typeof i==="string"||i===void 0))break;throw e[1](i,void 0,v0)}return i}catch(v2){e[2](v2)}}
    examples:
      long:
        input: '"abc"'
        output: '"abc"'
      short:
        input: '"ab"'
        output: '"ab"'
      unset:
        input: undefined
        output: undefined
      number:
        input: "1"
        error: Expected string.length >= 3 | env, received 1
  decode: identity
  encode: identity
