# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.json.with(S.to, S.union(["123", S.bigint, S.string]))
  input: JSON
  output: string | bigint
  instantiations: 5508
jsonSchema:
  input: '{ anyOf: [{ type: "string", const: "123" }, { type: "string" }] }'
  fromInputType: string
  output: Expected JSON, received "123" | bigint | string
  openapi-3.0:
    input: '{ anyOf: [{ type: "string", enum: ["123"] }, { type: "string" }] }'
isEqual:
  input: (a,b)=>a===b||e[0](a,b)
  output: strictEqual
compare:
  input: "throws: [Sury] Can't compare JSON. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
  output: "throws: [Sury] Can't compare \"123\" | bigint | string. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{for(;;){let r;if(typeof i==="string"&&i==="123")break;if(typeof i==="string"){try{let v0;try{v0=BigInt(i)}catch(_){e[0](i)}v0||i.trim()||e[0](i);i=v0;break}catch(x){(r||(r=[])).push(e[1](x))}break}e[2](i,...(r||[]))}return i}catch(v1){e[3](v1)}}
    examples:
      literal-member:
        input: '"123"'
        output: '"123"'
      bigint-string:
        input: '"124"'
        output: 124n
      catchall-string:
        input: '"abc"'
        output: '"abc"'
      empty-string:
        input: '""'
        output: '""'
      whitespace-string:
        input: '"   "'
        output: '"   "'
      no-double-decoding:
        input: "true"
        error: Expected "123" | bigint | string, received true
      number:
        input: "123"
        error: Expected "123" | bigint | string, received 123
  decode: eq-to-parse
  encode:
    expression: i=>{try{for(;;){if(typeof i==="string"&&i==="123")break;if(typeof i==="string")break;if(typeof i==="bigint"){i=""+i;break}e[0](i)}return i}catch(v0){e[1](v0)}}
    examples:
      bigint:
        input: 123n
        output: '"123"'
      literal-member:
        input: '"123"'
        output: '"123"'
      string:
        input: '"abc"'
        output: '"abc"'
