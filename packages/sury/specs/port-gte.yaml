# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.port.with(S.gte, 80)
  input: number
  output: number
  instantiations: 578
jsonSchema:
  input: '{ type: "integer", minimum: 80, maximum: 65535 }'
  output: '{ type: "integer", minimum: 80, maximum: 65535 }'
isEqual: strictEqual
compare: strictCompare
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{typeof i==="number"&&i==i||e[2](i);i>=0&&i<65536&&i%1==0||e[0](i);i>=80||e[1](i);return i}catch(v0){e[3](v0)}}
    examples:
      above:
        input: "81"
        output: "81"
      at-the-bound:
        input: "80"
        output: "80"
      below:
        input: "79"
        error: Expected port >= 80, received 79
      out-of-port-range:
        input: "0"
        error: Expected port >= 80, received 0
      wrong-type:
        input: '"81"'
        error: Expected port >= 80, received "81"
  decode:
    expression: i=>{try{i>=0&&i<65536&&i%1==0||e[0](i);i>=80||e[1](i);return i}catch(v0){e[2](v0)}}
    examples:
      above:
        input: "81"
        output: "81"
      at-the-bound:
        input: "80"
        output: "80"
      below:
        input: "79"
        error: Expected port >= 80, received 79
  encode:
    expression: i=>{try{i>=0&&i<65536&&i%1==0||e[0](i);i>=80||e[1](i);return i}catch(v0){e[2](v0)}}
    examples:
      above:
        input: "81"
        output: "81"
      at-the-bound:
        input: "80"
        output: "80"
      below:
        input: "79"
        error: Expected port >= 80, received 79
