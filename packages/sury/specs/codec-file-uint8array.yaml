# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.file.with(S.to, S.uint8Array)
  input: File
  output: Uint8Array<ArrayBufferLike>
  instantiations: 5245
jsonSchema:
  input: Expected JSON, received File
  output: Expected JSON, received Uint8Array
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    isAsync: true
    expression: i=>{i instanceof e[0]||e[1](i);let v0=i.arrayBuffer().then(b=>new Uint8Array(b));return v0}
    examples:
      reads-the-bytes:
        input: new File([new Uint8Array([137, 80, 78, 71])], "a.png")
        error: cannot represent a Uint8Array instance as spec source code
  decode:
    isAsync: true
    expression: i=>{let v0=i.arrayBuffer().then(b=>new Uint8Array(b));return v0}
    examples:
      reads-the-bytes:
        input: new File([new Uint8Array([137, 80, 78, 71])], "a.png")
        error: cannot represent a Uint8Array instance as spec source code
  encode:
    expression: i=>{return new e[0]([i],"")}
    examples:
      writes-an-unnamed-file:
        input: new Uint8Array([137, 80, 78, 71])
        error: cannot represent a File instance as spec source code
