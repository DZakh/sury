# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.email.with(S.to, S.uint8Array)
  input: string
  output: Uint8Array<ArrayBufferLike>
  instantiations: 4419
jsonSchema:
  input: '{ type: "string" }'
  output: Expected JSON, received Uint8Array
isEqual:
  input: strictEqual
  output: (a,b)=>{if(a===b)return true;let n=a.length;if(n!==b.length)return false;for(let i=0;i<n;i++)if(!(a[i]===b[i]))return false;return true}
compare:
  input: strictCompare
  output: "throws: [Sury] Can't compare Uint8Array. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{typeof i==="string"||e[3](i);e[1].test(i)||e[2](i);return e[0](i)}catch(v0){e[4](v0)}}
    examples:
      valid:
        input: '"a@b.co"'
        output: new Uint8Array([97, 64, 98, 46, 99, 111])
      invalid:
        input: '"nope"'
        error: Expected email, received "nope"
  decode:
    expression: i=>{try{e[1].test(i)||e[2](i);return e[0](i)}catch(v0){e[3](v0)}}
    examples:
      valid:
        input: '"a@b.co"'
        output: new Uint8Array([97, 64, 98, 46, 99, 111])
  encode:
    expression: i=>{try{let v0=e[0].decode(i);e[1].test(v0)||e[2](v0);return v0}catch(v1){e[3](v1)}}
    examples:
      valid:
        input: new Uint8Array([97, 64, 98, 46, 99, 111])
        output: '"a@b.co"'
      invalid:
        input: new Uint8Array([110, 111, 112, 101])
        error: Expected email, received "nope"
