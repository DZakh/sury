# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.file.with(S.to, S.string)
  input: File
  output: string
  instantiations: 5245
jsonSchema:
  input: Expected JSON, received File
  output: '{ type: "string", contentMediaType: "application/octet-stream" }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    isAsync: true
    expression: i=>{i instanceof e[0]||e[1](i);let v0=i.text();return v0}
    examples:
      reads-the-text:
        input: new File(["\u20ac"], "a.txt")
        output: '"€"'
  decode:
    isAsync: true
    expression: i=>{let v0=i.text();return v0}
    examples:
      reads-the-text:
        input: new File(["\u20ac"], "a.txt")
        output: '"€"'
  encode:
    expression: i=>{return new e[0]([i],"")}
    examples:
      writes-an-unnamed-file:
        input: '"€"'
        error: cannot represent a File instance as spec source code
