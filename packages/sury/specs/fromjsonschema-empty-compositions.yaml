# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: "S.fromJSONSchema({ anyOf: [], oneOf: [], allOf: [] })"
  input: JSON
  output: JSON
  instantiations: 747
vs:
  zod:
    _skip: not-applicable
jsonSchema:
  input: "{}"
  output: "{}"
operations:
  parse:
    expression: i=>{e[0](i);return i}
    examples:
      string:
        input: '"anything"'
        output: '"anything"'
      object:
        input: "{ value: true }"
        output: "{ value: true }"
      non-json:
        input: undefined
        error: Expected JSON, received undefined
  decode: identity
  encode: identity
