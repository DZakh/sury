# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.nullable(S.string).with(S.to, S.string)
  input: string | null
  output: string
  instantiations: 4529
jsonSchema:
  input: '{ type: "string" }'
  fromInputType: string
  output: '{ type: "string" }'
isEqual: strictEqual
compare:
  input: (a,b)=>a===b?0:(a===null?b===null?0:-1:b===null?1:a<b?-1:a>b?1:0)
  output: strictCompare
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{typeof i==="string"||e[0](i);return i}catch(v0){e[1](v0)}}
    examples:
      string:
        input: '"abc"'
        output: '"abc"'
      null-is-rejected:
        input: "null"
        error: Expected string, received null
  decode: eq-to-parse
  encode: identity
