# yaml-language-server: $schema=./spec.schema.json
ts:
  # FIXME: Codec next expects: stay the same — rule 4 matches "x" by type and bridges
  # undefined <-> null.
  schema: S.optional(S.literal("x")).with(S.to, S.nullable(S.literal("x")))
  input: '"x" | undefined'
  output: '"x" | undefined'
  instantiations: 5690
vs:
  zod:
    _skip: not-applicable
jsonSchema:
  # FIXME: S.toJSONSchema recurses forever on this conversion — neither side is
  # unrepresentable ('"x" | undefined' and '"x" | null' both are).
  input: Maximum call stack size exceeded
  output: Maximum call stack size exceeded
operations:
  parse:
    expression: i=>{if(i===void 0){try{i=void 0}catch(e1){e[0](i,e1)}}else if(!(typeof i==="string"&&(i==="x"))){e[1](i)}return i}
    examples:
      literal:
        input: '"x"'
        output: '"x"'
      undefined-bridges-to-null:
        input: undefined
        output: undefined
      invalid:
        input: '"y"'
        error: Expected "x" | undefined, received "y"
  decode: eq-to-parse
  encode:
    expression: i=>{if(i===void 0){let v0=null;try{v0=void 0}catch(e1){e[0](v0,e1)}i=v0}else if(!(typeof i==="string"&&(i==="x"))){e[1](i)}return i}
    examples:
      literal:
        input: '"x"'
        output: '"x"'
      "null":
        input: "null"
        error: Expected "x" | undefined, received null
