# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: "S.file.with(S.to, S.jsonString.with(S.to, S.schema({ id: S.string })))"
  input: File
  output: "{ id: string; }"
  instantiations: 6672
jsonSchema:
  input: Expected JSON, received File
  output: '{ type: "object", properties: { id: { type: "string" } }, required: ["id"] }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    isAsync: true
    expression: i=>{i instanceof e[3]||e[4](i);let v0=i.text();return v0.then(v0=>{let v1;try{v1=JSON.parse(v0)}catch(t){e[0](v0)}typeof v1==="object"&&v1&&!Array.isArray(v1)||e[2](v1);let v2=v1["id"];typeof v2==="string"||e[1](v2);return {"id":v2,}})}
    examples:
      reads-and-parses:
        input: new File(['{"id":"a"}'], "c.json")
        output: '{ id: "a" }'
      not-json:
        input: new File(["nope"], "c.json")
        error: Expected JSON string, received "nope"
  decode:
    isAsync: true
    expression: i=>{let v0=i.text();return v0.then(v0=>{let v1;try{v1=JSON.parse(v0)}catch(t){e[0](v0)}typeof v1==="object"&&v1&&!Array.isArray(v1)||e[2](v1);let v2=v1["id"];typeof v2==="string"||e[1](v2);return {"id":v2,}})}
    examples:
      reads-and-parses:
        input: new File(['{"id":"a"}'], "c.json")
        output: '{ id: "a" }'
  encode:
    expression: i=>{return new e[1](["{\"id\":"+e[0](i["id"])+"}"],"")}
    examples:
      writes-an-unnamed-file:
        input: '{ id: "a" }'
        error: cannot represent a File instance as spec source code
