# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.uint8Array.with(S.to, S.string.with(S.noValidation, true))
  input: Uint8Array<ArrayBufferLike>
  output: string
  instantiations: 4916
jsonSchema:
  input: Expected JSON, received Uint8Array
  output: '{ type: "string" }'
isEqual:
  input: (a,b)=>{if(a===b)return true;let n=a.length;if(n!==b.length)return false;for(let i=0;i<n;i++)if(!(a[i]===b[i]))return false;return true}
  output: strictEqual
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{i instanceof e[1]||e[2](i);let v0=e[0].decode(i);return v0}catch(v1){e[3](v1)}}
    examples:
      utf8-bytes:
        input: new Uint8Array([226, 130, 172])
        output: '"€"'
      lossy-bytes:
        input: new Uint8Array([137, 80, 78, 71])
        output: '"�PNG"'
  decode:
    expression: i=>{try{let v0=e[0].decode(i);return v0}catch(v1){e[1](v1)}}
    examples:
      utf8-bytes:
        input: new Uint8Array([226, 130, 172])
        output: '"€"'
  encode:
    expression: i=>{try{return e[0](i)}catch(v0){e[1](v0)}}
    examples:
      text:
        input: '"€"'
        output: new Uint8Array([226, 130, 172])
