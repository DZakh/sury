# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.file.with(S.to, S.jsonString)
  input: File
  output: string
  instantiations: 5245
jsonSchema:
  input: Expected JSON, received File
  output: '{ type: "string", contentMediaType: "application/json" }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: 'SuryError: Ambiguous conversion from File to JSON string. Use S.to(from, to, {decode: "unpack" | "pack", encode: ...})'
  decode: eq-to-parse
  encode: eq-to-parse
