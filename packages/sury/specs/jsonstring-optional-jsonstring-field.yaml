# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: "S.jsonString.with(S.to, S.schema({ f: S.optional(S.jsonString) }))"
  input: string
  output: "{ f?: string | undefined; }"
  instantiations: 6201
jsonSchema:
  input: '{ type: "string", contentMediaType: "application/json" }'
  output: '{ type: "object", properties: { f: { type: "string", contentMediaType: "application/json" } } }'
  draft-2020-12:
    input: '{ type: "string", contentMediaType: "application/json", contentSchema: { type: "object", properties: { f: { type: "string", contentMediaType: "application/json" } } }, $schema: "https://json-schema.org/draft/2020-12/schema" }'
  openapi-3.0:
    input: '{ type: "string" }'
    output: '{ type: "object", properties: { f: { type: "string" } } }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{typeof i==="string"||e[4](i);let v0;try{v0=JSON.parse(i)}catch(t){e[0](i)}typeof v0==="object"&&v0&&!Array.isArray(v0)||e[3](v0);let v1=(v0["f"]??null);for(;;){if(typeof v1==="string"){try{JSON.parse(v1)}catch(t){e[1](v1)};break}if(v1===null){v1=void 0;break}e[2](v1)}return {f:v1}}
    examples:
      # FIXME: encode splices the field's document in raw (`"f":{}` for a field
      # holding the text `{}`) rather than as one of the outer document's
      # strings, so this parse and that encode do not round-trip. Same boundary
      # as `codec-jsonstring-object-optional-jsonstring`: the encode side reads
      # the arm through `storedApart` (json.ts), which leaves a document-carrying
      # arm alone. The required field (`jsonstring-object-field`) escapes it.
      nested-document:
        input: '"{\"f\":\"{}\"}"'
        output: '{ f: "{}" }'
      absent:
        input: '"{}"'
        output: "{ f: undefined }"
      null-is-absent:
        input: '"{\"f\":null}"'
        output: "{ f: undefined }"
      not-a-string:
        input: '"{\"f\":1}"'
        error: "Failed at f: Expected JSON string | undefined, received 1"
  decode:
    expression: i=>{let v0;try{v0=JSON.parse(i)}catch(t){e[0](i)}typeof v0==="object"&&v0&&!Array.isArray(v0)||e[3](v0);let v1=(v0["f"]??null);for(;;){if(typeof v1==="string"){try{JSON.parse(v1)}catch(t){e[1](v1)};break}if(v1===null){v1=void 0;break}e[2](v1)}return {f:v1}}
    examples:
      nested-document:
        input: '"{\"f\":\"{}\"}"'
        output: '{ f: "{}" }'
  encode:
    expression: i=>{let v0=i["f"];let v1="";if(v0!==void 0){v1+="\"f\":"+v0}return "{"+v1+"}"}
    examples:
      nested-document:
        input: '{ f: "{}" }'
        output: '"{\"f\":{}}"'
      absent:
        input: "{}"
        output: '"{}"'
