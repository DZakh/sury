# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.number.with(S.to, S.jsonString).with(S.pattern, /^\d+$/)
  input: number
  output: string
  instantiations: 5548
jsonSchema:
  input: '{ type: "number" }'
  output: '{ type: "string", contentMediaType: "application/json", pattern: "^\\d+$" }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{typeof i==="number"&&!Number.isNaN(i)||e[3](i);let v0=(Number.isFinite(i)?""+i:e[0](i));e[1].test(v0)||e[2](v0);return v0}
    examples:
      valid:
        input: "123"
        output: '"123"'
      negative-fails-pattern:
        input: "-5"
        error: Invalid pattern
  decode:
    expression: i=>{let v0=(Number.isFinite(i)?""+i:e[0](i));e[1].test(v0)||e[2](v0);return v0}
    examples:
      valid:
        input: "123"
        output: '"123"'
      negative-fails-pattern:
        input: "-5"
        error: Invalid pattern
  encode:
    expression: i=>{e[0].test(i)||e[3](i);let v0;try{v0=JSON.parse(i)}catch(t){e[1](i)}typeof v0==="number"&&!Number.isNaN(v0)||e[2](v0);return v0}
    examples:
      valid:
        input: '"123"'
        output: "123"
      fails-pattern:
        input: '"abc"'
        error: Invalid pattern
