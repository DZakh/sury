# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.map(S.string, S.string.with(S.asyncDecoderAssert, async (value) => { if (value === "bad") throw new Error("rejected"); }))
  input: Map<string, string>
  output: Map<string, string>
  instantiations: 5746
jsonSchema:
  input: Expected JSON, received Map<string, string>
  output: Expected JSON, received Map<string, unknown>
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    isAsync: true
    expression: i=>{i instanceof e[4]||e[5](i);let v3=[];for(let v0 of i){try{let v4=v0[0];typeof v4==="string"||e[3](v4);let v2=v0[1];typeof v2==="string"||e[2](v2);let v1;try{v1=e[0](v2).catch(x=>e[1](x))}catch(x){e[1](x)}v3.push(Promise.all([v4,v1.catch(v5=>{v5.path='["'+v0[0]+'"]'+v5.path;throw v5})]));}catch(v5){v5.path='["'+v0[0]+'"]'+v5.path;throw v5}}return Promise.all(v3).then(v6=>new Map(v6))}
    examples:
      valid:
        input: new Map([["k", "a"]])
        output: new Map([["k", "a"]])
      rejected-value-is-located-by-its-key:
        input: new Map([["k", "bad"]])
        error: 'Failed at ["k"]: rejected'
  decode:
    isAsync: true
    expression: i=>{let v2=[];for(let v0 of i){try{let v1;try{v1=e[0](v0[1]).catch(x=>e[1](x))}catch(x){e[1](x)}v2.push(Promise.all([v0[0],v1.catch(v3=>{v3.path='["'+v0[0]+'"]'+v3.path;throw v3})]));}catch(v3){v3.path='["'+v0[0]+'"]'+v3.path;throw v3}}return Promise.all(v2).then(v4=>new Map(v4))}
    examples:
      valid:
        input: new Map([["k", "a"]])
        output: new Map([["k", "a"]])
  encode:
    expression: i=>{let v2=new Map;for(let v0 of i){try{let v1;try{v1=e[0](v0[1])}catch(x){e[1](x)}typeof v1==="string"||e[2](v1);v2.set(v0[0],v1);}catch(v3){v3.path='["'+v0[0]+'"]'+v3.path;throw v3}}return v2}
    examples:
      valid:
        input: new Map([["k", "a"]])
        output: new Map([["k", "a"]])
