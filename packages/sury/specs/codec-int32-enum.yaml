# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.int32.with(S.to, S.union([0, 1]))
  input: number
  output: 0 | 1
  instantiations: 5040
jsonSchema:
  input: '{ type: "integer", minimum: -2147483648, maximum: 2147483647 }'
  output: Cannot read properties of undefined (reading 'v')
vs:
  zod:
    _skip: todo(#…)
# FIXME: a number schema converting to a union of number literals crashes
# the union compiler at creation instead of narrowing to the listed values.
operations:
  parse:
    creationError: "TypeError: Cannot read properties of undefined (reading 'v')"
  decode: eq-to-parse
  encode:
    expression: i=>{typeof i==="number"&&i===i&&(i===0&&i<=2147483647&&i>=-2147483648&&i%1===0||i===1&&i<=2147483647&&i>=-2147483648&&i%1===0)||e[0](i);return i}
    examples:
      listed:
        input: "1"
        output: "1"
      unlisted:
        input: "2"
        error: Expected 0 | 1, received 2
