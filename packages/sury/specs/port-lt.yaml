# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.port.with(S.lt, 80)
  input: number
  output: number
  instantiations: 5165
vs:
  zod:
    _skip: not-applicable
jsonSchema:
  input: '{ type: "integer", minimum: 0, exclusiveMaximum: 80 }'
  output: '{ type: "integer", minimum: 0, exclusiveMaximum: 80 }'
operations:
  parse:
    expression: i=>{try{typeof i==="number"&&!Number.isNaN(i)||e[2](i);i>=0&&i<65536&&i%1===0||e[0](i);i<80||e[1](i);return i}catch(x){throw e[3](x,i)}}
    examples:
      above:
        input: "81"
        error: Expected port < 80, received 81
      at-the-bound:
        input: "80"
        error: Expected port < 80, received 80
      below:
        input: "79"
        output: "79"
      out-of-port-range:
        input: "0"
        output: "0"
      wrong-type:
        input: '"81"'
        error: Expected port < 80, received "81"
  decode:
    expression: i=>{try{i>=0&&i<65536&&i%1===0||e[0](i);i<80||e[1](i);return i}catch(x){throw e[2](x,i)}}
    examples:
      above:
        input: "81"
        error: Expected port < 80, received 81
      at-the-bound:
        input: "80"
        error: Expected port < 80, received 80
      below:
        input: "79"
        output: "79"
  encode:
    expression: i=>{try{i>=0&&i<65536&&i%1===0||e[0](i);i<80||e[1](i);return i}catch(x){throw e[2](x,i)}}
    examples:
      above:
        input: "81"
        error: Expected port < 80, received 81
      at-the-bound:
        input: "80"
        error: Expected port < 80, received 80
      below:
        input: "79"
        output: "79"
