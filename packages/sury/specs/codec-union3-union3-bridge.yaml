# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.bigint, S.number, null]).with(S.to, S.union([S.bigint, S.number, undefined]))
  input: number | bigint | null
  output: number | bigint | undefined
  instantiations: 5666
jsonSchema:
  input: Expected JSON, received bigint | number | null
  output: Expected JSON, received bigint | number | undefined
isEqual: strictEqual
compare: (a,b)=>a===b?0:(typeof a==="bigint"?typeof b==="bigint"?a<b?-1:a>b?1:0:-1:typeof b==="bigint"?1:typeof a==="number"&&a==a?typeof b==="number"&&b==b?a<b?-1:a>b?1:0:-1:typeof b==="number"&&b==b?1:0)
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{for(;;){if(typeof i==="bigint")break;if(typeof i==="number"&&i==i)break;if(i===null){i=void 0;break}e[0](i)}return i}
    examples:
      bigint:
        input: 123n
        output: 123n
        divergence:
          reason: the checks compile the reverse direction, which this codec leaves ambiguous, so they raise on the schema instead of answering about the value
          check: Ambiguous bigint -> bigint | number | undefined. Should number be decoded or ignored? Choose with S.to for bigint -> number, or S.never -> number
      number:
        input: "123.12"
        output: "123.12"
        divergence:
          reason: the checks compile the reverse direction, which this codec leaves ambiguous, so they raise on the schema instead of answering about the value
          check: Ambiguous bigint -> bigint | number | undefined. Should number be decoded or ignored? Choose with S.to for bigint -> number, or S.never -> number
      null-bridges-to-undefined:
        input: "null"
        output: undefined
        divergence:
          reason: the checks compile the reverse direction, which this codec leaves ambiguous, so they raise on the schema instead of answering about the value
          check: Ambiguous bigint -> bigint | number | undefined. Should number be decoded or ignored? Choose with S.to for bigint -> number, or S.never -> number
  decode: eq-to-parse
  encode:
    expression: i=>{for(;;){if(typeof i==="bigint")break;if(typeof i==="number"&&i==i)break;if(i===void 0){i=null;break}e[0](i)}return i}
    examples:
      undefined-bridges-to-null:
        input: undefined
        output: "null"
      bigint:
        input: 123n
        output: 123n
      number:
        input: "123.12"
        output: "123.12"
