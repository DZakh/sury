# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.optional(S.number).with(S.to, S.string)
  input: number | undefined
  output: string
  instantiations: 4587
jsonSchema:
  input: '{ type: "number" }'
  fromInputType: number
  output: '{ type: "string" }'
isEqual: strictEqual
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{if(typeof i==="number"&&i==i){i=""+i}else{e[0](i)}return i}catch(v0){e[1](v0)}}
    examples:
      number:
        input: "12"
        output: '"12"'
      undefined-is-not-text:
        input: undefined
        error: Expected number, received undefined
  decode: eq-to-parse
  encode:
    expression: i=>{try{let v0=+i;v0==v0&&(v0||i.trim())||e[0](i);i=v0;return i}catch(v1){e[1](v1)}}
    examples:
      text:
        input: '"12"'
        output: "12"
