# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: "S.string.with(S.to, S.number).with(S.meta, { examples: [42] })"
  input: string
  output: number
  instantiations: 5029
jsonSchema:
  input: '{ type: "string", examples: ["42"] }'
  output: '{ type: "number", examples: [42] }'
isEqual: strictEqual
compare: strictCompare
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{typeof i==="string"||e[1](i);let v0=+i;v0==v0&&(v0||i.trim())||e[0](i);return v0}catch(v1){e[2](v1)}}
    examples:
      number-text:
        input: '"42"'
        output: "42"
  decode:
    expression: i=>{try{let v0=+i;v0==v0&&(v0||i.trim())||e[0](i);return v0}catch(v1){e[1](v1)}}
    examples:
      number-text:
        input: '"42"'
        output: "42"
  encode:
    expression: i=>{return ""+i}
    examples:
      number:
        input: "42"
        output: '"42"'
