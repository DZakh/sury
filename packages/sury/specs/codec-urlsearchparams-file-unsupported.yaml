# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: "S.urlSearchParams.with(S.to, S.schema({ f: S.file }))"
  input: URLSearchParams
  output: "{ f: File; }"
  instantiations: 5475
jsonSchema:
  input: Expected JSON, received URLSearchParams
  output: "Failed at f: Expected JSON, received File"
isEqual:
  input: (a,b)=>a===b||e[0](a,b)
  output: (a,b)=>a===b||a.f===b.f
compare:
  input: (a,b)=>a===b?0:e[0](a,b)
  output: (a,b)=>a===b?0:a.f===b.f?0:1
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Failed at f: Can't decode search param -> File. Define custom codec with S.to"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Failed at f: Can't decode File -> URLSearchParams. Define custom codec with S.to"
