# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.nullable(S.record(S.string))
  input: Record<string, string> | null
  output: Record<string, string> | null
  instantiations: 635
jsonSchema:
  input: '{ anyOf: [{ type: "object", additionalProperties: { type: "string" } }, { type: "null" }] }'
  output: '{ anyOf: [{ type: "object", additionalProperties: { type: "string" } }, { type: "null" }] }'
  openapi-3.0:
    input: '{ type: "object", additionalProperties: { type: "string" }, nullable: true }'
    output: '{ type: "object", additionalProperties: { type: "string" }, nullable: true }'
isEqual: (a,b)=>a===b||(a===null?b===null:b!==null&&e[0](a,b))
compare: "throws: [Sury] Can't compare { [key: string]: string; }. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{for(;;){if(typeof i==="object"&&i&&!Array.isArray(i)){for(let v0 in i){let v1=i[v0];typeof v1==="string"||e[0](v1,[v0]);};break}if(i===null)break;throw e[1](i)}return i}catch(v2){e[2](v2)}}
    examples:
      record:
        input: '{ a: "x" }'
        output: '{ a: "x" }'
      other-record:
        input: '{ a: "y" }'
        output: '{ a: "y" }'
      nothing:
        input: "null"
        output: "null"
      not-a-record:
        input: "42"
        error: "Expected { [key: string]: string; } | null, received 42"
  decode: identity
  encode: identity
