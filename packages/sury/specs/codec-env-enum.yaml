# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.env.with(S.to, S.union(["development", "production"]))
  input: string | undefined
  output: '"development" | "production"'
  instantiations: 4952
jsonSchema:
  input: '{ type: "string" }'
  fromInputType: string
  output: '{ enum: ["development", "production"] }'
isEqual: strictEqual
compare:
  input: (a,b)=>a===b?0:a===void 0?b===void 0?0:-1:b===void 0?1:a<b?-1:a>b?1:0
  output: strictCompare
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{(typeof i==="string"||i===void 0)||e[1](i);(i==="development"||i==="production")||e[0](i);return i}catch(v0){e[2](v0)}}
    examples:
      development:
        input: '"development"'
        output: '"development"'
      production:
        input: '"production"'
        output: '"production"'
      other:
        input: '"test"'
        error: Expected "development" | "production", received "test"
      blank:
        input: '""'
        error: Expected "development" | "production", received ""
  decode:
    expression: i=>{try{(i==="development"||i==="production")||e[0](i);return i}catch(v0){e[1](v0)}}
    examples:
      development:
        input: '"development"'
        output: '"development"'
  encode:
    expression: i=>{try{typeof i==="string"&&(i==="development"||i==="production")||e[0](i);return i}catch(v0){e[1](v0)}}
    examples:
      development:
        input: '"development"'
        output: '"development"'
      other:
        input: '"test"'
        error: Expected "development" | "production", received "test"
