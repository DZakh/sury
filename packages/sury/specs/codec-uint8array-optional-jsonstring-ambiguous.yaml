# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.uint8Array.with(S.to, S.optional(S.jsonString))
  input: Uint8Array<ArrayBufferLike>
  output: string | undefined
  instantiations: 5434
jsonSchema:
  input: Expected JSON, received Uint8Array
  output: Expected JSON, received JSON string | undefined
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: 'SuryError: Ambiguous conversion from Uint8Array to JSON string | undefined. Use S.to(from, to, {decode: "unpack" | "pack", encode: ...})'
  decode: eq-to-parse
  encode: eq-to-parse
