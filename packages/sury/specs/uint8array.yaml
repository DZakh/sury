# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.uint8Array
  input: Uint8Array<ArrayBufferLike>
  output: Uint8Array<ArrayBufferLike>
  instantiations: 254
jsonSchema:
  input: Expected JSON, received Uint8Array
  output: Expected JSON, received Uint8Array
vs:
  zod:
    schema: z.instanceof(Uint8Array)
    divergence: Zod pins `Uint8Array<ArrayBuffer>`; Sury infers `Uint8Array<ArrayBufferLike>`.
    input: Uint8Array<ArrayBuffer>
    output: Uint8Array<ArrayBuffer>
operations:
  parse:
    expression: i=>{i instanceof e[0]||e[1](i);return i}
    examples:
      valid:
        input: new Uint8Array([1, 2, 3])
        output: new Uint8Array([1, 2, 3])
      invalid-type:
        input: '"hi"'
        error: Expected Uint8Array, received "hi"
  decode: identity
  encode: identity
