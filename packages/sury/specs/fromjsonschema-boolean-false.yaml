# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.fromJSONSchemaOrThrow(false)
  input: never
  output: never
  instantiations: 398
jsonSchema:
  input: "{ not: {} }"
  output: "{ not: {} }"
isEqual: (a,b)=>a===b||e[0](a,b)
compare: "throws: [Sury] Can't compare never. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
vs:
  zod: z.never()
operations:
  parse:
    expression: i=>{e[0](i);return i}
    examples:
      rejects-null:
        input: "null"
        error: Expected never, received null
      rejects-object:
        input: "{}"
        error: Expected never, received {}
  decode: eq-to-parse
  encode: eq-to-parse
