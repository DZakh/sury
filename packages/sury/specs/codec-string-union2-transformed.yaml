# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.string.with(S.to, S.union([S.string.with(S.to, S.number), S.string]))
  input: string
  output: string | number
  instantiations: 5597
jsonSchema:
  input: '{ type: "string" }'
  output: '{ anyOf: [{ type: "number" }, { type: "string" }] }'
isEqual: strictEqual
compare:
  input: strictCompare
  output: "throws: [Sury] Can't compare number | string. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{typeof i==="string"||e[1](i);let v1;for(;;){l2:{let v0=+i;if(!(v0==v0&&(v0||i.trim()))){(v1||(v1=[])).push(e[0],i,void 0);break l2};i=v0;break}break;}return i}catch(v3){e[2](v3)}}
    examples:
      decodes-to-number:
        input: '"123"'
        output: "123"
      falls-back-to-string:
        input: '"abc"'
        output: '"abc"'
      float:
        input: '"1.5"'
        output: "1.5"
  decode:
    expression: i=>{let v1;for(;;){l2:{let v0=+i;if(!(v0==v0&&(v0||i.trim()))){(v1||(v1=[])).push(e[0],i,void 0);break l2};i=v0;break}break;}return i}
    examples:
      decodes-to-number:
        input: '"123"'
        output: "123"
      falls-back-to-string:
        input: '"abc"'
        output: '"abc"'
      empty-string:
        input: '""'
        output: '""'
      whitespace-string:
        input: '"   "'
        output: '"   "'
      hex-string:
        input: '"0x10"'
        output: "16"
      infinity-string:
        input: '"Infinity"'
        output: Infinity
  encode:
    expression: i=>{try{for(;;){if(typeof i==="number"&&i==i){i=""+i;break}if(typeof i==="string")break;throw e[0](i)}return i}catch(v0){e[1](v0)}}
    examples:
      number:
        input: "123"
        output: '"123"'
      string:
        input: '"abc"'
        output: '"abc"'
