# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.number, S.string]).with(S.to, S.string)
  input: string | number
  output: string
  instantiations: 5766
vs:
  zod:
    _skip: not-applicable
jsonSchema:
  input: '{ anyOf: [{ type: "number" }, { type: "string" }] }'
  output: '{ type: "string" }'
operations:
  parse:
    creationError: "SuryError: Can't decode number | string to string: string matches it, number doesn't. Use S.to on it, or S.never to make it unreachable"
  decode: eq-to-parse
  encode: eq-to-parse
