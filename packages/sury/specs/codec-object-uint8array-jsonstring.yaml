# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: "S.schema({ payload: S.uint8Array }).with(S.to, S.jsonString)"
  input: "{ payload: Uint8Array<ArrayBufferLike>; }"
  output: string
  instantiations: 5915
vs:
  zod:
    _skip: not-applicable
jsonSchema:
  input: 'Failed at ["payload"]: Expected JSON, received Uint8Array'
  output: '{ type: "string" }'
operations:
  parse:
    expression: i=>{typeof i==="object"&&i&&!Array.isArray(i)||e[4](i);let v0=i["payload"];v0 instanceof e[0]||e[1](v0);return "{\"payload\":"+e[3](e[2](v0))+"}"}
    examples:
      ascii:
        input: "{ payload: new Uint8Array([72, 105]) }"
        output: '"{\"payload\":\"SGk=\"}"'
      not-valid-utf8:
        input: "{ payload: new Uint8Array([255, 254, 253]) }"
        output: '"{\"payload\":\"//79\"}"'
  decode:
    expression: i=>{return "{\"payload\":"+e[1](e[0](i["payload"]))+"}"}
    examples:
      ascii:
        input: "{ payload: new Uint8Array([72, 105]) }"
        output: '"{\"payload\":\"SGk=\"}"'
      not-valid-utf8:
        input: "{ payload: new Uint8Array([255, 254, 253]) }"
        output: '"{\"payload\":\"//79\"}"'
  encode:
    expression: i=>{let v0;try{v0=JSON.parse(i)}catch(t){e[0](i)}typeof v0==="object"&&v0&&!Array.isArray(v0)||e[5](v0);let v1=v0["payload"];typeof v1==="string"||e[4](v1);e[2].test(v1)||e[3](v1);return {"payload":e[1](v0["payload"]),}}
    examples:
      ascii:
        input: '"{\"payload\":\"SGk=\"}"'
        output: "{ payload: new Uint8Array([72, 105]) }"
      not-base64:
        input: '"{\"payload\":\"not base64!\"}"'
        error: 'Failed at ["payload"]: Expected base64, received "not base64!"'
