# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.json.with(S.to, S.record(S.optional(S.boolean)))
  input: JSON
  output: "{ [x: string]: boolean | undefined; }"
  instantiations: 4828
jsonSchema:
  input: '{ type: "object", additionalProperties: { anyOf: [{ type: "boolean" }, { type: "null" }] } }'
  fromInputType: "{ [key: string]: boolean | null; }"
  output: '{ type: "object", additionalProperties: { type: "boolean" } }'
  fromOutputType: "{ [key: string]: boolean; }"
  openapi-3.0:
    input: '{ type: "object", additionalProperties: { type: "boolean", nullable: true } }'
isEqual:
  input: (a,b)=>a===b||e[0](a,b)
  output: (a,b)=>{if(a===b)return true;let n=0;for(const k in a){if(!(k in b)||!(a[k]===b[k]))return false;n++}for(const k in b)n--;return !n}
compare:
  input: "throws: [Sury] Can't compare JSON. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
  output: "throws: [Sury] Can't compare { [key: string]: boolean | undefined; }. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{typeof i==="object"&&i&&!Array.isArray(i)||e[1](i);let v2={};for(let v0 in i){let v1=i[v0];for(;;){if(typeof v1==="boolean")break;if(v1===null){v1=void 0;break}e[0](v1,[v0])}v2[v0]=v1}return v2}catch(v3){e[2](v3)}}
    examples:
      null-becomes-undefined:
        input: "{ a: null, b: true }"
        output: "{ a: undefined, b: true }"
      empty:
        input: "{}"
        output: "{}"
      wrong-value-type:
        input: "{ a: 1 }"
        error: "Failed at a: Expected boolean | undefined, received 1"
  decode: eq-to-parse
  encode:
    expression: i=>{try{let v3={};for(let v1 in i){let v2=i[v1];for(;;){if(typeof v2==="boolean")break;if(v2===void 0){v2=null;break}e[0](v2,[v1])}v3[v1]=v2}return v3}catch(v4){e[1](v4)}}
    examples:
      undefined-becomes-null:
        input: "{ a: undefined, b: true }"
        output: "{ a: null, b: true }"
      empty:
        input: "{}"
        output: "{}"
