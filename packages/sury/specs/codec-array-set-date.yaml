# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.array(S.isoDateTime.with(S.to, S.date)).with(S.to, S.set(S.date))
  input: string[]
  output: Set<Date>
  instantiations: 5782
jsonSchema:
  input: '{ items: { type: "string", format: "date-time" }, type: "array" }'
  output: Expected JSON, received Set<Date>
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{Array.isArray(i)||e[4](i);let v4=new Array(i.length);for(let v0=0;v0<i.length;++v0){try{let v2=i[v0];typeof v2==="string"||e[3](v2);e[1].test(v2)||e[2](v2);let v1=new Date(i[v0]);!Number.isNaN(v1.getTime())||e[0](v1);v4[v0]=v1}catch(v3){v3.path='["'+v0+'"]'+v3.path;throw v3}}return new Set(v4)}
    examples:
      valid:
        input: '["1970-01-01T00:00:00.000Z"]'
        output: new Set([new Date("1970-01-01T00:00:00.000Z")])
      invalid-item:
        input: '["not-a-date"]'
        error: 'Failed at ["0"]: Invalid datetime string! Expected UTC'
  decode:
    expression: i=>{let v4=new Array(i.length);for(let v0=0;v0<i.length;++v0){try{let v2=i[v0];e[1].test(v2)||e[2](v2);let v1=new Date(i[v0]);!Number.isNaN(v1.getTime())||e[0](v1);v4[v0]=v1}catch(v3){v3.path='["'+v0+'"]'+v3.path;throw v3}}return new Set(v4)}
    examples:
      valid:
        input: '["1970-01-01T00:00:00.000Z"]'
        output: new Set([new Date("1970-01-01T00:00:00.000Z")])
  encode:
    expression: i=>{let v2=Array.from(i);let v6=new Array(v2.length);for(let v3=0;v3<v2.length;++v3){try{let v4=v2[v3].toISOString();e[0].test(v4)||e[1](v4);v6[v3]=v4}catch(v5){v5.path='["'+v3+'"]'+v5.path;throw v5}}return v6}
    examples:
      valid:
        input: new Set([new Date("1970-01-01T00:00:00.000Z")])
        output: '["1970-01-01T00:00:00.000Z"]'
