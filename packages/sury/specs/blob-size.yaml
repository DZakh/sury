# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.blob.with(S.size, 2)
  input: Blob
  output: Blob
  instantiations: 5162
jsonSchema:
  input: '{ type: "string", contentMediaType: "application/octet-stream" }'
  fromInputType: string
  output: '{ type: "string", contentMediaType: "application/octet-stream" }'
  fromOutputType: string
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{i instanceof e[1]||e[2](i);i.size===2||e[0](i);return i}
    examples:
      at-the-size:
        input: new Blob(["ab"])
        output: new Blob(["ab"])
      under-the-size:
        input: new Blob(["a"])
        error: Expected Blob.size == 2, received Blob
      over-the-size:
        input: new Blob(["abc"])
        error: Expected Blob.size == 2, received Blob
      wrong-type:
        input: '"ab"'
        error: Expected Blob.size == 2, received "ab"
      wrong-type-null:
        input: "null"
        error: Expected Blob.size == 2, received null
  decode:
    expression: i=>{i.size===2||e[0](i);return i}
    examples:
      off-the-size:
        input: new Blob(["abc"])
        error: Expected Blob.size == 2, received Blob
  encode:
    expression: i=>{i.size===2||e[0](i);return i}
    examples:
      off-the-size:
        input: new Blob(["abc"])
        error: Expected Blob.size == 2, received Blob
