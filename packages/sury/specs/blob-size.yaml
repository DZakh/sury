# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.blob.with(S.size, 2)
  input: Blob
  output: Blob
  instantiations: 592
jsonSchema:
  input: Expected JSON, received Blob.size == 2
  output: Expected JSON, received Blob.size == 2
isEqual: strictEqual
compare: "throws: [Sury] Can't compare Blob.size == 2. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{i instanceof e[1]||e[2](i);i.size===2||e[0](i);return i}catch(v0){e[3](v0)}}
    examples:
      at-the-size:
        input: new Blob(["ab"])
        output: new Blob(["ab"])
      under-the-size:
        input: new Blob(["a"])
        error: Expected Blob.size == 2, received Blob
      over-the-size:
        input: new Blob(["abc"])
        error: Expected Blob.size == 2, received Blob
      wrong-type:
        input: '"ab"'
        error: Expected Blob.size == 2, received "ab"
      wrong-type-null:
        input: "null"
        error: Expected Blob.size == 2, received null
  decode:
    expression: i=>{try{i.size===2||e[0](i);return i}catch(v0){e[1](v0)}}
    examples:
      off-the-size:
        input: new Blob(["abc"])
        error: Expected Blob.size == 2, received Blob
  encode:
    expression: i=>{try{i.size===2||e[0](i);return i}catch(v0){e[1](v0)}}
    examples:
      off-the-size:
        input: new Blob(["abc"])
        error: Expected Blob.size == 2, received Blob
