# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.env.with(S.to, S.date)
  input: string | undefined
  output: Date
  instantiations: 4419
jsonSchema:
  input: '{ type: "string" }'
  fromInputType: string
  output: Expected JSON, received Date
isEqual:
  input: strictEqual
  output: (a,b)=>a===b||+a===+b
compare:
  input: (a,b)=>a===b?0:a===void 0?b===void 0?0:-1:b===void 0?1:a<b?-1:a>b?1:0
  output: (a,b)=>a===b?0:+a<+b?-1:+a>+b?1:+a===+b?0:1
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{typeof i==="string"||e[1](i);let v0=new Date(i);!Number.isNaN(v0.getTime())||e[0](v0);return v0}catch(v1){e[2](v1)}}
    examples:
      valid:
        input: '"2024-01-01T00:00:00.000Z"'
        output: new Date("2024-01-01T00:00:00.000Z")
      invalid:
        input: '"nope"'
        error: Expected Date, received invalid Date
      blank:
        input: '""'
        error: Expected Date, received invalid Date
  decode:
    expression: i=>{try{i!==void 0||e[1](i);let v0=new Date(i);!Number.isNaN(v0.getTime())||e[0](v0);return v0}catch(v1){e[2](v1)}}
    examples:
      valid:
        input: '"2024-01-01T00:00:00.000Z"'
        output: new Date("2024-01-01T00:00:00.000Z")
  encode:
    expression: i=>{try{let v0;try{v0=i.toISOString()}catch(_){e[0](i)}return v0}catch(v1){e[1](v1)}}
    examples:
      valid:
        input: new Date("2024-01-01T00:00:00.000Z")
        output: '"2024-01-01T00:00:00.000Z"'
