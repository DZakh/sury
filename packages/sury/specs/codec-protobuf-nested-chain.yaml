# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: "S.schema({ m: S.schema({ a: S.int32.with(S.protobufField, 1) }).with(S.to, S.schema({ b: S.string.with(S.protobufField, 1) }), { decode: (w) => ({ b: String(w.a) }), encode: (d) => ({ a: Number(d.b) }) }).with(S.protobufField, 1) }).with(S.to, S.protobuf)"
  input: "{ m: { a: number; }; }"
  output: Uint8Array<ArrayBufferLike>
  instantiations: 8303
jsonSchema:
  input: '{ type: "object", properties: { m: { type: "object", properties: { a: { type: "integer", minimum: -2147483648, maximum: 2147483647 } }, required: ["a"] } }, required: ["m"] }'
  output: "[Sury] S.protobuf: field \"m\" is a message that converts further with S.to, which a nested field can't"
isEqual:
  input: (a,b)=>a===b||a.m.a===b.m.a
  output: (a,b)=>{if(a===b)return true;let n=a.length;if(n!==b.length)return false;for(let i=0;i<n;i++)if(!(a[i]===b[i]))return false;return true}
compare:
  input: (a,b)=>a===b?0:a.m.a<b.m.a?-1:a.m.a>b.m.a?1:0
  output: (a,b)=>{if(a===b)return 0;let n=a.length,m=b.length,l=n<m?n:m,c;for(let i=0;i<l;i++)if(c=a[i]<b[i]?-1:a[i]>b[i]?1:0)return c;return n<m?-1:n>m?1:0}
vs:
  zod:
    _skip: todo(#…)
operations:
  parse:
    creationError: "Error: [Sury] S.protobuf: field \"m\" is a message that converts further with S.to, which a nested field can't"
  decode: eq-to-parse
  encode: eq-to-parse
