# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.array(S.string).with(S.empty)
  input: string[]
  output: string[]
  instantiations: 659
vs:
  zod: z.array(z.string()).length(0)
jsonSchema:
  input: '{ items: { type: "string" }, type: "array", minItems: 0, maxItems: 0 }'
  output: '{ items: { type: "string" }, type: "array", minItems: 0, maxItems: 0 }'
operations:
  parse:
    expression: i=>{Array.isArray(i)||e[2](i);for(let v0=0;v0<i.length;++v0){try{let v1=i[v0];typeof v1==="string"||e[0](v1);}catch(v2){v2.path='["'+v0+'"]'+v2.path;throw v2}}i.length===0||e[1](i);return i}
    examples:
      at-the-length:
        input: '["a", "b"]'
        error: Expected string[].length == 0, received ["a", "b"]
      longer:
        input: '["a", "b", "c"]'
        error: Expected string[].length == 0, received ["a", "b", "c"]
      shorter:
        input: '["a"]'
        error: Expected string[].length == 0, received ["a"]
      empty:
        input: "[]"
        output: "[]"
      wrong-type:
        input: '"ab"'
        error: Expected string[].length == 0, received "ab"
  decode:
    expression: i=>{i.length===0||e[0](i);return i}
    examples:
      at-the-length:
        input: '["a", "b"]'
        error: Expected string[].length == 0, received ["a", "b"]
      shorter:
        input: '["a"]'
        error: Expected string[].length == 0, received ["a"]
      empty:
        input: "[]"
        output: "[]"
  encode:
    expression: i=>{i.length===0||e[0](i);return i}
    examples:
      at-the-length:
        input: '["a", "b"]'
        error: Expected string[].length == 0, received ["a", "b"]
      shorter:
        input: '["a"]'
        error: Expected string[].length == 0, received ["a"]
      empty:
        input: "[]"
        output: "[]"
