# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.string.with(S.minLength, 4), S.string.with(S.maxLength, 1), S.string.with(S.length, 2)])
  input: string
  output: string
  instantiations: 1994
jsonSchema:
  input: '{ anyOf: [{ type: "string", minLength: 4 }, { type: "string", maxLength: 1 }, { type: "string", minLength: 2, maxLength: 2 }] }'
  output: '{ anyOf: [{ type: "string", minLength: 4 }, { type: "string", maxLength: 1 }, { type: "string", minLength: 2, maxLength: 2 }] }'
isEqual: strictEqual
compare: strictCompare
vs:
  zod: z.union([z.string().min(4), z.string().max(1), z.string().length(2)])
operations:
  parse:
    expression: i=>{try{let v0;for(;;){l4:{if(typeof i==="string"){for(;;){l1:{if(!(i.length>3)){(v0||(v0=[])).push(e[0],i,void 0);break l1};break}l2:{if(!(i.length<2)){(v0||(v0=[])).push(e[1],i,void 0);break l2};break}l3:{if(!(i.length===2)){(v0||(v0=[])).push(e[2],i,void 0);break l3};break}break l4};break}}throw e[3](i,void 0,v0)}return i}catch(v5){e[4](v5)}}
    examples:
      accepted-first:
        input: '"long"'
        output: '"long"'
      accepted-second:
        input: '""'
        output: '""'
      accepted-third:
        input: '"ok"'
        output: '"ok"'
      rejected-by-all:
        input: '"bad"'
        error: |-
          Expected string.length >= 4 | string.length <= 1 | string.length == 2, received "bad"
          - Expected string.length >= 4, received "bad"
          - Expected string.length <= 1, received "bad"
          - Expected string.length == 2, received "bad"
      wrong-tag:
        input: "42"
        error: Expected string.length >= 4 | string.length <= 1 | string.length == 2, received 42
  decode: identity
  encode: identity
