# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.schema("apple"), S.string.with(S.to, S.number, Number, String), S.string])
  input: string
  output: string | number
  instantiations: 6477
vs:
  zod:
    _skip: not-applicable
jsonSchema:
  input: '{ anyOf: [{ type: "string", const: "apple" }, { type: "string" }] }'
  inputType: string
  output: '{ anyOf: [{ type: "string", const: "apple" }, { type: "number" }, { type: "string" }] }'
  outputType: string | number
operations:
  parse:
    expression: i=>{for(;;){let r;if(typeof i==="string"&&i==="apple")break;if(typeof i==="string"){try{let v0;try{v0=e[0](i)}catch(x){e[1](x);e[2](x)}i=v0;break}catch(x){(r||(r=[])).push(e[3](x))}break}e[4](i,...(r||[]))}return i}
    examples:
      literal-claims-its-value:
        input: '"apple"'
        output: '"apple"'
      coerced-by-codec:
        input: '"42"'
        output: "42"
      unparseable-string-coerces-to-nan:
        input: '"cherry"'
        output: NaN
      wrong-tag:
        input: "42"
        error: Expected "apple" | string, received 42
  decode: eq-to-parse
  encode:
    expression: i=>{for(;;){if(typeof i==="number"&&!Number.isNaN(i)){let v0;try{v0=e[0](i)}catch(x){e[1](x);e[2](x)}i=v0;break}if(typeof i==="string")break;e[3](i)}return i}
    examples:
      number-back-to-string:
        input: "42"
        output: '"42"'
      string-passes-through:
        input: '"apple"'
        output: '"apple"'
