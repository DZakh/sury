# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.array(S.number).with(S.to, S.map(S.string, S.number))
  input: number[]
  output: Map<string, number>
  instantiations: 5535
jsonSchema:
  input: '{ items: { type: "number" }, type: "array" }'
  output: Expected JSON, received Map<string, number>
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Can't decode number[] to Map<string, number>. Use S.to to define a custom decoder"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Failed at []: Can't decode [string, number] to number. Use S.to to define a custom decoder"
