# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.array(S.number).with(S.to, S.map(S.string, S.number))
  input: number[]
  output: Map<string, number>
  instantiations: 4860
jsonSchema:
  input: '{ items: { type: "number" }, type: "array" }'
  output: Expected JSON, received Map<string, number>
isEqual:
  input: (a,b)=>{if(a===b)return true;let n=a.length;if(n!==b.length)return false;for(let i=0;i<n;i++)if(!(a[i]===b[i]))return false;return true}
  output: (a,b)=>a===b||e[0](a,b)
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Can't decode number[] -> Map<string, number>. Define custom codec with S.to"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Failed at []: Can't decode [string, number] -> number. Define custom codec with S.to"
