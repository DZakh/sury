# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.schema(null)
  input: "null"
  output: "null"
  instantiations: 369
jsonSchema:
  input: '{ type: "null" }'
  output: '{ type: "null" }'
  openapi-3.0:
    input: "{ enum: [null] }"
    output: "{ enum: [null] }"
isEqual: alwaysEqual
compare: alwaysCompare
vs:
  zod: z.null()
operations:
  parse:
    expression: i=>{try{i===null||e[0](i);return i}catch(v0){e[1](v0)}}
    examples:
      valid:
        input: "null"
        output: "null"
      invalid-type:
        input: undefined
        error: Expected null, received undefined
  decode: eq-to-parse
  encode: eq-to-parse
