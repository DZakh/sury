# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: "S.shape(S.string, (string) => ({ foo: string }))"
  input: string
  output: "{ foo: string; }"
  instantiations: 503
jsonSchema:
  input: '{ type: "string" }'
  output: '{ type: "object", properties: { foo: { type: "string" } }, required: ["foo"] }'
isEqual:
  input: strictEqual
  output: (a,b)=>a===b||a.foo===b.foo
compare:
  input: strictCompare
  output: "throws: [Sury] Can't compare { foo: string; }. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{typeof i==="string"||e[0](i);return {foo:i}}catch(v0){e[1](v0)}}
    examples:
      valid:
        input: '"bar"'
        output: '{ foo: "bar" }'
      not-a-string:
        input: "1"
        error: Expected string, received 1
  decode:
    expression: i=>{return {foo:i}}
    examples:
      valid:
        input: '"bar"'
        output: '{ foo: "bar" }'
  encode:
    expression: i=>{return i.foo}
    examples:
      round-trip:
        input: '{ foo: "bar" }'
        output: '"bar"'
