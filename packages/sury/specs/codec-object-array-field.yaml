# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: "S.schema({ x: S.array(S.string) }).with(S.to, S.schema({ x: S.array(S.string) }), { decode: (v) => v, encode: (v) => v })"
  input: "{ x: string[]; }"
  output: "{ x: string[]; }"
  instantiations: 6175
jsonSchema:
  input: '{ type: "object", properties: { x: { items: { type: "string" }, type: "array" } }, required: ["x"] }'
  output: '{ type: "object", properties: { x: { items: { type: "string" }, type: "array" } }, required: ["x"] }'
isEqual: (a,b)=>a===b||e[0](a.x,b.x)
compare: "throws: [Sury] Can't compare { x: string[]; }. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
vs:
  zod:
    _skip: not-applicable
operations:
  # FIXME: `decode` and `encode` declare the source's array field
  # (`let v0=i["x"]`) and never read it - the coder takes the object whole, and
  # the array decoder materializes a var before finding it has no loop to emit.
  parse:
    expression: i=>{typeof i==="object"&&i&&!Array.isArray(i)||e[7](i);let v0=i.x;Array.isArray(v0)||e[1](v0);for(let v1=0;v1<v0.length;++v1){let v2=v0[v1];typeof v2==="string"||e[0](v2,["x",v1]);}let v3;try{v3=e[2]({x:v0})}catch(x){e[3](x)}typeof v3==="object"&&v3&&!Array.isArray(v3)||e[6](v3);let v4=v3.x;Array.isArray(v4)||e[5](v4);for(let v5=0;v5<v4.length;++v5){let v6=v4[v5];typeof v6==="string"||e[4](v6,["x",v5]);}return {x:v4}}
    examples:
      round-trip:
        input: '{ x: ["a"] }'
        output: '{ x: ["a"] }'
  decode:
    expression: i=>{let v0=i.x;let v2;try{v2=e[0](i)}catch(x){e[1](x)}typeof v2==="object"&&v2&&!Array.isArray(v2)||e[4](v2);let v3=v2.x;Array.isArray(v3)||e[3](v3);for(let v4=0;v4<v3.length;++v4){let v5=v3[v4];typeof v5==="string"||e[2](v5,["x",v4]);}return {x:v3}}
    examples:
      round-trip:
        input: '{ x: ["a"] }'
        output: '{ x: ["a"] }'
  encode:
    expression: i=>{let v0=i.x;let v2;try{v2=e[0](i)}catch(x){e[1](x)}typeof v2==="object"&&v2&&!Array.isArray(v2)||e[4](v2);let v3=v2.x;Array.isArray(v3)||e[3](v3);for(let v4=0;v4<v3.length;++v4){let v5=v3[v4];typeof v5==="string"||e[2](v5,["x",v4]);}return {x:v3}}
    examples:
      round-trip:
        input: '{ x: ["a"] }'
        output: '{ x: ["a"] }'
