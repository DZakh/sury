# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: "S.optional(S.string).with(S.to, S.optional(S.jsonString.with(S.to, S.schema({ a: S.number }))))"
  input: string | undefined
  output: "{ a: number; } | undefined"
  instantiations: 6261
jsonSchema:
  input: Expected JSON, received string | undefined
  output: "Expected JSON, received { a: number; } | undefined"
isEqual:
  input: strictEqual
  output: (a,b)=>a===b||(a===void 0?b===void 0:b!==void 0&&a.a===b.a)
compare:
  input: (a,b)=>a===b?0:(a===void 0?b===void 0?0:-1:b===void 0?1:a<b?-1:a>b?1:0)
  output: "throws: [Sury] Can't compare { a: number; }. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
vs:
  zod:
    _skip: todo(#…)
operations:
  parse:
    creationError: "SuryError: Ambiguous string | undefined -> JSON string | undefined. Should string be decoded or ignored? Choose with S.to for string -> JSON string, or S.never -> string"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Ambiguous { a: number; } | undefined -> string | undefined. Should JSON string be decoded or ignored? Choose with S.to for JSON string -> string, or S.never -> JSON string"
