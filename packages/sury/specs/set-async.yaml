# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: 'S.set(S.string.with(S.to, S.string, { decode: { async: async (value) => { if (value === "bad") { throw new Error("rejected") } return value } }, encode: "auto" }))'
  input: Set<string>
  output: Set<string>
  instantiations: 5620
jsonSchema:
  input: Expected JSON, received Set<string>
  output: Expected JSON, received Set<string>
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    isAsync: true
    expression: i=>{i instanceof e[4]||e[5](i);let v2=[];let v4=0;for(let v0 of i){let v3=v4++;try{typeof v0==="string"||e[3](v0);let v1;try{v1=e[0](v0).catch(x=>e[1](x))}catch(x){e[1](x)}v2.push(v1.then(v1=>{typeof v1==="string"||e[2](v1);return v1}).catch(v5=>{v5.path='["'+v3+'"]'+v5.path;throw v5}));}catch(v5){v5.path='["'+v3+'"]'+v5.path;throw v5}}return Promise.all(v2).then(v6=>new Set(v6))}
    examples:
      valid:
        input: new Set(["a", "b"])
        output: new Set(["a", "b"])
      rejected-item-is-located:
        input: new Set(["bad", "b"])
        error: 'Failed at ["0"]: rejected'
      rejected-later-item:
        input: new Set(["a", "bad"])
        error: 'Failed at ["1"]: rejected'
  decode:
    isAsync: true
    expression: i=>{let v2=[];let v4=0;for(let v0 of i){let v3=v4++;try{let v1;try{v1=e[0](v0).catch(x=>e[1](x))}catch(x){e[1](x)}v2.push(v1.then(v1=>{typeof v1==="string"||e[2](v1);return v1}).catch(v5=>{v5.path='["'+v3+'"]'+v5.path;throw v5}));}catch(v5){v5.path='["'+v3+'"]'+v5.path;throw v5}}return Promise.all(v2).then(v6=>new Set(v6))}
    examples:
      valid:
        input: new Set(["a"])
        output: new Set(["a"])
  encode: identity
