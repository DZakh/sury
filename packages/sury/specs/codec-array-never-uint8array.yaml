# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.array(S.never).with(S.to, S.array(S.uint8Array))
  input: never[]
  output: Uint8Array<ArrayBufferLike>[]
  instantiations: 4737
jsonSchema:
  input: '{ items: { not: {} }, type: "array" }'
  output: "Failed at []: Expected JSON, received Uint8Array"
isEqual: (a,b)=>{if(a===b)return true;let n=a.length;if(n!==b.length)return false;for(let i=0;i<n;i++)if(!(e[0](a[i],b[i])))return false;return true}
compare:
  input: "throws: [Sury] Can't compare never[]. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
  output: "throws: [Sury] Can't compare Uint8Array[]. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{Array.isArray(i)||e[1](i);for(let v0=0;v0<i.length;++v0){let v1=i[v0];e[0](v1,[v0]);}return i}catch(v3){e[2](v3)}}
    examples:
      empty:
        input: "[]"
        output: "[]"
      one-value:
        input: "[1]"
        error: "Failed at [0]: Expected never, received 1"
  decode:
    expression: i=>{try{for(let v0=0;v0<i.length;++v0){let v1=i[v0];e[0](v1,[v0]);}return i}catch(v3){e[1](v3)}}
    examples:
      empty:
        input: "[]"
        output: "[]"
  encode:
    expression: i=>{try{for(let v1=0;v1<i.length;++v1){let v2=i[v1];e[0](v2,[v1]);}return i}catch(v3){e[1](v3)}}
    examples:
      empty:
        input: "[]"
        output: "[]"
