{
  "type": "object",
  "properties": {
    "ts": {
      "type": "object",
      "properties": {
        "schema": {
          "type": "string",
          "description": "The schema under test, as JS `.with`-chain source, e.g. `S.string.with(S.min, 3)`. Authored by hand; this is the one thing \"spec new\"/\"spec update\" never overwrite."
        },
        "input": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "object",
              "properties": {
                "_skip": {
                  "type": "string",
                  "description": "Why this dimension is not asserted. An enum reason (parser-only, serializer-only, lossy, not-applicable) or todo(#…) for a not-yet-built dimension — never a bare/unexplained skip."
                }
              },
              "additionalProperties": false,
              "required": [
                "_skip"
              ],
              "description": "Explicit opt-out for a dimension that isn't asserted for this spec."
            }
          ],
          "description": "S.Input<schema>, printed as a TS type string (e.g. `string`). Filled by `pnpm spec update` via vendored TS introspection."
        },
        "output": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "object",
              "properties": {
                "_skip": {
                  "type": "string",
                  "description": "Why this dimension is not asserted. An enum reason (parser-only, serializer-only, lossy, not-applicable) or todo(#…) for a not-yet-built dimension — never a bare/unexplained skip."
                }
              },
              "additionalProperties": false,
              "required": [
                "_skip"
              ],
              "description": "Explicit opt-out for a dimension that isn't asserted for this spec."
            }
          ],
          "description": "S.Output<schema>, printed as a TS type string. Filled by `pnpm spec update` via vendored TS introspection."
        },
        "instantiations": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "object",
              "properties": {
                "_skip": {
                  "type": "string",
                  "description": "Why this dimension is not asserted. An enum reason (parser-only, serializer-only, lossy, not-applicable) or todo(#…) for a not-yet-built dimension — never a bare/unexplained skip."
                }
              },
              "additionalProperties": false,
              "required": [
                "_skip"
              ],
              "description": "Explicit opt-out for a dimension that isn't asserted for this spec."
            }
          ],
          "description": "Type-instantiation count contributed by declaring the schema and extracting its Output/Input types. Filled by `pnpm spec update`."
        },
        "bundleBytes": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "object",
              "properties": {
                "_skip": {
                  "type": "string",
                  "description": "Why this dimension is not asserted. An enum reason (parser-only, serializer-only, lossy, not-applicable) or todo(#…) for a not-yet-built dimension — never a bare/unexplained skip."
                }
              },
              "additionalProperties": false,
              "required": [
                "_skip"
              ],
              "description": "Explicit opt-out for a dimension that isn't asserted for this spec."
            }
          ],
          "description": "Tree-shaken, minified+gzipped byte size of `S.parser(schema)` bundled against the dev source. Filled by `pnpm spec update`."
        }
      },
      "additionalProperties": false,
      "required": [
        "schema",
        "input",
        "output",
        "instantiations",
        "bundleBytes"
      ],
      "description": "The JS `.with`-chain surface: source plus every surface-specific dimension."
    },
    "jsonSchema": {
      "anyOf": [
        {
          "type": "object",
          "properties": {
            "input": {},
            "output": {}
          },
          "additionalProperties": false,
          "required": [
            "input",
            "output"
          ]
        },
        {
          "type": "object",
          "properties": {
            "_skip": {
              "type": "string",
              "description": "Why this dimension is not asserted. An enum reason (parser-only, serializer-only, lossy, not-applicable) or todo(#…) for a not-yet-built dimension — never a bare/unexplained skip."
            }
          },
          "additionalProperties": false,
          "required": [
            "_skip"
          ],
          "description": "Explicit opt-out for a dimension that isn't asserted for this spec."
        }
      ],
      "description": "S.toJSONSchema(schema) for both directions. Filled by `pnpm spec update`."
    },
    "operations": {
      "type": "object",
      "properties": {
        "parse": {
          "anyOf": [
            {
              "type": "string",
              "const": "identity"
            },
            {
              "type": "object",
              "properties": {
                "expression": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "object",
                      "properties": {
                        "_skip": {
                          "type": "string",
                          "description": "Why this dimension is not asserted. An enum reason (parser-only, serializer-only, lossy, not-applicable) or todo(#…) for a not-yet-built dimension — never a bare/unexplained skip."
                        }
                      },
                      "additionalProperties": false,
                      "required": [
                        "_skip"
                      ],
                      "description": "Explicit opt-out for a dimension that isn't asserted for this spec."
                    }
                  ],
                  "description": "Codegen golden: `.toString()` of the compiled parser/decoder/encoder function. Filled by `pnpm spec update`."
                },
                "examples": {
                  "type": "object",
                  "additionalProperties": {
                    "anyOf": [
                      {
                        "type": "object",
                        "properties": {
                          "input": {
                            "type": "string",
                            "description": "Source text for the input value, e.g. '\"hello\"' or \"42\". Written by hand; `pnpm spec update` derives output/error from it."
                          },
                          "output": {
                            "type": "string",
                            "description": "Source text for the expected output value. Filled by `pnpm spec update`."
                          },
                          "bench": {
                            "type": "boolean",
                            "description": "Reserved for the not-yet-wired performance dimension."
                          }
                        },
                        "additionalProperties": false,
                        "required": [
                          "input",
                          "output"
                        ]
                      },
                      {
                        "type": "object",
                        "properties": {
                          "input": {
                            "type": "string",
                            "description": "Source text for the input value, e.g. '\"hello\"' or \"42\". Written by hand; `pnpm spec update` derives output/error from it."
                          },
                          "error": {
                            "type": "string",
                            "description": "Expected error message. Filled by `pnpm spec update`."
                          },
                          "bench": {
                            "type": "boolean",
                            "description": "Reserved for the not-yet-wired performance dimension."
                          }
                        },
                        "additionalProperties": false,
                        "required": [
                          "input",
                          "error"
                        ]
                      }
                    ],
                    "description": "One named, runnable case: an input plus its expected output or error."
                  },
                  "description": "Named example cases for this operation, keyed by a short descriptive name (e.g. `valid`, `invalid-type`)."
                }
              },
              "additionalProperties": false,
              "required": [
                "expression",
                "examples"
              ],
              "description": "A full operation block: the compiled codegen plus its runnable examples."
            }
          ],
          "description": "unknown → Output, with validation (the untrusted-input direction)."
        },
        "decode": {
          "anyOf": [
            {
              "type": "string",
              "const": "identity"
            },
            {
              "type": "object",
              "properties": {
                "expression": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "object",
                      "properties": {
                        "_skip": {
                          "type": "string",
                          "description": "Why this dimension is not asserted. An enum reason (parser-only, serializer-only, lossy, not-applicable) or todo(#…) for a not-yet-built dimension — never a bare/unexplained skip."
                        }
                      },
                      "additionalProperties": false,
                      "required": [
                        "_skip"
                      ],
                      "description": "Explicit opt-out for a dimension that isn't asserted for this spec."
                    }
                  ],
                  "description": "Codegen golden: `.toString()` of the compiled parser/decoder/encoder function. Filled by `pnpm spec update`."
                },
                "examples": {
                  "type": "object",
                  "additionalProperties": {
                    "anyOf": [
                      {
                        "type": "object",
                        "properties": {
                          "input": {
                            "type": "string",
                            "description": "Source text for the input value, e.g. '\"hello\"' or \"42\". Written by hand; `pnpm spec update` derives output/error from it."
                          },
                          "output": {
                            "type": "string",
                            "description": "Source text for the expected output value. Filled by `pnpm spec update`."
                          },
                          "bench": {
                            "type": "boolean",
                            "description": "Reserved for the not-yet-wired performance dimension."
                          }
                        },
                        "additionalProperties": false,
                        "required": [
                          "input",
                          "output"
                        ]
                      },
                      {
                        "type": "object",
                        "properties": {
                          "input": {
                            "type": "string",
                            "description": "Source text for the input value, e.g. '\"hello\"' or \"42\". Written by hand; `pnpm spec update` derives output/error from it."
                          },
                          "error": {
                            "type": "string",
                            "description": "Expected error message. Filled by `pnpm spec update`."
                          },
                          "bench": {
                            "type": "boolean",
                            "description": "Reserved for the not-yet-wired performance dimension."
                          }
                        },
                        "additionalProperties": false,
                        "required": [
                          "input",
                          "error"
                        ]
                      }
                    ],
                    "description": "One named, runnable case: an input plus its expected output or error."
                  },
                  "description": "Named example cases for this operation, keyed by a short descriptive name (e.g. `valid`, `invalid-type`)."
                }
              },
              "additionalProperties": false,
              "required": [
                "expression",
                "examples"
              ],
              "description": "A full operation block: the compiled codegen plus its runnable examples."
            }
          ],
          "description": "Input → Output, no top-level type narrowing (input is already typed)."
        },
        "encode": {
          "anyOf": [
            {
              "type": "string",
              "const": "identity"
            },
            {
              "type": "object",
              "properties": {
                "expression": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "object",
                      "properties": {
                        "_skip": {
                          "type": "string",
                          "description": "Why this dimension is not asserted. An enum reason (parser-only, serializer-only, lossy, not-applicable) or todo(#…) for a not-yet-built dimension — never a bare/unexplained skip."
                        }
                      },
                      "additionalProperties": false,
                      "required": [
                        "_skip"
                      ],
                      "description": "Explicit opt-out for a dimension that isn't asserted for this spec."
                    }
                  ],
                  "description": "Codegen golden: `.toString()` of the compiled parser/decoder/encoder function. Filled by `pnpm spec update`."
                },
                "examples": {
                  "type": "object",
                  "additionalProperties": {
                    "anyOf": [
                      {
                        "type": "object",
                        "properties": {
                          "input": {
                            "type": "string",
                            "description": "Source text for the input value, e.g. '\"hello\"' or \"42\". Written by hand; `pnpm spec update` derives output/error from it."
                          },
                          "output": {
                            "type": "string",
                            "description": "Source text for the expected output value. Filled by `pnpm spec update`."
                          },
                          "bench": {
                            "type": "boolean",
                            "description": "Reserved for the not-yet-wired performance dimension."
                          }
                        },
                        "additionalProperties": false,
                        "required": [
                          "input",
                          "output"
                        ]
                      },
                      {
                        "type": "object",
                        "properties": {
                          "input": {
                            "type": "string",
                            "description": "Source text for the input value, e.g. '\"hello\"' or \"42\". Written by hand; `pnpm spec update` derives output/error from it."
                          },
                          "error": {
                            "type": "string",
                            "description": "Expected error message. Filled by `pnpm spec update`."
                          },
                          "bench": {
                            "type": "boolean",
                            "description": "Reserved for the not-yet-wired performance dimension."
                          }
                        },
                        "additionalProperties": false,
                        "required": [
                          "input",
                          "error"
                        ]
                      }
                    ],
                    "description": "One named, runnable case: an input plus its expected output or error."
                  },
                  "description": "Named example cases for this operation, keyed by a short descriptive name (e.g. `valid`, `invalid-type`)."
                }
              },
              "additionalProperties": false,
              "required": [
                "expression",
                "examples"
              ],
              "description": "A full operation block: the compiled codegen plus its runnable examples."
            }
          ],
          "description": "Output → Input (the reverse direction)."
        }
      },
      "additionalProperties": false,
      "required": [
        "parse",
        "decode",
        "encode"
      ],
      "description": "The three compiled directions through the schema: parse, decode, encode."
    }
  },
  "additionalProperties": false,
  "required": [
    "ts",
    "jsonSchema",
    "operations"
  ],
  "description": "A Sury spec: one schema's complete, machine-checked contract — type, JSON Schema, and per-operation codegen + examples. See the `spec` skill."
}
