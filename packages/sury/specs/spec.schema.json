{
  "type": "object",
  "properties": {
    "ts": {
      "type": "object",
      "properties": {
        "schema": {
          "type": "string",
          "description": "The schema under test, as JS `.with`-chain source (e.g. `S.string.with(S.min, 3)`). You write this by hand; `spec check --write` never touches it."
        },
        "aliases": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Alternate `.with`-chain sources that must produce a schema equivalent to `schema` — same ts.input/ts.output, jsonSchema, and operations. Checked live (not separately snapshotted) by `spec check`. You write these by hand."
        },
        "input": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "object",
              "properties": {
                "_skip": {
                  "type": "string",
                  "description": "Why unasserted: parser-only | serializer-only | lossy | not-applicable | todo(#…)."
                }
              },
              "additionalProperties": false,
              "required": [
                "_skip"
              ],
              "description": "Explicit opt-out for an unasserted dimension."
            }
          ],
          "description": "`S.Input<typeof schema>` as a TS type string. Filled by `spec check --write`."
        },
        "output": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "object",
              "properties": {
                "_skip": {
                  "type": "string",
                  "description": "Why unasserted: parser-only | serializer-only | lossy | not-applicable | todo(#…)."
                }
              },
              "additionalProperties": false,
              "required": [
                "_skip"
              ],
              "description": "Explicit opt-out for an unasserted dimension."
            }
          ],
          "description": "`S.Output<typeof schema>` as a TS type string. Filled by `spec check --write`."
        },
        "instantiations": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "object",
              "properties": {
                "_skip": {
                  "type": "string",
                  "description": "Why unasserted: parser-only | serializer-only | lossy | not-applicable | todo(#…)."
                }
              },
              "additionalProperties": false,
              "required": [
                "_skip"
              ],
              "description": "Explicit opt-out for an unasserted dimension."
            }
          ],
          "description": "TS type-instantiation cost of this schema. Filled by `spec check --write`."
        },
        "bundleBytes": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "object",
              "properties": {
                "_skip": {
                  "type": "string",
                  "description": "Why unasserted: parser-only | serializer-only | lossy | not-applicable | todo(#…)."
                }
              },
              "additionalProperties": false,
              "required": [
                "_skip"
              ],
              "description": "Explicit opt-out for an unasserted dimension."
            }
          ],
          "description": "Minified+gzipped bundle size of `schema` itself. Filled by `spec check --write`."
        }
      },
      "additionalProperties": false,
      "required": [
        "schema",
        "input",
        "output",
        "instantiations",
        "bundleBytes"
      ],
      "description": "The JS `.with`-chain surface: the schema itself, plus its inferred types, instantiation cost, and bundle size."
    },
    "vs": {
      "type": "object",
      "properties": {
        "zod": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "object",
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "Equivalent Zod (v4) schema, at least one of whose inferred types differs from ts, e.g. `z.object({...})`."
                },
                "divergence": {
                  "type": "string",
                  "description": "Hand-written: exactly how the Zod type differs from Sury's and why, e.g. `Zod groups optionals last; S.merge keeps insertion order`."
                },
                "input": {
                  "type": "string",
                  "description": "Zod's inferred input type, recorded only when it diverges from ts.input (filled by `--write`); omit when it matches."
                },
                "output": {
                  "type": "string",
                  "description": "Zod's inferred output type, recorded only when it diverges from ts.output (filled by `--write`); omit when it matches."
                }
              },
              "additionalProperties": false,
              "required": [
                "schema",
                "divergence"
              ],
              "description": "Overwrite form: a Zod equivalent that infers a different type than Sury on at least one side, with the divergent side(s) recorded."
            },
            {
              "type": "object",
              "properties": {
                "_skip": {
                  "type": "string",
                  "description": "Why unasserted: parser-only | serializer-only | lossy | not-applicable | todo(#…)."
                }
              },
              "additionalProperties": false,
              "required": [
                "_skip"
              ],
              "description": "Explicit opt-out for an unasserted dimension."
            }
          ],
          "description": "Equivalent Zod (v4) schema, e.g. `z.string().min(3)`. Bare string: inferred types must equal ts.input/ts.output. Object `{schema,divergence,input?,output?}`: the Zod type differs from ts on at least one side — `divergence` explains how, the divergent side is recorded (filled by `--write`), a matching side is omitted. `_skip` alone if Zod can't express it at all."
        }
      },
      "additionalProperties": false,
      "required": [
        "zod"
      ],
      "description": "Cross-library equivalents, type-checked against this spec."
    },
    "jsonSchema": {
      "type": "object",
      "properties": {
        "input": {
          "type": "string"
        },
        "output": {
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "input",
        "output"
      ],
      "description": "S.toJSONSchema(schema) for both directions, as a one-line source-text string (same formatting as example values) — or (per direction) the message S.toJSONSchema threw if that direction can't be represented (e.g. a bigint/symbol field). Filled by `spec check --write`."
    },
    "operations": {
      "type": "object",
      "properties": {
        "parse": {
          "anyOf": [
            {
              "type": "string",
              "const": "identity"
            },
            {
              "type": "object",
              "properties": {
                "expression": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "object",
                      "properties": {
                        "_skip": {
                          "type": "string",
                          "description": "Why unasserted: parser-only | serializer-only | lossy | not-applicable | todo(#…)."
                        }
                      },
                      "additionalProperties": false,
                      "required": [
                        "_skip"
                      ],
                      "description": "Explicit opt-out for an unasserted dimension."
                    }
                  ],
                  "description": "Compiled function source (`.toString()`). Filled by `spec check --write`."
                },
                "examples": {
                  "type": "object",
                  "additionalProperties": {
                    "anyOf": [
                      {
                        "type": "object",
                        "properties": {
                          "input": {
                            "type": "string",
                            "description": "Source text for the input, e.g. '\"hello\"'. Hand-written; `spec check --write` fills output/error."
                          },
                          "output": {
                            "type": "string",
                            "description": "Expected output source text. Filled by `spec check --write`."
                          },
                          "bench": {
                            "type": "boolean",
                            "description": "Reserved for the not-yet-wired performance dimension."
                          }
                        },
                        "additionalProperties": false,
                        "required": [
                          "input",
                          "output"
                        ]
                      },
                      {
                        "type": "object",
                        "properties": {
                          "input": {
                            "type": "string",
                            "description": "Source text for the input, e.g. '\"hello\"'. Hand-written; `spec check --write` fills output/error."
                          },
                          "error": {
                            "type": "string",
                            "description": "Expected error message. Filled by `spec check --write`."
                          },
                          "bench": {
                            "type": "boolean",
                            "description": "Reserved for the not-yet-wired performance dimension."
                          }
                        },
                        "additionalProperties": false,
                        "required": [
                          "input",
                          "error"
                        ]
                      }
                    ],
                    "description": "A named example: input plus expected output or error."
                  },
                  "description": "Named example cases, keyed by a short name (e.g. `valid`, `invalid-type`)."
                }
              },
              "additionalProperties": false,
              "required": [
                "expression",
                "examples"
              ],
              "description": "Compiled codegen plus its runnable examples."
            }
          ],
          "description": "unknown → Output, with validation (the untrusted-input direction)."
        },
        "decode": {
          "anyOf": [
            {
              "type": "string",
              "const": "identity"
            },
            {
              "type": "string",
              "const": "eq-to-parse"
            },
            {
              "type": "object",
              "properties": {
                "expression": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "object",
                      "properties": {
                        "_skip": {
                          "type": "string",
                          "description": "Why unasserted: parser-only | serializer-only | lossy | not-applicable | todo(#…)."
                        }
                      },
                      "additionalProperties": false,
                      "required": [
                        "_skip"
                      ],
                      "description": "Explicit opt-out for an unasserted dimension."
                    }
                  ],
                  "description": "Compiled function source (`.toString()`). Filled by `spec check --write`."
                },
                "examples": {
                  "type": "object",
                  "additionalProperties": {
                    "anyOf": [
                      {
                        "type": "object",
                        "properties": {
                          "input": {
                            "type": "string",
                            "description": "Source text for the input, e.g. '\"hello\"'. Hand-written; `spec check --write` fills output/error."
                          },
                          "output": {
                            "type": "string",
                            "description": "Expected output source text. Filled by `spec check --write`."
                          },
                          "bench": {
                            "type": "boolean",
                            "description": "Reserved for the not-yet-wired performance dimension."
                          }
                        },
                        "additionalProperties": false,
                        "required": [
                          "input",
                          "output"
                        ]
                      },
                      {
                        "type": "object",
                        "properties": {
                          "input": {
                            "type": "string",
                            "description": "Source text for the input, e.g. '\"hello\"'. Hand-written; `spec check --write` fills output/error."
                          },
                          "error": {
                            "type": "string",
                            "description": "Expected error message. Filled by `spec check --write`."
                          },
                          "bench": {
                            "type": "boolean",
                            "description": "Reserved for the not-yet-wired performance dimension."
                          }
                        },
                        "additionalProperties": false,
                        "required": [
                          "input",
                          "error"
                        ]
                      }
                    ],
                    "description": "A named example: input plus expected output or error."
                  },
                  "description": "Named example cases, keyed by a short name (e.g. `valid`, `invalid-type`)."
                }
              },
              "additionalProperties": false,
              "required": [
                "expression",
                "examples"
              ],
              "description": "Compiled codegen plus its runnable examples."
            }
          ],
          "description": "Input → Output, no top-level type narrowing (input is already typed)."
        },
        "encode": {
          "anyOf": [
            {
              "type": "string",
              "const": "identity"
            },
            {
              "type": "string",
              "const": "eq-to-parse"
            },
            {
              "type": "object",
              "properties": {
                "expression": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "object",
                      "properties": {
                        "_skip": {
                          "type": "string",
                          "description": "Why unasserted: parser-only | serializer-only | lossy | not-applicable | todo(#…)."
                        }
                      },
                      "additionalProperties": false,
                      "required": [
                        "_skip"
                      ],
                      "description": "Explicit opt-out for an unasserted dimension."
                    }
                  ],
                  "description": "Compiled function source (`.toString()`). Filled by `spec check --write`."
                },
                "examples": {
                  "type": "object",
                  "additionalProperties": {
                    "anyOf": [
                      {
                        "type": "object",
                        "properties": {
                          "input": {
                            "type": "string",
                            "description": "Source text for the input, e.g. '\"hello\"'. Hand-written; `spec check --write` fills output/error."
                          },
                          "output": {
                            "type": "string",
                            "description": "Expected output source text. Filled by `spec check --write`."
                          },
                          "bench": {
                            "type": "boolean",
                            "description": "Reserved for the not-yet-wired performance dimension."
                          }
                        },
                        "additionalProperties": false,
                        "required": [
                          "input",
                          "output"
                        ]
                      },
                      {
                        "type": "object",
                        "properties": {
                          "input": {
                            "type": "string",
                            "description": "Source text for the input, e.g. '\"hello\"'. Hand-written; `spec check --write` fills output/error."
                          },
                          "error": {
                            "type": "string",
                            "description": "Expected error message. Filled by `spec check --write`."
                          },
                          "bench": {
                            "type": "boolean",
                            "description": "Reserved for the not-yet-wired performance dimension."
                          }
                        },
                        "additionalProperties": false,
                        "required": [
                          "input",
                          "error"
                        ]
                      }
                    ],
                    "description": "A named example: input plus expected output or error."
                  },
                  "description": "Named example cases, keyed by a short name (e.g. `valid`, `invalid-type`)."
                }
              },
              "additionalProperties": false,
              "required": [
                "expression",
                "examples"
              ],
              "description": "Compiled codegen plus its runnable examples."
            }
          ],
          "description": "Output → Input (the reverse direction)."
        }
      },
      "additionalProperties": false,
      "required": [
        "parse",
        "decode",
        "encode"
      ],
      "description": "The three compiled directions through the schema: parse, decode, encode."
    }
  },
  "additionalProperties": false,
  "required": [
    "ts",
    "vs",
    "jsonSchema",
    "operations"
  ],
  "description": "One schema's complete, machine-checked contract. See the `spec` skill."
}
