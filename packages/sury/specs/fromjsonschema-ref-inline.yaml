# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: 'S.fromJSONSchema({ type: "object", properties: { from: { $ref: "#/$defs/Point" }, to: { $ref: "#/$defs/Point" } }, required: ["from", "to"], $defs: { Point: { type: "object", properties: { x: { type: "number" }, y: { type: "number" } }, required: ["x", "y"] } } })'
  input: "{ from: { x: number; y: number; }; to: { x: number; y: number; }; }"
  output: "{ from: { x: number; y: number; }; to: { x: number; y: number; }; }"
  instantiations: 2764
vs:
  zod:
    _skip: not-applicable
jsonSchema:
  input: '{ type: "object", additionalProperties: true, properties: { from: { type: "object", additionalProperties: true, properties: { x: { type: "number" }, y: { type: "number" } }, required: ["x", "y"] }, to: { type: "object", additionalProperties: true, properties: { x: { type: "number" }, y: { type: "number" } }, required: ["x", "y"] } }, required: ["from", "to"] }'
  output: '{ type: "object", additionalProperties: true, properties: { from: { type: "object", additionalProperties: true, properties: { x: { type: "number" }, y: { type: "number" } }, required: ["x", "y"] }, to: { type: "object", additionalProperties: true, properties: { x: { type: "number" }, y: { type: "number" } }, required: ["x", "y"] } }, required: ["from", "to"] }'
operations:
  parse:
    expression: i=>{typeof i==="object"&&i&&!Array.isArray(i)||e[3](i);e[1](i)||e[4](i);e[2](i)||e[5](i);for(let v0 in i){try{e[0](i[v0]);}catch(v1){v1.path='["'+v0+'"]'+v1.path;throw v1}}return i}
    examples:
      both-points:
        input: "{ from: { x: 0, y: 0 }, to: { x: 1, y: 2 } }"
        output: "{ from: { x: 0, y: 0 }, to: { x: 1, y: 2 } }"
      missing-a-coordinate:
        input: "{ from: { x: 0 }, to: { x: 1, y: 2 } }"
        error: Should pass every declared property schema.
      wrong-coordinate-type:
        input: '{ from: { x: 0, y: 0 }, to: { x: 1, y: "2" } }'
        error: Should pass every declared property schema.
      missing-a-point:
        input: "{ from: { x: 0, y: 0 } }"
        error: Should contain every required property.
  decode:
    expression: i=>{e[0](i)||e[2](i);e[1](i)||e[3](i);return i}
    examples:
      both-points:
        input: "{ from: { x: 0, y: 0 }, to: { x: 1, y: 2 } }"
        output: "{ from: { x: 0, y: 0 }, to: { x: 1, y: 2 } }"
      missing-a-coordinate:
        input: "{ from: { x: 0 }, to: { x: 1, y: 2 } }"
        error: Should pass every declared property schema.
      wrong-coordinate-type:
        input: '{ from: { x: 0, y: 0 }, to: { x: 1, y: "2" } }'
        error: Should pass every declared property schema.
      missing-a-point:
        input: "{ from: { x: 0, y: 0 } }"
        error: Should contain every required property.
  encode:
    expression: i=>{e[0](i)||e[2](i);e[1](i)||e[3](i);return i}
    examples:
      both-points:
        input: "{ from: { x: 0, y: 0 }, to: { x: 1, y: 2 } }"
        output: "{ from: { x: 0, y: 0 }, to: { x: 1, y: 2 } }"
      missing-a-coordinate:
        input: "{ from: { x: 0 }, to: { x: 1, y: 2 } }"
        error: Should pass every declared property schema.
      wrong-coordinate-type:
        input: '{ from: { x: 0, y: 0 }, to: { x: 1, y: "2" } }'
        error: Should pass every declared property schema.
      missing-a-point:
        input: "{ from: { x: 0, y: 0 } }"
        error: Should contain every required property.
