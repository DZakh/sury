# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.string.with(S.refine, (v) => { throw new TypeError("predicate blew up") }), S.string])
  input: string
  output: string
  instantiations: 5503
vs:
  zod: z.union([z.string().refine((v) => { throw new TypeError("predicate blew up") }), z.string()])
jsonSchema:
  input: '{ type: "string" }'
  output: '{ type: "string" }'
operations:
  parse:
    expression: i=>{try{if(typeof i==="string"){for(;;){let r;try{e[0](i)||e[1](i);break}catch(x){(r||(r=[])).push(e[2](x))}break;}}else{e[3](i)}return i}catch(x){throw e[4](x,i)}}
    examples:
      foreign-error-propagates-instead-of-matching-the-catch-all:
        input: '"x"'
        error: predicate blew up
      wrong-type-still-reports-the-union:
        input: "1"
        error: Expected string, received 1
  decode: identity
  encode: identity
