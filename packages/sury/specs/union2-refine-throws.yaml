# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.string.with(S.refine, (v) => { throw new TypeError("predicate blew up") }), S.string])
  input: string
  output: string
  instantiations: 5518
vs:
  zod: z.union([z.string().refine((v) => { throw new TypeError("predicate blew up") }), z.string()])
jsonSchema:
  input: '{ type: "string" }'
  output: '{ type: "string" }'
operations:
  parse:
    expression: i=>{if(typeof i==="string"){try{e[0](i)||e[1](i);}catch(e0){e[2](e0);}}else{e[3](i)}return i}
    examples:
      foreign-error-propagates-instead-of-matching-the-catch-all:
        input: '"x"'
        error: predicate blew up
      wrong-type-still-reports-the-union:
        input: "1"
        error: Expected string | string, received 1
  decode: identity
  encode: identity
