# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: "S.env.with(S.to, S.schema({ PORT: S.port }))"
  input: string | undefined
  output: "{ PORT: number; }"
  instantiations: 5446
jsonSchema:
  input: '{ type: "string" }'
  fromInputType: string
  output: '{ type: "object", properties: { PORT: { type: "integer", minimum: 0, maximum: 65535 } }, required: ["PORT"] }'
isEqual:
  input: strictEqual
  output: (a,b)=>a===b||a.PORT===b.PORT
compare:
  input: (a,b)=>a===b?0:a===void 0?b===void 0?0:-1:b===void 0?1:a<b?-1:a>b?1:0
  output: "throws: [Sury] Can't compare { PORT: port; }. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Can't decode env -> { PORT: port; }. Define custom codec with S.to"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Can't decode { PORT: port; } -> env. Define custom codec with S.to"
