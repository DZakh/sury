# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.boolean.with(S.to, S.union([S.string, S.symbol]))
  input: boolean
  output: string | symbol
  instantiations: 5394
jsonSchema:
  input: '{ type: "boolean" }'
  output: Expected JSON, received string | symbol
isEqual: strictEqual
compare:
  input: strictCompare
  output: "throws: [Sury] Can't compare string | symbol. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Can't decode boolean -> symbol. Define custom codec with S.to"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Can't decode symbol -> boolean. Define custom codec with S.to"
