# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.any
  input: any
  output: any
  instantiations: 254
jsonSchema:
  input: "{}"
  output: "{}"
isEqual: (a,b)=>a===b||e[0](a,b)
compare: (a,b)=>a===b?0:e[0](a,b)
vs:
  zod: z.any()
operations:
  parse: identity
  decode: identity
  encode: identity
