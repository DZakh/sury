# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.string.with(S.length, 2).with(S.length, 2)
  input: string
  output: string
  instantiations: 5192
vs:
  zod: z.string().length(2).length(2)
jsonSchema:
  input: '{ type: "string", minLength: 2, maxLength: 2 }'
  output: '{ type: "string", minLength: 2, maxLength: 2 }'
operations:
  parse:
    expression: i=>{typeof i==="string"||e[1](i);i.length===2||e[0](i);return i}
    examples:
      at-the-length:
        input: '"ab"'
        output: '"ab"'
      too-short:
        input: '"a"'
        error: Expected string.length == 2, received "a"
      too-long:
        input: '"abc"'
        error: Expected string.length == 2, received "abc"
      empty:
        input: '""'
        error: Expected string.length == 2, received ""
  decode:
    expression: i=>{i.length===2||e[0](i);return i}
    examples:
      at-the-length:
        input: '"ab"'
        output: '"ab"'
      too-long:
        input: '"abc"'
        error: Expected string.length == 2, received "abc"
  encode:
    expression: i=>{i.length===2||e[0](i);return i}
    examples:
      at-the-length:
        input: '"ab"'
        output: '"ab"'
      too-long:
        input: '"abc"'
        error: Expected string.length == 2, received "abc"
