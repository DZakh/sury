# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.string.with(S.length, 2).with(S.length, 2, "MY MESSAGE")
  input: string
  output: string
  instantiations: 1085
jsonSchema:
  input: '{ type: "string", minLength: 2, maxLength: 2 }'
  output: '{ type: "string", minLength: 2, maxLength: 2 }'
isEqual: strictEqual
vs:
  zod: z.string().length(2).length(2, "MY MESSAGE")
operations:
  parse:
    expression: i=>{try{typeof i==="string"||e[1](i);i.length===2||e[0](i);return i}catch(v0){e[2](v0)}}
    examples:
      at-the-length:
        input: '"ab"'
        output: '"ab"'
      too-short:
        input: '"a"'
        error: MY MESSAGE
      too-long:
        input: '"abc"'
        error: MY MESSAGE
      empty:
        input: '""'
        error: MY MESSAGE
  decode:
    expression: i=>{try{i.length===2||e[0](i);return i}catch(v0){e[1](v0)}}
    examples:
      at-the-length:
        input: '"ab"'
        output: '"ab"'
      too-long:
        input: '"abc"'
        error: MY MESSAGE
  encode:
    expression: i=>{try{i.length===2||e[0](i);return i}catch(v0){e[1](v0)}}
    examples:
      at-the-length:
        input: '"ab"'
        output: '"ab"'
      too-long:
        input: '"abc"'
        error: MY MESSAGE
