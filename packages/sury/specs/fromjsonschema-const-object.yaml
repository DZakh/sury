# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: 'S.fromJSONSchema({ const: { kind: "point", coords: [1, 2] } })'
  input: '{ kind: "point"; coords: [1, 2]; }'
  output: '{ kind: "point"; coords: [1, 2]; }'
  instantiations: 839
vs:
  zod:
    _skip: not-applicable
jsonSchema:
  input: '{ const: { kind: "point", coords: [1, 2] } }'
  output: '{ const: { kind: "point", coords: [1, 2] } }'
operations:
  parse:
    expression: i=>{e[0](i);e[1](i)||e[2](i);return i}
    examples:
      deep-equal:
        input: '{ kind: "point", coords: [1, 2] }'
        output: '{ kind: "point", coords: [1, 2] }'
      extra-property-rejected:
        input: '{ kind: "point", coords: [1, 2], z: 3 }'
        error: Should equal the const property.
      different-value-rejected:
        input: '{ kind: "point", coords: [1, 3] }'
        error: Should equal the const property.
  decode:
    expression: i=>{e[0](i)||e[1](i);return i}
    examples:
      deep-equal:
        input: '{ kind: "point", coords: [1, 2] }'
        output: '{ kind: "point", coords: [1, 2] }'
      extra-property-rejected:
        input: '{ kind: "point", coords: [1, 2], z: 3 }'
        error: Should equal the const property.
      different-value-rejected:
        input: '{ kind: "point", coords: [1, 3] }'
        error: Should equal the const property.
  encode:
    expression: i=>{e[0](i)||e[1](i);return i}
    examples:
      deep-equal:
        input: '{ kind: "point", coords: [1, 2] }'
        output: '{ kind: "point", coords: [1, 2] }'
      extra-property-rejected:
        input: '{ kind: "point", coords: [1, 2], z: 3 }'
        error: Should equal the const property.
      different-value-rejected:
        input: '{ kind: "point", coords: [1, 3] }'
        error: Should equal the const property.
