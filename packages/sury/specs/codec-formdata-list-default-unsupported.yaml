# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: 'S.formData.with(S.to, S.schema({ a: S.optional(S.array(S.string), ["m"]) }))'
  input: FormData
  output: "{ a: string[]; }"
  instantiations: 5974
jsonSchema:
  input: Expected JSON, received FormData
  output: '{ type: "object", properties: { a: { items: { type: "string" }, type: "array" } }, required: ["a"] }'
isEqual:
  input: (a,b)=>a===b||e[0](a,b)
  output: (a,b)=>a===b||e[0](a.a,b.a)
compare:
  input: "throws: [Sury] Can't compare FormData. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
  output: "throws: [Sury] Can't compare { a: string[]; }. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Failed at a: Can't decode form field -> string[] with a default. No entries is the empty list, so the default is never read"
  decode: eq-to-parse
  encode:
    expression: i=>{try{let v0=i.a;if(Array.isArray(v0)){for(let v1=0;v1<v0.length;++v1){let v2=v0[v1];typeof v2==="string"||e[0](v2,["a",v1]);}}else{throw e[1](v0)}let v3=new e[2]();for(let v4=0;v4<v0.length;++v4){v3.append("a",v0[v4]);}return v3}catch(v5){e[3](v5)}}
    examples:
      built:
        input: '{ a: ["m"] }'
        output: ((f) => (f.append("a", "m"), f))(new FormData())
