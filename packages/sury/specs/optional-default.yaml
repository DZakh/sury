# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.string.with(S.optional, "foo")
  input: string | undefined
  output: string
  instantiations: 4437
jsonSchema:
  input: Expected JSON, received string | undefined
  output: '{ type: "string" }'
isEqual: strictEqual
vs:
  zod: z.string().optional().transform((value) => value ?? "foo")
operations:
  parse:
    expression: i=>{try{for(;;){if(typeof i==="string")break;if(i===void 0){i="foo";break}e[0](i)}return i}catch(v0){e[1](v0)}}
    examples:
      string:
        input: '"bar"'
        output: '"bar"'
      undefined-becomes-the-default:
        input: undefined
        output: '"foo"'
      null-is-not-accepted:
        input: "null"
        error: Expected string | undefined, received null
  decode: eq-to-parse
  encode:
    expression: i=>{try{typeof i==="string"||e[0](i);return i}catch(v0){e[1](v0)}}
    examples:
      string:
        input: '"foo"'
        output: '"foo"'
