# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.urlSearchParams
  input: URLSearchParams
  output: URLSearchParams
  instantiations: 267
jsonSchema:
  input: Expected JSON, received URLSearchParams
  output: Expected JSON, received URLSearchParams
isEqual: (a,b)=>a===b||e[0](a,b)
compare: "throws: [Sury] Can't compare URLSearchParams. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{i instanceof e[0]||e[1](i);return i}catch(v0){e[2](v0)}}
    examples:
      valid:
        input: new URLSearchParams()
        output: new URLSearchParams()
      string:
        input: '"q=1"'
        error: Expected URLSearchParams, received "q=1"
      object:
        input: "{}"
        error: Expected URLSearchParams, received {}
  decode: identity
  encode: identity
