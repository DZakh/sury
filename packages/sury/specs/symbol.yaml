# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.symbol
  input: symbol
  output: symbol
  instantiations: 254
jsonSchema:
  input: Expected JSON, received symbol
  output: Expected JSON, received symbol
isEqual: strictEqual
vs:
  zod: z.symbol()
operations:
  parse:
    expression: i=>{try{typeof i==="symbol"||e[0](i);return i}catch(v0){e[1](v0)}}
    examples:
      valid:
        input: Symbol.for("test")
        output: Symbol.for("test")
      invalid-string:
        input: '"symbol"'
        error: Expected symbol, received "symbol"
  decode: identity
  encode: identity
