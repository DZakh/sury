# yaml-language-server: $schema=./spec.schema.json
ts:
  # FIXME: Codec next expects: stay the same — rule 4 passes string through and
  # bridges undefined <-> null.
  schema: S.optional(S.string).with(S.to, S.nullable(S.string))
  input: string | undefined
  output: string | undefined
  instantiations: 5327
vs:
  zod:
    _skip: not-applicable
jsonSchema:
  input: Expected JSON, received string | undefined
  output: Expected JSON, received string | undefined
operations:
  parse:
    expression: i=>{if(i===void 0){try{i=void 0}catch(e1){e[0](i,e1)}}else if(!(typeof i==="string")){e[1](i)}return i}
    examples:
      string:
        input: '"abc"'
        output: '"abc"'
      undefined-bridges-to-null:
        input: undefined
        output: undefined
  decode: eq-to-parse
  encode:
    expression: i=>{if(i===void 0){let v0=null;try{v0=void 0}catch(e1){e[0](v0,e1)}i=v0}else if(!(typeof i==="string")){e[1](i)}return i}
    examples:
      string:
        input: '"abc"'
        output: '"abc"'
      null-bridges-to-undefined:
        input: "null"
        error: Expected string | undefined, received null
