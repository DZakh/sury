# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.optional(S.string).with(S.to, S.nullable(S.string))
  input: string | undefined
  output: string | null
  instantiations: 5542
jsonSchema:
  input: Expected JSON, received undefined | string
  output: '{ anyOf: [{ type: "null" }, { type: "string" }] }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{for(;;){if(i===void 0){i=null;break;}if(typeof i==="string")break;e[0](i)}return i}
    examples:
      string:
        input: '"abc"'
        output: '"abc"'
      undefined-bridges-to-null:
        input: undefined
        output: "null"
  decode: eq-to-parse
  encode:
    expression: i=>{for(;;){if(i===null){i=void 0;break;}if(typeof i==="string")break;e[0](i)}return i}
    examples:
      string:
        input: '"abc"'
        output: '"abc"'
      null-bridges-to-undefined:
        input: "null"
        output: undefined
