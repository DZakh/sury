# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.optional(S.string).with(S.to, S.nullable(S.boolean))
  input: string | undefined
  output: boolean | null
  instantiations: 5600
jsonSchema:
  input: Expected JSON, received undefined | string
  output: '{ anyOf: [{ type: "null" }, { type: "boolean" }] }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Invalid operation: can't convert undefined | string to null | boolean — string has no same-type variant on the other side. Use S.to to say what you mean, or S.never to mark a variant unreachable"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Invalid operation: can't convert null | boolean to undefined | string — boolean has no same-type variant on the other side. Use S.to to say what you mean, or S.never to mark a variant unreachable"
