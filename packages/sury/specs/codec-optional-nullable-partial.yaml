# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.optional(S.string).with(S.to, S.nullable(S.boolean))
  input: string | undefined
  output: boolean | null
  instantiations: 5458
vs:
  zod:
    _skip: not-applicable
jsonSchema:
  input: Expected JSON, received string | undefined
  fromInputType: Expected JSON, received string | undefined
  output: '{ anyOf: [{ type: "boolean" }, { type: "null" }] }'
operations:
  parse:
    creationError: "SuryError: Invalid operation: can't convert string | undefined to boolean | null — string has no same-type variant on the other side. Use S.to to say what you mean, or S.never to mark a variant unreachable"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Invalid operation: can't convert boolean | null to string | undefined — boolean has no same-type variant on the other side. Use S.to to say what you mean, or S.never to mark a variant unreachable"
