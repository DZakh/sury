# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.optional(S.string).with(S.to, S.nullable(S.boolean))
  input: string | undefined
  output: boolean | null
  instantiations: 5390
vs:
  zod:
    _skip: not-applicable
jsonSchema:
  input: Expected JSON, received string | undefined
  output: '{ anyOf: [{ type: "boolean" }, { type: "null" }] }'
operations:
  parse:
    creationError: "SuryError: Can't decode string | undefined to boolean | null: string has no variant of the same type on the other side. Use S.to on it, or S.never to make it unreachable"
  decode: eq-to-parse
  encode: eq-to-parse
