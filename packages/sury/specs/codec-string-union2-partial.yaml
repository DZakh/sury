# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.string.with(S.to, S.union([S.number, S.string]))
  input: string
  output: string | number
  instantiations: 5373
jsonSchema:
  input: '{ type: "string" }'
  output: '{ anyOf: [{ type: "number" }, { type: "string" }] }'
isEqual: strictEqual
compare:
  input: strictCompare
  output: (a,b)=>a===b?0:(typeof a==="number"&&a==a?typeof b==="number"&&b==b?a<b?-1:a>b?1:0:-1:typeof b==="number"&&b==b?1:a<b?-1:a>b?1:0)
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Ambiguous string -> number | string. Should number be decoded or ignored? Choose with S.to for string -> number, or S.never -> number"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Ambiguous number | string -> string. Should number be decoded or ignored? Choose with S.to for number -> string, or S.never -> number"
