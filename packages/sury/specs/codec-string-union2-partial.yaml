# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.string.with(S.to, S.union([S.number, S.string]))
  input: string
  output: string | number
  instantiations: 6059
vs:
  zod:
    _skip: not-applicable
jsonSchema:
  input: '{ type: "string" }'
  output: '{ anyOf: [{ type: "number" }, { type: "string" }] }'
operations:
  parse:
    creationError: "SuryError: Can't decode string to number | string: string matches it, number doesn't. Use S.to on it, or S.never to make it unreachable"
  decode: eq-to-parse
  encode: eq-to-parse
