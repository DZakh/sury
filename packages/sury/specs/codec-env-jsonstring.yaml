# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: "S.record(S.env).with(S.to, S.schema({ FLAGS: S.jsonString.with(S.to, S.schema({ debug: S.boolean })) }))"
  input: "{ [x: string]: string | undefined; }"
  output: "{ FLAGS: { debug: boolean; }; }"
  instantiations: 6675
jsonSchema:
  input: '{ type: "object", additionalProperties: { type: "string" } }'
  fromInputType: "{ [key: string]: string; }"
  output: '{ type: "object", properties: { FLAGS: { type: "object", properties: { debug: { type: "boolean" } }, required: ["debug"] } }, required: ["FLAGS"] }'
isEqual:
  input: (a,b)=>{if(a===b)return true;let n=0;for(const k in a){if(!(k in b)||!(a[k]===b[k]))return false;n++}for(const k in b)n--;return !n}
  output: (a,b)=>a===b||a.FLAGS.debug===b.FLAGS.debug
compare:
  input: "throws: [Sury] Can't compare { [key: string]: env; }. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
  output: "throws: [Sury] Can't compare { FLAGS: { debug: boolean; }; }. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{typeof i==="object"&&i&&!Array.isArray(i)||e[5](i);for(let v0 in i){let v1=i[v0];(typeof v1==="string"||v1===void 0)||e[0](v1,[v0]);}let v2=i.FLAGS;typeof v2==="string"||e[4](v2);let v3;try{v3=JSON.parse(v2)}catch(t){e[1](v2)}typeof v3==="object"&&v3&&!Array.isArray(v3)||e[3](v3);let v4=v3.debug;typeof v4==="boolean"||e[2](v4);v2={debug:v4};return {FLAGS:v2}}catch(v5){e[6](v5)}}
    examples:
      nested-document:
        input: '{ FLAGS: "{\"debug\":true}" }'
        output: "{ FLAGS: { debug: true } }"
      not-json:
        input: '{ FLAGS: "nope" }'
        error: 'Failed at FLAGS: Expected JSON string, received "nope"'
      missing:
        input: "{}"
        error: "Failed at FLAGS: Expected JSON string, received undefined"
  decode:
    expression: i=>{try{let v1=i.FLAGS;typeof v1==="string"||e[3](v1);let v2;try{v2=JSON.parse(v1)}catch(t){e[0](v1)}typeof v2==="object"&&v2&&!Array.isArray(v2)||e[2](v2);let v3=v2.debug;typeof v3==="boolean"||e[1](v3);v1={debug:v3};return {FLAGS:v1}}catch(v4){e[4](v4)}}
    examples:
      nested-document:
        input: '{ FLAGS: "{\"debug\":true}" }'
        output: "{ FLAGS: { debug: true } }"
  encode:
    expression: i=>{let v0=i.FLAGS;return {FLAGS:"{\"debug\":"+v0.debug+"}"}}
    examples:
      built:
        input: "{ FLAGS: { debug: true } }"
        output: '{ FLAGS: "{\"debug\":true}" }'
