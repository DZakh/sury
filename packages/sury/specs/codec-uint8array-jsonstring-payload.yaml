# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: "S.uint8Array.with(S.to, S.jsonString.with(S.to, S.schema({ id: S.string })))"
  input: Uint8Array<ArrayBufferLike>
  output: "{ id: string; }"
  instantiations: 6660
jsonSchema:
  input: Expected JSON, received Uint8Array
  output: '{ type: "object", properties: { id: { type: "string" } }, required: ["id"] }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{i instanceof e[4]||e[5](i);let v1=e[0].decode(i);let v0;try{v0=JSON.parse(v1)}catch(t){e[1](v1)}typeof v0==="object"&&v0&&!Array.isArray(v0)||e[3](v0);let v2=v0["id"];typeof v2==="string"||e[2](v2);return {"id":v2,}}
    examples:
      utf8-document:
        input: new TextEncoder().encode('{"id":"a"}')
        output: '{ id: "a" }'
      non-ascii-value:
        input: new TextEncoder().encode('{"id":"\u20ac"}')
        output: '{ id: "€" }'
      not-json:
        input: new Uint8Array([137, 80, 78, 71])
        error: Expected JSON string, received "�PNG"
  decode:
    expression: i=>{let v1=e[0].decode(i);let v0;try{v0=JSON.parse(v1)}catch(t){e[1](v1)}typeof v0==="object"&&v0&&!Array.isArray(v0)||e[3](v0);let v2=v0["id"];typeof v2==="string"||e[2](v2);return {"id":v2,}}
    examples:
      utf8-document:
        input: new TextEncoder().encode('{"id":"a"}')
        output: '{ id: "a" }'
  encode:
    expression: i=>{return e[1].encode("{\"id\":"+e[0](i["id"])+"}")}
    examples:
      non-ascii-value:
        input: '{ id: "€" }'
        error: cannot represent a Uint8Array instance as spec source code
