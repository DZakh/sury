# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.set(S.string).with(S.to, S.array(S.string))
  input: Set<string>
  output: string[]
  instantiations: 5536
jsonSchema:
  input: Expected JSON, received Set<string>
  output: '{ items: { type: "string" }, type: "array" }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{i instanceof e[1]||e[2](i);let v1=0;for(let v0 of i){try{typeof v0==="string"||e[0](v0);v1++;}catch(v2){v2.path=[v1,...v2.path];throw v2}}let v3=Array.from(i);return v3}
    examples:
      valid:
        input: new Set(["a", "b"])
        output: '["a", "b"]'
      invalid-item:
        input: new Set(["a", 2])
        error: "Failed at [1]: Expected string, received 2"
      not-a-set:
        input: '["a"]'
        error: Expected Set<string>, received ["a"]
  decode:
    expression: i=>{let v2=Array.from(i);return v2}
    examples:
      valid:
        input: new Set(["a"])
        output: '["a"]'
  encode:
    expression: i=>{return new Set(i)}
    examples:
      valid:
        input: '["a", "b"]'
        output: new Set(["a", "b"])
