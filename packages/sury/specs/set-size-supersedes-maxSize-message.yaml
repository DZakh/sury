# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.instance(Set).with(S.maxSize, 5, "MAX").with(S.size, 3)
  input: Set<unknown>
  output: Set<unknown>
  instantiations: 2896
jsonSchema:
  input: Expected JSON, received Set.size == 3
  output: Expected JSON, received Set.size == 3
isEqual: (a,b)=>a===b||e[0](a,b)
compare: "throws: [Sury] Can't compare Set.size == 3. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{i instanceof e[1]||e[2](i);i.size===3||e[0](i);return i}catch(v0){e[3](v0)}}
    examples:
      at-the-size:
        input: new Set([1, 2, 3])
        output: new Set([1, 2, 3])
      too-small:
        input: new Set([1, 2])
        error: Expected Set.size == 3, received Set
      too-big-within-the-superseded-bound:
        input: new Set([1, 2, 3, 4])
        error: Expected Set.size == 3, received Set
      too-big-past-the-superseded-bound:
        input: new Set([1, 2, 3, 4, 5, 6])
        error: Expected Set.size == 3, received Set
      wrong-type:
        input: "null"
        error: Expected Set.size == 3, received null
  decode:
    expression: i=>{try{i.size===3||e[0](i);return i}catch(v0){e[1](v0)}}
    examples:
      at-the-size:
        input: new Set([1, 2, 3])
        output: new Set([1, 2, 3])
      too-big-within-the-superseded-bound:
        input: new Set([1, 2, 3, 4])
        error: Expected Set.size == 3, received Set
      too-big-past-the-superseded-bound:
        input: new Set([1, 2, 3, 4, 5, 6])
        error: Expected Set.size == 3, received Set
  encode:
    expression: i=>{try{i.size===3||e[0](i);return i}catch(v0){e[1](v0)}}
    examples:
      at-the-size:
        input: new Set([1, 2, 3])
        output: new Set([1, 2, 3])
      too-big-within-the-superseded-bound:
        input: new Set([1, 2, 3, 4])
        error: Expected Set.size == 3, received Set
      too-big-past-the-superseded-bound:
        input: new Set([1, 2, 3, 4, 5, 6])
        error: Expected Set.size == 3, received Set
