# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: "S.json.with(S.to, S.schema({ payload: S.uint8Array }))"
  input: JSON
  output: "{ payload: Uint8Array<ArrayBufferLike>; }"
  instantiations: 5446
jsonSchema:
  input: '{ type: "object", properties: { payload: { type: "string", contentEncoding: "base64" } }, additionalProperties: false, required: ["payload"] }'
  fromInputType: "{ payload: string; }"
  output: "Failed at payload: Expected JSON, received Uint8Array"
  openapi-3.0:
    input: '{ type: "object", properties: { payload: { type: "string", format: "byte" } }, additionalProperties: false, required: ["payload"] }'
isEqual:
  input: (a,b)=>a===b||e[0](a,b)
  output: (a,b)=>a===b||e[0](a.payload,b.payload)
compare:
  input: "throws: [Sury] Can't compare JSON. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
  output: "throws: [Sury] Can't compare { payload: Uint8Array; }. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{typeof i==="object"&&i&&!Array.isArray(i)||e[4](i);let v0=i.payload;typeof v0==="string"||e[3](v0);e[1](v0)||e[2](v0);return {payload:e[0](v0)}}catch(v1){e[5](v1)}}
    examples:
      base64-value:
        input: '{ payload: "iVBORw==" }'
        output: "{ payload: new Uint8Array([137, 80, 78, 71]) }"
      not-base64:
        input: '{ payload: "!!" }'
        error: 'Failed at payload: Expected base64, received "!!"'
      missing-payload:
        input: "{}"
        error: "Failed at payload: Expected base64, received undefined"
  decode: eq-to-parse
  encode:
    expression: i=>{try{let v0=i.payload;let v1=e[0](v0);return {payload:v1}}catch(v2){e[1](v2)}}
    examples:
      base64-value:
        input: "{ payload: new Uint8Array([137, 80, 78, 71]) }"
        output: '{ payload: "iVBORw==" }'
      empty:
        input: "{ payload: new Uint8Array([]) }"
        output: '{ payload: "" }'
