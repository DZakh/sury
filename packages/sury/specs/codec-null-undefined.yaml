# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.schema(null).with(S.to, S.schema(undefined))
  input: "null"
  output: undefined
  instantiations: 4837
jsonSchema:
  input: '{ type: "null" }'
  output: Expected JSON, received undefined
  openapi-3.0:
    input: "{ enum: [null] }"
isEqual: alwaysEqual
compare: alwaysCompare
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{i===null||e[0](i);return void 0}catch(v0){e[1](v0)}}
    examples:
      valid:
        input: "null"
        output: undefined
      invalid:
        input: '"abc"'
        error: Expected null, received "abc"
  decode: eq-to-parse
  encode:
    expression: i=>{try{i===void 0||e[0](i);return null}catch(v0){e[1](v0)}}
    examples:
      valid:
        input: undefined
        output: "null"
