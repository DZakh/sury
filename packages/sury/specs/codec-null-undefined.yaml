# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.schema(null).with(S.to, S.schema(undefined))
  input: "null"
  output: undefined
  instantiations: 5509
vs:
  zod:
    _skip: not-applicable
jsonSchema:
  input: '{ type: "null" }'
  output: Expected JSON, received undefined
operations:
  parse:
    expression: i=>{try{i===null||e[0](i);return void 0}catch(x){throw e[1](x,i)}}
    examples:
      valid:
        input: "null"
        output: undefined
      invalid:
        input: '"abc"'
        error: Expected null, received "abc"
  decode: eq-to-parse
  encode:
    expression: i=>{try{i===void 0||e[0](i);return null}catch(x){throw e[1](x,i)}}
    examples:
      valid:
        input: undefined
        output: "null"
