# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.uint8Array.with(S.to, S.json)
  input: Uint8Array<ArrayBufferLike>
  output: JSON
  instantiations: 4419
jsonSchema:
  input: Expected JSON, received Uint8Array
  output: "{}"
isEqual:
  input: (a,b)=>{if(a===b)return true;let n=a.length;if(n!==b.length)return false;for(let i=0;i<n;i++)if(!(a[i]===b[i]))return false;return true}
  output: (a,b)=>a===b||e[0](a,b)
compare:
  input: (a,b)=>{if(a===b)return 0;let n=a.length,m=b.length,l=n<m?n:m,c;for(let i=0;i<l;i++)if(c=a[i]<b[i]?-1:a[i]>b[i]?1:0)return c;return n<m?-1:n>m?1:0}
  output: (a,b)=>a===b?0:e[0](a,b)
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Can't decode Uint8Array -> JSON. Define custom codec with S.to"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Can't decode JSON -> Uint8Array. Define custom codec with S.to"
