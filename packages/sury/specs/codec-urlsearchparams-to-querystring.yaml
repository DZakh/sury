# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.urlSearchParams.with(S.to, S.queryString)
  input: URLSearchParams
  output: string
  instantiations: 4432
jsonSchema:
  input: Expected JSON, received URLSearchParams
  output: '{ type: "string" }'
isEqual:
  input: (a,b)=>a===b||e[0](a,b)
  output: strictEqual
compare:
  input: "throws: [Sury] Can't compare URLSearchParams. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
  output: strictCompare
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{i instanceof e[0]||e[1](i);return i.toString()}catch(v0){e[2](v0)}}
    examples:
      valid:
        input: ((p) => (p.append("q", "hi"), p.append("page", "2"), p))(new URLSearchParams())
        output: '"q=hi&page=2"'
      empty:
        input: new URLSearchParams()
        output: '""'
  decode:
    expression: i=>{return i.toString()}
    examples:
      valid:
        input: ((p) => (p.append("q", "hi"), p.append("page", "2"), p))(new URLSearchParams())
        output: '"q=hi&page=2"'
  encode:
    expression: i=>{try{return new e[0](i)}catch(v0){e[1](v0)}}
    examples:
      valid:
        input: '"q=hi&page=2"'
        output: ((p) => (p.append("q", "hi"), p.append("page", "2"), p))(new URLSearchParams())
      empty:
        input: '""'
        output: new URLSearchParams()
