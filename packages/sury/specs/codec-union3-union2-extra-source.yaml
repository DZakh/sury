# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.string, S.number, S.boolean]).with(S.to, S.union([S.number, S.string]))
  input: string | number | boolean
  output: string | number
  instantiations: 5850
jsonSchema:
  input: '{ anyOf: [{ type: "string" }, { type: "number" }, { type: "boolean" }] }'
  output: '{ anyOf: [{ type: "number" }, { type: "string" }] }'
isEqual: strictEqual
compare:
  input: (a,b)=>a===b?0:(typeof a==="string"?typeof b==="string"?a<b?-1:a>b?1:0:-1:typeof b==="string"?1:typeof a==="number"&&a==a?typeof b==="number"&&b==b?a<b?-1:a>b?1:0:-1:typeof b==="number"&&b==b?1:a<b?-1:a>b?1:0)
  output: (a,b)=>a===b?0:(typeof a==="number"&&a==a?typeof b==="number"&&b==b?a<b?-1:a>b?1:0:-1:typeof b==="number"&&b==b?1:a<b?-1:a>b?1:0)
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Ambiguous string | number | boolean -> number | string. Should boolean be decoded or ignored? Choose with S.to for boolean -> number, or S.never -> boolean"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Ambiguous number | string -> string | number | boolean. Should boolean be decoded or ignored? Choose with S.to for number -> boolean, or S.never -> boolean"
