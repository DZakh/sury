# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.string.with(S.to, S.number, (value) => Number(value)), S.number])
  input: string | number
  output: number
  instantiations: 5227
jsonSchema:
  input: '{ anyOf: [{ type: "string" }, { type: "number" }] }'
  output: '{ type: "number" }'
isEqual: strictEqual
compare:
  input: "throws: [Sury] Can't compare string | number. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
  output: strictCompare
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{for(;;){if(typeof i==="string"){let v0;try{v0=e[0](i)}catch(x){e[1](x)}typeof v0==="number"&&v0==v0||e[2](v0);i=v0;break}if(typeof i==="number"&&i==i)break;throw e[3](i)}return i}catch(v1){e[4](v1)}}
    examples:
      custom-arm:
        input: '"12"'
        output: "12"
      sibling-arm:
        input: "5"
        output: "5"
  decode: eq-to-parse
  encode:
    creationError: 'SuryError: Ambiguous encode for number -> string. Only decode is defined. Add encode to S.to as a function, "auto" or "never"'
