# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.env.with(S.to, S.port).with(S.to, S.optional(S.port))
  input: string | undefined
  output: number | undefined
  instantiations: 4767
jsonSchema:
  input: '{ type: "string" }'
  fromInputType: string
  output: Expected JSON, received port | undefined
isEqual: strictEqual
vs:
  zod:
    _skip: not-applicable
operations:
  # FIXME: the link and the arm each check the range - `S.env.with(S.to,
  # S.optional(S.port))` spells the same read with one.
  parse:
    expression: i=>{(typeof i==="string"||i===void 0)||e[3](i);i=i||void 0;if(i){let v0=+i;v0==v0&&(v0||i.trim())||e[2](i);v0>=0&&v0<65536&&v0%1==0||e[1](v0);v0>=0&&v0<65536&&v0%1==0||e[0](v0);i=v0;}return i}
    examples:
      valid:
        input: '"8080"'
        output: "8080"
      blank:
        input: '""'
        output: undefined
      unset:
        input: undefined
        output: undefined
      too-big:
        input: '"70000"'
        error: Expected port, received 70000
  decode:
    expression: i=>{i=i||void 0;if(i){let v0=+i;v0==v0&&(v0||i.trim())||e[2](i);v0>=0&&v0<65536&&v0%1==0||e[1](v0);v0>=0&&v0<65536&&v0%1==0||e[0](v0);i=v0;}return i}
    examples:
      valid:
        input: '"8080"'
        output: "8080"
      unset:
        input: undefined
        output: undefined
  encode:
    expression: i=>{for(;;){if(typeof i==="number"&&i==i){i>=0&&i<65536&&i%1==0||e[1](i);i>=0&&i<65536&&i%1==0||e[0](i);i=""+i;break}if(i===void 0)break;e[2](i)}return i}
    examples:
      valid:
        input: "8080"
        output: '"8080"'
      unset:
        input: undefined
        output: undefined
