# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.string.with(S.length, 0)
  input: '""'
  output: '""'
  instantiations: 845
jsonSchema:
  input: '{ type: "string", minLength: 0, maxLength: 0 }'
  output: '{ type: "string", minLength: 0, maxLength: 0 }'
isEqual: strictEqual
compare: strictCompare
vs:
  zod:
    schema: z.string().length(0)
    divergence: '`S.empty` pins the Sury type to exactly `""` where Zod keeps the unbounded `string`.'
    input: string
    output: string
operations:
  parse:
    expression: i=>{try{typeof i==="string"||e[1](i);i.length===0||e[0](i);return i}catch(v0){e[2](v0)}}
    examples:
      at-the-length:
        input: '"ab"'
        error: Expected string.length == 0, received "ab"
      longer:
        input: '"abc"'
        error: Expected string.length == 0, received "abc"
      shorter:
        input: '"a"'
        error: Expected string.length == 0, received "a"
      empty:
        input: '""'
        output: '""'
      single-space:
        input: '" "'
        error: Expected string.length == 0, received " "
      wrong-type:
        input: "1"
        error: Expected string.length == 0, received 1
  decode:
    expression: i=>{try{i.length===0||e[0](i);return i}catch(v0){e[1](v0)}}
    examples:
      at-the-length:
        input: '"ab"'
        error: Expected string.length == 0, received "ab"
      shorter:
        input: '"a"'
        error: Expected string.length == 0, received "a"
      empty:
        input: '""'
        output: '""'
  encode:
    expression: i=>{try{i.length===0||e[0](i);return i}catch(v0){e[1](v0)}}
    examples:
      at-the-length:
        input: '"ab"'
        error: Expected string.length == 0, received "ab"
      shorter:
        input: '"a"'
        error: Expected string.length == 0, received "a"
      empty:
        input: '""'
        output: '""'
