# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.instance(Set)
  input: Set<unknown>
  output: Set<unknown>
  instantiations: 2253
jsonSchema:
  input: Expected JSON, received Set
  output: Expected JSON, received Set
isEqual: (a,b)=>a===b||e[0](a,b)
compare: "throws: [Sury] Can't compare Set. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{i instanceof e[0]||e[1](i);return i}catch(v0){e[2](v0)}}
    examples:
      valid:
        input: new Set(["foo", "bar"])
        output: new Set(["foo", "bar"])
      same-size-other-members:
        input: new Set(["foo", "baz"])
        output: new Set(["foo", "baz"])
      not-a-set:
        input: "123"
        error: Expected Set, received 123
      array-is-not-a-set:
        input: '["foo"]'
        error: Expected Set, received ["foo"]
  decode: identity
  encode: identity
