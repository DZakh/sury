# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.unknown
  input: unknown
  output: unknown
  instantiations: 254
jsonSchema:
  # FIXME: should be {} (any value), same as S.toInputJSONSchemaOrThrow(S.json) - see any.yaml.
  input: "{}"
  fromInputType: JSON
  output: "{}"
  fromOutputType: JSON
isEqual: (a,b)=>a===b||e[0](a,b)
compare: "throws: [Sury] Can't compare unknown. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
vs:
  zod: z.unknown()
operations:
  parse: identity
  decode: identity
  encode: identity
