# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.nullish(S.string)
  input: string | null | undefined
  output: string | null | undefined
  instantiations: 419
jsonSchema:
  input: Expected JSON, received undefined | null | string
  output: Expected JSON, received undefined | null | string
vs:
  zod: z.string().nullish()
operations:
  parse:
    expression: i=>{(i===void 0||i===null||typeof i==="string")||e[0](i);return i}
    examples:
      string:
        input: '"foo"'
        output: '"foo"'
      undefined:
        input: undefined
        output: undefined
      "null":
        input: "null"
        output: "null"
      invalid-type:
        input: "1"
        error: Expected undefined | null | string, received 1
  decode: identity
  encode: identity
