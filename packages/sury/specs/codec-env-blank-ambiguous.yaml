# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: "S.record(S.env).with(S.to, S.schema({ a: S.string, n: S.number.with(S.gte, 18) }))"
  input: "{ [x: string]: string | undefined; }"
  output: "{ a: string; n: number; }"
  instantiations: 6295
jsonSchema:
  input: '{ type: "object", additionalProperties: { type: "string" } }'
  fromInputType: "{ [key: string]: string; }"
  output: '{ type: "object", properties: { a: { type: "string" }, n: { type: "number", minimum: 18 } }, required: ["a", "n"] }'
isEqual:
  input: (a,b)=>{if(a===b)return true;let n=0;for(const k in a){if(!(k in b)||!(a[k]===b[k]))return false;n++}for(const k in b)n--;return !n}
  output: (a,b)=>a===b||a.a===b.a&&a.n===b.n
compare:
  input: (a,b)=>{if(a===b)return 0;let ka=Object.keys(a).sort(),kb=Object.keys(b).sort(),n=ka.length,m=kb.length,l=n<m?n:m,c;for(let i=0;i<l;i++){if(ka[i]!==kb[i])return ka[i]<kb[i]?-1:1;if(c=a[ka[i]]===void 0?b[kb[i]]===void 0?0:-1:b[kb[i]]===void 0?1:a[ka[i]]<b[kb[i]]?-1:a[ka[i]]>b[kb[i]]?1:0)return c}return n<m?-1:n>m?1:0}
  output: (a,b)=>a===b?0:a.a<b.a?-1:a.a>b.a?1:a.n<b.n?-1:a.n>b.n?1:0
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: 'SuryError: Failed at a: Ambiguous "" for string. Should a blank input be rejected, kept, or read as absent? Choose with S.nonEmpty, S.minLength(0), or S.optional'
  decode: eq-to-parse
  encode:
    expression: i=>{let v0=i.n;v0>=18||e[0](v0);return {a:i.a,n:""+v0}}
    examples:
      too-young:
        input: '{ a: "x", n: 7 }'
        error: "Failed at n: Expected number >= 18, received 7"
