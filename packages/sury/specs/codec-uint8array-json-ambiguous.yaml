# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.uint8Array.with(S.to, S.json)
  input: Uint8Array<ArrayBufferLike>
  output: JSON
  instantiations: 5233
jsonSchema:
  input: Expected JSON, received Uint8Array
  output: "{}"
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: 'SuryError: Ambiguous conversion from Uint8Array to JSON. Use S.to(from, to, {decode: "unpack" | "pack", encode: ...})'
  decode: eq-to-parse
  encode: eq-to-parse
