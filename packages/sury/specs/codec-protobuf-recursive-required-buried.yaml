# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: 'S.recursive("Root", () => { const M = S.recursive("M", (m) => S.schema({ x: S.recursive("N", () => S.schema({ y: m.with(S.protobufField, 1) })).with(S.protobufField, 1) })); return S.schema({ a: S.optional(M).with(S.protobufField, 1) }) }).with(S.to, S.protobuf)'
  input: "{ a?: unknown; }"
  output: Uint8Array<ArrayBufferLike>
  instantiations: 11454
jsonSchema:
  input: '[Sury] S.protobuf: field "y" makes "M" hold itself with no way to end. Make it optional or repeated'
  output: '[Sury] S.protobuf: field "y" makes "M" hold itself with no way to end. Make it optional or repeated'
isEqual:
  input: (a,b)=>a===b||e[0](a,b)
  output: (a,b)=>{if(a===b)return true;let n=a.length;if(n!==b.length)return false;for(let i=0;i<n;i++)if(!(a[i]===b[i]))return false;return true}
vs:
  zod:
    _skip: todo(#…)
operations:
  parse:
    creationError: 'Error: [Sury] S.protobuf: field "y" makes "M" hold itself with no way to end. Make it optional or repeated'
  decode: eq-to-parse
  encode: eq-to-parse
