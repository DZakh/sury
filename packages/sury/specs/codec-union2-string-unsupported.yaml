# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.boolean, S.symbol]).with(S.to, S.string)
  input: boolean | symbol
  output: string
  instantiations: 5137
jsonSchema:
  input: Expected JSON, received boolean | symbol
  output: '{ type: "string" }'
isEqual: strictEqual
compare:
  input: "throws: [Sury] Can't compare boolean | symbol. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
  output: strictCompare
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Can't decode symbol -> string. Define custom codec with S.to"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Can't decode string -> symbol. Define custom codec with S.to"
