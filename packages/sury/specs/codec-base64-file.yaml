# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.base64.with(S.to, S.file)
  input: string
  output: File
  instantiations: 4431
jsonSchema:
  input: '{ type: "string", contentEncoding: "base64", contentMediaType: "application/octet-stream" }'
  output: Expected JSON, received File
  openapi-3.0:
    input: '{ type: "string", format: "byte" }'
isEqual: strictEqual
compare:
  input: strictCompare
  output: "throws: [Sury] Can't compare File. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{typeof i==="string"||e[4](i);e[2](i)||e[3](i);return new e[1]([e[0](i)],"")}catch(v0){e[5](v0)}}
    examples:
      the-bytes:
        input: '"aGk="'
        output: new File(["hi"], "")
      not-base64:
        input: '"!!"'
        error: Expected base64, received "!!"
  decode:
    expression: i=>{try{e[2](i)||e[3](i);return new e[1]([e[0](i)],"")}catch(v0){e[4](v0)}}
    examples:
      the-bytes:
        input: '"aGk="'
        output: new File(["hi"], "")
      not-base64:
        input: '"!!"'
        error: Expected base64, received "!!"
  encode:
    isAsync: true
    expression: i=>{try{let v0;try{v0=i.arrayBuffer().then(e[0]).catch(e[1])}catch(x){e[1](x)}return v0.catch(e[2])}catch(v1){return e[3](v1)}}
    examples:
      stores-the-bytes:
        input: new File([new Uint8Array([137, 80, 78, 71])], "a.png")
        output: '"iVBORw=="'
