# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: 'S.fromJSONSchemaOrThrow({ type: "object", required: ["a"] })'
  input: "{ [key: string]: JSON; } & { a: JSON; }"
  output: "{ [key: string]: JSON; } & { a: JSON; }"
  instantiations: 1165
jsonSchema:
  input: '{ type: "object", required: ["a"] }'
  output: '{ type: "object", required: ["a"] }'
isEqual: (a,b)=>{if(a===b)return true;let n=0;for(const k in a){if(!(k in b)||!(e[0](a[k],b[k])))return false;n++}for(const k in b)n--;return !n}
compare: "throws: [Sury] Can't compare { [key: string]: JSON; }. Only primitives, Date, URL and tuples of them are orderable. Use isEqual for equality"
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{typeof i==="object"&&i&&!Array.isArray(i)||e[2](i);e[1](i)||e[3](i);for(let v0 in i){try{e[0](i[v0]);}catch(v1){v1.path=[v0,...v1.path];throw v1}}return i}catch(v2){e[4](v2)}}
    examples:
      required-key-present:
        input: "{ a: 1 }"
        output: "{ a: 1 }"
      required-key-absent:
        input: "{}"
        error: Should contain every required property.
      other-keys-pass-through:
        input: '{ a: 1, b: "x" }'
        output: '{ a: 1, b: "x" }'
  decode:
    expression: i=>{try{e[0](i)||e[1](i);return i}catch(v1){e[2](v1)}}
    examples:
      required-key-present:
        input: "{ a: 1 }"
        output: "{ a: 1 }"
      required-key-absent:
        input: "{}"
        error: Should contain every required property.
  encode:
    expression: i=>{try{e[0](i)||e[1](i);return i}catch(v1){e[2](v1)}}
    examples:
      required-key-present:
        input: "{ a: 1 }"
        output: "{ a: 1 }"
      required-key-absent:
        input: "{}"
        error: Should contain every required property.
