# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: 'S.fromJSONSchemaOrThrow({ type: "object", required: ["a"] })'
  input: "{ [key: string]: JSON; } & { a: JSON; }"
  output: "{ [key: string]: JSON; } & { a: JSON; }"
  instantiations: 1165
jsonSchema:
  input: '{ type: "object", required: ["a"] }'
  output: '{ type: "object", required: ["a"] }'
isEqual: (a,b)=>{if(a===b)return true;let n=0;for(const k in a){if(!(k in b)||!(e[0](a[k],b[k])))return false;n++}for(const k in b)n--;return !n}
compare: (a,b)=>{if(a===b)return 0;let ka=Object.keys(a).sort(),kb=Object.keys(b).sort(),n=ka.length,m=kb.length,l=n<m?n:m,c;for(let i=0;i<l;i++){if(ka[i]!==kb[i])return ka[i]<kb[i]?-1:1;if(c=e[0](a[ka[i]],b[kb[i]]))return c}return n<m?-1:n>m?1:0}
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{typeof i==="object"&&i&&!Array.isArray(i)||e[2](i);e[1](i)||e[3](i);for(let v0 in i){try{e[0](i[v0]);}catch(v1){v1.path=[v0,...v1.path];throw v1}}return i}
    examples:
      required-key-present:
        input: "{ a: 1 }"
        output: "{ a: 1 }"
      required-key-absent:
        input: "{}"
        error: Should contain every required property.
      other-keys-pass-through:
        input: '{ a: 1, b: "x" }'
        output: '{ a: 1, b: "x" }'
  decode:
    expression: i=>{e[0](i)||e[1](i);return i}
    examples:
      required-key-present:
        input: "{ a: 1 }"
        output: "{ a: 1 }"
      required-key-absent:
        input: "{}"
        error: Should contain every required property.
  encode:
    expression: i=>{e[0](i)||e[1](i);return i}
    examples:
      required-key-present:
        input: "{ a: 1 }"
        output: "{ a: 1 }"
      required-key-absent:
        input: "{}"
        error: Should contain every required property.
