# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: 'S.schema({ map: S.record(S.int32).with(S.protobufField, {number: 1, type: "message"}) }).with(S.to, S.protobuf)'
  input: "{ map: Record<string, number>; }"
  output: Uint8Array<ArrayBufferLike>
  instantiations: 5958
jsonSchema:
  input: '{ type: "object", properties: { map: { type: "object", additionalProperties: { type: "integer", minimum: -2147483648, maximum: 2147483647 } } }, required: ["map"] }'
  output: '[Sury] S.protobuf: field "map" is a message but its schema is not an object'
isEqual:
  input: (a,b)=>a===b||e[0](a.map,b.map)
  output: (a,b)=>{if(a===b)return true;let n=a.length;if(n!==b.length)return false;for(let i=0;i<n;i++)if(!(a[i]===b[i]))return false;return true}
compare:
  input: (a,b)=>a===b?0:e[0](a.map,b.map)
  output: (a,b)=>{if(a===b)return 0;let n=a.length,m=b.length,l=n<m?n:m,c;for(let i=0;i<l;i++)if(c=a[i]<b[i]?-1:a[i]>b[i]?1:0)return c;return n<m?-1:n>m?1:0}
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: 'Error: [Sury] S.protobuf: field "map" is a message but its schema is not an object'
  decode: eq-to-parse
  encode: eq-to-parse
