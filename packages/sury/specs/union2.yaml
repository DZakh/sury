# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.string, S.number])
  aliases:
    - S.anyOf([S.string, S.number])
  input: string | number
  output: string | number
  instantiations: 972
jsonSchema:
  input: '{ anyOf: [{ type: "string" }, { type: "number" }] }'
  output: '{ anyOf: [{ type: "string" }, { type: "number" }] }'
isEqual: strictEqual
compare: (a,b)=>a===b?0:(typeof a==="string"?typeof b==="string"?a<b?-1:a>b?1:0:-1:typeof b==="string"?1:a<b?-1:a>b?1:0)
vs:
  zod: z.union([z.string(), z.number()])
operations:
  parse:
    expression: i=>{(typeof i==="string"||typeof i==="number"&&i==i)||e[0](i);return i}
    examples:
      valid-string:
        input: '"hello"'
        output: '"hello"'
      valid-number:
        input: "42"
        output: "42"
      invalid:
        input: "true"
        error: Expected string | number, received true
  decode: identity
  encode: identity
