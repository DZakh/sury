# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.env.with(S.to, S.email)
  input: string | undefined
  output: string
  instantiations: 4419
jsonSchema:
  input: '{ type: "string", format: "email" }'
  fromInputType: string
  output: '{ type: "string", format: "email" }'
isEqual: strictEqual
compare:
  input: (a,b)=>a===b?0:a===void 0?b===void 0?0:-1:b===void 0?1:a<b?-1:a>b?1:0
  output: strictCompare
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{try{typeof i==="string"||e[2](i);e[0].test(i)||e[1](i);return i}catch(v0){e[3](v0)}}
    examples:
      valid:
        input: '"a@b.co"'
        output: '"a@b.co"'
      invalid:
        input: '"nope"'
        error: Expected email, received "nope"
      blank:
        input: '""'
        error: Expected email, received ""
  decode:
    expression: i=>{try{i!==void 0||e[2](i);e[0].test(i)||e[1](i);return i}catch(v0){e[3](v0)}}
    examples:
      unset:
        input: undefined
        error: Expected email, received undefined
      valid:
        input: '"a@b.co"'
        output: '"a@b.co"'
  encode:
    expression: i=>{try{e[0].test(i)||e[1](i);return i}catch(v0){e[2](v0)}}
    examples:
      valid:
        input: '"a@b.co"'
        output: '"a@b.co"'
