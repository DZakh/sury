# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.schema(["bar", true])
  input: '["bar", true]'
  output: '["bar", true]'
  instantiations: 603
  bundleBytes: 9382
jsonSchema:
  input: '{ type: "array", minItems: 2, maxItems: 2, items: [{ type: "string", const: "bar" }, { type: "boolean", const: true }] }'
  output: '{ type: "array", minItems: 2, maxItems: 2, items: [{ type: "string", const: "bar" }, { type: "boolean", const: true }] }'
operations:
  parse:
    expression: i=>{Array.isArray(i)&&i.length===2||e[2](i);let v0=i["0"],v1=i["1"];v0==="bar"||e[0](v0);v1===true||e[1](v1);return i}
    examples:
      valid:
        input: '["bar", true]'
        output: '["bar", true]'
      wrong-length:
        input: '["bar", true, false]'
        error: Expected ["bar", true], received ["bar", true, false]
      invalid-item:
        input: '["bar", false]'
        error: 'Failed at ["1"]: Expected true, received false'
  decode: identity
  encode: identity
