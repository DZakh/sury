{
  "name": "e2e",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "res": "rescript watch",
    "test": "pnpm --filter=sury build:entry && rescript && vitest run",
    "ppx:install": "node ../sury-ppx/install.cjs",
    "ppx:build": "cd ../sury-ppx/src && dune build",
    "benchmark:comparison": "pnpm --filter=sury build:entry && vitest bench --run",
    "benchmark:comparison:report": "pnpm --filter=sury build:entry && vitest bench --run --outputJson=bench-results.json"
  },
  "devDependencies": {
    "@codspeed/vitest-plugin": "5.6.0"
  },
  "dependencies": {
    "rescript": "12.2.0",
    "vitest": "4.1.7",
    "sury": "file:../sury",
    "sury-ppx": "workspace:sury-ppx",
    "arktype": "2.1.20",
    "valibot": "1.0.0",
    "zod": "4.0.0-beta.20250420T053007",
    "@sinclair/typebox": "0.34.33"
  }
}
