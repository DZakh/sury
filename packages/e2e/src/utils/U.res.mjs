// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Vitest from "./Vitest.res.mjs";
import * as Stdlib_Dict from "@rescript/runtime/lib/es6/Stdlib_Dict.js";
import * as Primitive_exceptions from "@rescript/runtime/lib/es6/Primitive_exceptions.js";

let noopOpCode = S.decoder(S.unknown, S.unknown).toString();

function throwError(error) {
  throw error;
}

function unsafeGetVariantPayload(variant) {
  return variant._0;
}

let Test = /* @__PURE__ */Primitive_exceptions.create("U.Test");

function throwTestException() {
  throw {
    RE_EXN_ID: Test,
    Error: new Error()
  };
}

function assertThrowsTestException(t, fn, message) {
  try {
    fn();
    return Vitest.Assert.fail(t, "Didn't throw");
  } catch (raw_exn) {
    let exn = Primitive_exceptions.internalToException(raw_exn);
    if (exn.RE_EXN_ID === Test) {
      return Vitest.Assert.pass(t, message);
    } else {
      return Vitest.Assert.fail(t, "Thrown another exception");
    }
  }
}

function assertThrows(t, cb, errorPayload) {
  let any;
  try {
    any = cb();
  } catch (raw_exn) {
    let exn = Primitive_exceptions.internalToException(raw_exn);
    if (exn.RE_EXN_ID === S.Exn) {
      return Vitest.Assert.is(t, exn._1.message, S.$$Error.make(errorPayload).message, undefined);
    }
    throw exn;
  }
  Vitest.Assert.fail(t, "Asserted result is not Error. Recieved: " + JSON.stringify(any));
}

function assertThrowsMessage(t, cb, errorMessage, message) {
  let any;
  try {
    any = cb();
  } catch (raw_exn) {
    let exn = Primitive_exceptions.internalToException(raw_exn);
    if (exn.RE_EXN_ID === S.Exn) {
      return Vitest.Assert.is(t, exn._1.message, errorMessage, message);
    }
    throw exn;
  }
  Vitest.Assert.fail(t, `Asserted result is not S.Exn "` + errorMessage + `". Instead got: ` + JSON.stringify(any));
}

async function assertThrowsAsync(t, cb, errorPayload) {
  let any;
  try {
    any = await cb();
  } catch (raw_exn) {
    let exn = Primitive_exceptions.internalToException(raw_exn);
    if (exn.RE_EXN_ID === S.Exn) {
      return Vitest.Assert.is(t, exn._1.message, S.$$Error.make(errorPayload).message, undefined);
    }
    throw exn;
  }
  return Vitest.Assert.fail(t, "Asserted result is not Error. Recieved: " + JSON.stringify(any));
}

function getCompiledCodeString(schema, op) {
  let toFn = schema => {
    if (op === "Parse") {
      return S.decoder(S.unknown, schema);
    } else if (op === "EncodeToJson") {
      return S.decoder(schema, S.json);
    } else if (op === "Convert") {
      return S.decoder(S.reverse(schema), S.unknown);
    } else if (op === "Assert") {
      return S.decoder(S.unknown, S.to(schema, S.noValidation(S.literal(), true)));
    } else if (op === "EncodeAsync") {
      return S.asyncDecoder(schema, S.unknown);
    } else if (op === "ReverseParse") {
      return S.decoder(S.unknown, S.reverse(schema));
    } else if (op === "ConvertAsync") {
      return S.asyncDecoder(S.reverse(schema), S.unknown);
    } else if (op === "Encode") {
      return S.decoder(schema, S.unknown);
    } else {
      return S.asyncDecoder(S.unknown, schema);
    }
  };
  let fn = toFn(schema);
  let code = {
    contents: fn.toString()
  };
  let defs = schema.$defs;
  if (defs !== undefined && code.contents !== noopOpCode) {
    Stdlib_Dict.forEachWithKey(defs, (schema, key) => {
      try {
        let defFn = toFn(schema);
        code.contents = code.contents + "\n" + (key + `: ` + defFn.toString());
        return;
      } catch (_exn) {
        return;
      }
    });
  }
  return code.contents;
}

function cleanUpSchema(schema) {
  let $$new = {};
  Object.entries(schema).forEach(param => {
    let value = param[1];
    let key = param[0];
    switch (key) {
      case "hasTransform" :
      case "isAsync" :
      case "k" :
      case "of" :
      case "output" :
      case "p" :
      case "r" :
      case "seq" :
        return;
      default:
        if (typeof value === "function") {
          return;
        } else {
          if (typeof value === "object" && value !== null) {
            $$new[key] = cleanUpSchema(value);
          } else {
            $$new[key] = value;
          }
          return;
        }
    }
  });
  return $$new;
}

function unsafeAssertEqualSchemas(t, s1, s2, message) {
  Vitest.Assert.unsafeDeepEqual(t, cleanUpSchema(s1), cleanUpSchema(s2), message);
}

function assertCompiledCode(t, schema, op, code, message) {
  Vitest.Assert.is(t, getCompiledCodeString(schema, op), code, message);
}

function assertCompiledCodeIsNoop(t, schema, op, message) {
  assertCompiledCode(t, schema, op, noopOpCode, message);
}

function assertReverseParsesBack(t, schema, value) {
  Vitest.Assert.unsafeDeepEqual(t, S.parseOrThrow(S.decodeOrThrow(value, schema, S.unknown), schema), value, undefined);
}

function assertReverseReversesBack(t, schema) {
  unsafeAssertEqualSchemas(t, schema, S.reverse(S.reverse(schema)), undefined);
}

let assertEqualSchemas = unsafeAssertEqualSchemas;

export {
  noopOpCode,
  throwError,
  unsafeGetVariantPayload,
  Test,
  throwTestException,
  assertThrowsTestException,
  assertThrows,
  assertThrowsMessage,
  assertThrowsAsync,
  getCompiledCodeString,
  cleanUpSchema,
  unsafeAssertEqualSchemas,
  assertCompiledCode,
  assertCompiledCodeIsNoop,
  assertEqualSchemas,
  assertReverseParsesBack,
  assertReverseReversesBack,
}
/* noopOpCode Not a pure module */
